# FASE 2 — 2.20I DIAGNÓSTICO NO DESTRUCTIVO DEL REFRESH REAL

## Mandato operativo

Base: `0ac6b4a`. El intento 2.20H terminó en `real_refresh_failed`, con Store E.A.ZOCOOL y una Connection activa. Antes del intento: `credential_version=1`, access token vencido, refresh cifrado presente y lease libre. No se debe repetir el refresh, consumir el refresh token, ejecutar OAuth, crear datos de negocio ni hacer listing sync.

### Reglas críticas

- No ejecutar `POST /oauth/token`, `MercadoLibreCredentialService.getValidAccessToken()` sobre la Connection real, OAuth, `/connect`, listing sync ni ninguna operación que rote credenciales.
- No mostrar access/refresh tokens, ciphertext, client secret, master key ni service-role key.
- Sólo consultar estado real read-only; cualquier fixture debe ser local o una transacción con `ROLLBACK`.

### Diagnóstico requerido

Inspeccionar el camino A–M: resolver Connection; leer `integration_secrets`; descifrar; comprobar expiración; reclamar lease; releer/double-check; construir request; POST al proveedor; parsear respuesta; validar Zod; cifrar; completar CAS; liberar lease/error cleanup. Determinar qué etapa puede producir `real_refresh_failed` sin volver a invocar Mercado Libre.

Auditar configuración sin imprimir valores: `MERCADO_LIBRE_CLIENT_ID`, `MERCADO_LIBRE_CLIENT_SECRET`, `INTEGRATION_SECRETS_MASTER_KEY`. Confirmar descifrado server-side de access y refresh sin usar el refresh contra el proveedor. Auditar estáticamente claim/complete/release y el contrato `grant_type=refresh_token`, client id/secret, refresh token, `application/x-www-form-urlencoded`, URL oficial y timeout de 15 s.

Revisar evidencia segura del intento anterior (logs, stderr/stdout, error normalizado, HTTP status preservado, Sentry disponible y documentación 2.20H). Si no existe evidencia suficiente para saber si llegó a `/oauth/token`, declararlo `UNKNOWN`. No exponer body crudo.

Si la observabilidad es insuficiente, puede prepararse instrumentación mínima y segura con tests focalizados para claim, descifrado, HTTP 4xx/5xx, timeout/network, respuesta inválida, refresh ausente y CAS. No crear migraciones. Si se modifica TypeScript, ejecutar tests focalizados, typecheck y lint; build sólo si corresponde. No cerrar Git.

### Resultado de este diagnóstico

- Configuración: Client ID `SET`; Client Secret `SET`; PKCE `false`; redirect HTTPS exacta sin query/hash.
- Master key: `VALID` (Base64URL de 32 bytes; valor no mostrado).
- Descifrado server-side: access `PASS`; refresh `PASS`.
- Estado remoto read-only: `credential_version=1`; lease libre; access expirado; refresh cifrado presente.
- Evidencia del intento: sólo `real_refresh_failed`; no existe status HTTP/provider code preservado ni log seguro que confirme llegada al endpoint. Por tanto, llegada a `/oauth/token`: `UNKNOWN`.
- No hubo retry, nueva escritura confirmada ni listing sync; no se modificó código ni migration.
- Clasificación: `C. OBSERVABILITY_INSUFFICIENT_NEEDS_SAFE_INSTRUMENTATION`.
- Acción mínima antes de un único retry futuro: añadir clasificación server-side segura (sin secretos) que distinga configuración, descifrado, claim, red/timeout, HTTP del proveedor, respuesta inválida, cifrado, CAS y release; después obtener aprobación explícita para un único retry.

### Checkpoint obligatorio

Reportar etapa probable/exacta; si se llegó a `/oauth/token`; indicadores de configuración; validez de master key y descifrado; versión/lease/refresh presente; contrato y encoding; status/provider code previo; suficiencia de observabilidad; archivos y migraciones modificados; validaciones; remote writes; retry; listing sync; clasificación; acción mínima; documentación; `git diff --check`; y confirmar `git add/commit/push = NO`.

## Estado archivado

El mandato 2.20I queda archivado aquí. Se preserva `docs/prompts/phase-02/2.20e-first-real-listing-sync.md` sin stagear.
