# Cierre Subfase 2.5 — Connections

Status: **COMPLETED**

Purpose:
Cerrar y versionar Connections provider-agnostic sin OAuth ni secretos.

Result:
Connections auditadas: provider canónico, estados `active|disabled`, FK compuesta tenant-safe, reserva global de cuenta activa, repository server-only, tests y documentación. Validación correcta; commit pendiente.

Commit:
PENDING

## Prompt

Mandato de cierre 2.5: auditar y versionar la migración `20260822202032_phase_2_connections.sql`, ConnectionRepository, tests, documentación y prompt 2.5; ejecutar test/typecheck/lint/build/diff check; hacer staging explícito, commit `feat(database): add provider connections foundation` y push a `develop`. Confirmar esquema, providers, `active|disabled`, unicidad global de cuenta activa, FK `(store_id, organization_id)` con restrict, ausencia de secretos/OAuth, DB sin ejecutar, Store Scope sin rediseño, Bootstrap Owner deferred y Fase 3 no iniciada. Mantener contenidos Obsidian en español UTF-8 y no tocar labels/templates/IDs internos multilenguaje. No crear OAuth, tokens, API Mercado Libre, resolver, RLS, rate limit, Docker ni operaciones Supabase remotas.

## Commit

PENDING
