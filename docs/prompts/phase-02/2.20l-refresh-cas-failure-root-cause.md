# FASE 2 — 2.20L REFRESH CAS FAILURE ROOT CAUSE & FIX

Base: `a990a1d`. El segundo refresh real llegó a una respuesta válida y a credenciales rotadas en memoria, pero terminó en `CAS_COMPLETE / REFRESH_CAS_FAILED`; la fila remota permanece en versión 1 y el refresh persistido se considera potencialmente consumido. No ejecutar refresh real, reconnect, listing sync, OAuth, `GET /users/me`, DB push ni migración remota.

## Mandato

Antes de 2.20L, auditar sólo lectura los cambios manuales de `docs/prompts/phase-01/`; no corregir, stagear, borrar ni incluirlos. También preservar `docs/prompts/phase-02/2.20e-first-real-listing-sync.md`.

Encontrar y demostrar la causa exacta de la falla CAS. Inspeccionar el servicio, repository claim/complete/release, migration `20260825100000_safe_integration_secret_refresh_rotation.sql`, triggers, firmas RPC y argumentos TypeScript. Reconstruir versión leída, versión de claim, versión devuelta, reread, expectedVersion de complete, lease ID, WHERE/CAS, triggers y estado esperado. Validar expresamente si claim/release actualizan la versión, si triggers consideran lease como cambio de credencial, si hay mismatch de versión/lease/tipo o si reconnect afecta el flujo.

Reproducir localmente con fixtures sintéticos: versión inicial, claim, reread, provider falso, cifrado falso y complete CAS. Demostrar el fallo antes del fix, sin secretos ni llamadas a Mercado Libre. El arreglo mínimo debe mantener un refresher activo, no mantener transacción durante HTTP, impedir stale writers, reservar `credential_version` para cambios reales de credenciales, no invalidar CAS por lease técnico, invalidar writers por reconnect, exigir ownership de lease y preservar server-only/RLS/service-role.

Si se requiere SQL, crear migration forward-only sin editar migraciones aplicadas. Agregar tests de claim, complete, bump de versión, stale writer, lease, concurrencia, reconnect, release y provider mock end-to-end. Ejecutar reset/matriz local si aplica, tests focalizados, typecheck, lint y `git diff --check`. No hacer Git closure ni cambios remotos.

## Auditoría manual de Fase 1

Los prompts manuales de Fase 1 son intención del usuario. Deben clasificarse por archivo con propósito, exactitud histórica, inconsistencias, contradicciones con arquitectura/seguridad, duplicación, naming, gaps, riesgo y recomendación `KEEP`, `KEEP_WITH_FUTURE_EDIT`, `REVIEW` o `REMOVE`; ningún cambio sobre ellos está autorizado.

## Resultado local — sin fix especulativo

La reproducción local completa con el mismo servicio TypeScript, repository, cliente Supabase, migration, RPC y trigger pasó: versión `1 → 2`, lease liberado y access fixture vigente. La firma y el hash de definición de `complete_integration_secret_refresh`, así como el trigger de `integration_secrets`, son idénticos en local y remoto. Claim no cambia `credential_version`; sólo los cambios de credenciales lo incrementan.

No existe evidencia preservada del error SQL/condición concreta de la ejecución remota 2.20K. Por tanto, `REFRESH_CAS_FAILED` no puede atribuirse honestamente a un trigger, versión, lease, tipo o RPC sin inventar una causa. No se modifica código productivo, migration ni remoto. Antes de un reconnect, se requiere una futura ejecución diagnóstica que preserve de forma segura si el complete falla por error RPC o por CAS condicionado, sin volver a usar el refresh token potencialmente consumido.
