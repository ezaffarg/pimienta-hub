# 2.7B — Database Runtime Validation Retry

Status: COMPLETED

Purpose:
Reintentar la primera validación real de PostgreSQL/Supabase local después del bloqueo 429 del registry y normalizar nombres de prompts de cierre/readiness.

Context:

- Fase 2 code/design complete;
- runtime validation sigue pendiente;
- Docker operativo;
- usuario aceptó autorización adicional de Docker/Windows;
- primer `supabase start` falló por 429.

Result:
LOCAL VALIDATED. Docker 29.7.2 y Supabase CLI 2.114.0 iniciaron el stack local. Dos `db reset` aplicaron las migraciones 20260821230525 y 20260822202032 desde cero. La matriz transaccional con fixtures falsos verificó roles, Stores, FKs compuestas, ON DELETE RESTRICT, Connections cross-tenant, allowlists, defaults, unicidad parcial activa, liberación disabled, NULL y scopes; finalizó en rollback. Remote permanece no enlazado; Bootstrap Owner, fallback Clerk y RLS siguen diferidos.

Commit:
PENDING

---

# FASE 2 — DATABASE RUNTIME VALIDATION RETRY
# + NORMALIZACIÓN DE NOMBRES DE PROMPTS
# + REINTENTO SUPABASE LOCAL TRAS AUTORIZACIÓN DOCKER

Proyecto: e-ngenieria Hub. Rama: `develop`. Commit base: `1444268 docs(phase-2): close persistence readiness`.

Estado: Fase 2 CLOSED — CODE/DESIGN COMPLETE; runtime DB validation pendiente/bloqueada en primer intento. El primer `bunx supabase start` falló antes de crear servicios por `429 Too Many Requests` durante la descarga de imagen oficial. Docker 29.7.2, Docker Compose v5.4.0 y Supabase CLI 2.114.0. No hay login, link, db push ni proyecto remoto. Los cambios locales son sólo documentación del primer intento.

`docs/meli-database.md` sigue siendo el documento canónico y activo de persistencia; `docs/database-runtime-validation.md` es únicamente el registro de checkpoint/pruebas reales.

## Objetivos

1. Archivar este prompt.
2. Normalizar nombres históricos de prompts 2.x.
3. Actualizar índices y referencias.
4. Reintentar Supabase local.
5. Si inicia, ejecutar validación runtime.
6. Mantener `meli-database.md` actualizado.
7. Actualizar Obsidian.
8. Detenerse antes de git add/commit/push y sin iniciar Fase 3.

## Normalización requerida

- Renombrar con `git mv` `2.x-phase-close-readiness.md` a `2.6-phase-close-readiness.md`.
- Renombrar con `git mv` `2.x-phase-close-commit.md` a `2.6b-phase-close-commit.md`.
- Renombrar el prompt no versionado `2.runtime-db-validation.md` a `2.7-runtime-db-validation.md`, sin perder Prompt, Result, Status ni Commit.
- Buscar y actualizar referencias legítimas a los tres paths; actualizar `docs/prompts/phase-02/README.md` en orden cronológico. No reescribir texto histórico salvo paths.
- En `docs/prompts/README.md`, si aún falta, establecer: evitar placeholders permanentes `2.x`; usar número/subfase cronológica; usar `b`/`c` o `-retry`/`-close`; archivar todos los prompts antes de actuar. Ejemplo: 2.5, 2.5-close, 2.6, 2.6b, 2.7, 2.7b.

## Auditoría y prechecks

Auditar `git status --short`, `git diff --stat` y `git diff`; los cambios previos deben ser documentales. Si aparece código/schema/dependencia inesperada, detenerse. Ejecutar `docker --version`, `docker compose version`, `docker info` y opcionalmente `docker ps`, sin borrar imágenes, prune, cambios de Docker ni Docker MCP. Ejecutar `bunx supabase --version`, revisar `supabase/config.toml` sin cambios innecesarios y confirmar configuración local, sin project_ref remoto ni secretos.

## Runtime autorizado, local solamente

Ejecutar un único `bunx supabase start` y permitir descargas oficiales. Si vuelve a fallar por 429, no repetir en bucle, no cambiar registry/mirror ni usar login remoto; registrar bloqueo y detener validación runtime. Si inicia: registrar sólo `STACK_LOCAL_RUNNING = true`, usar `bunx supabase status` sin divulgar claves y ejecutar `bunx supabase db reset` local para aplicar 20260821230525 y 20260822202032. Ante error SQL no editar migraciones silenciosamente: informar archivo, error y solución mínima.

Con reset correcto, verificar realmente tablas `hub_memberships`, `stores`, `store_assignments`, `connections`, sus PK/NOT NULL/CHECK/UNIQUE/FK/índices, y con fixtures falsos: membership válida/duplicado/roles; Store activa-disabled-status inválido-nombre vacío-nombre duplicado; assignment A→A y cross-tenant rechazado; delete restrict de Store/membership; Connection tenant-safe; provider allowlist; status/default; unicidad parcial global activa/liberación disabled/múltiples disabled; NULL external account; `scopes text[]` y default. Ejecutar segundo `db reset` para reproducibilidad.

No implementar Bootstrap First Owner: cardinalidad Owner sigue ambigua; no crear unique partial. Mantener recomendación advisory lock transaccional por Organization + re-check + insert, que requerirá RPC/function/migration posterior. Mantener fallback `org:admin → Owner`, `org:member → Employee`; para retirarlo faltan DB runtime validada, bootstrap seguro, membership persistence y transición probada. No habilitar RLS, rate limiting ni migración nueva.

Actualizar resultados reales en `meli-database.md`, `database-runtime-validation.md`, `plan-y-gobierno.md`, `codex-handoff.md`, README de phase-02 y Obsidian sin claves, passwords, JWT, tokens, SQL completo ni logs completos. Si pasa: LOCAL VALIDATED y Remote NOT LINKED/NOT VALIDATED; si falla: BLOCKED — IMAGE REGISTRY 429. Ejecutar test, typecheck, lint, build, diff check/status/diff; no formateo masivo. Actualizar 2.7 a BLOCKED BY REGISTRY 429 y este 2.7b a COMPLETED con resultado real; Commit PENDING.

Prohibido: git add/commit/push; login/link/db push/remoto/producción/staging; OAuth/tokens/Mercado Libre/StoreIntegrationResolver/Fase 3; RLS, rate limiting, Bootstrap Owner, migración adicional, Docker MCP/extensiones; reintentos indefinidos de start.

El checkpoint debe informar los 48 resultados solicitados: archivo/renames/índices/documentos canónicos, Docker y start, stack/reset/migraciones/schema/constraints/matriz runtime/reproducibilidad, Owner/fallback, documentación/Obsidian, validaciones, Git/Supabase final y todas las confirmaciones de límites.

## References

Evidence: [database-runtime-validation.md](../../database-runtime-validation.md). Operational index: [README.md](./README.md)
