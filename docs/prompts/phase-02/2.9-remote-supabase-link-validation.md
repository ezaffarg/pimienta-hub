# 2.9 — Remote Supabase Link and Validation

Status: COMPLETED

Purpose:
Vincular de forma controlada el repositorio con el proyecto Supabase remoto dedicado a e-ngenieria Hub, aplicar las migraciones versionadas y validar el schema remoto antes de iniciar Fase 3/OAuth.

Remote project ref:
ffcudwwrzttkumbdvada

Result:
Project `ffcudwwrzttkumbdvada` linkeado; tres migrations aplicadas y alineadas. Schema, constraints y bootstrap funcional validados remotamente con fixtures ficticios limpiados; conteos finales cero. Concurrencia permanece validada localmente y no se repitió remota. Fallback transitorio; Fase 3 no iniciada.

Commit:
PENDING

---

Mandato 2.9: confirmar `.env.local` ignorado/no tracked y presencia de variables sin exponer valores; verificar CLI 2.114.0 y flags; login/link controlado exclusivamente a `ffcudwwrzttkumbdvada`; comprobar target y migration history; aplicar sólo las tres migrations autorizadas mediante `db push`; validar schema, constraints y RPC bootstrap remotos con fixtures ficticios, limpiarlos sin reset remoto; documentar/Obsidian/validar app y detenerse sin Git write. Prohibidos cualquier otro target, reset remoto, schema manual, OAuth, RLS, rate limiting y Fase 3.

## Continuación — matriz remota verificable

No repetir login, link ni push. Cada assertion crítica se ejecuta y registra por separado, con fixtures `remote_validation_29_*` únicos y limpieza específica inmediata; sin truncate, reset remoto ni borrado genérico. Se distinguen concurrencia local validada y bootstrap remoto funcional. Al concluir se actualizan documentación/Obsidian, validaciones de aplicación y este prompt, sin Git write.

## Continuación — resto de matriz remota

Continuar desde unique Organization/user hasta bootstrap funcional y cleanup final, sin repetir roles, login, link ni `db push`. Cada resultado debe ser observable por separado y toda fixture debe usar el prefijo exclusivo `remote_validation_29_` y eliminarse por identificador exacto, respetando FKs. No ejecutar concurrencia remota si requiere infraestructura adicional: se documenta concurrencia local validada y bootstrap remoto funcional validado.

## Cierre formal 2.9

Remote linked y validado sin reset remoto. Migrations: 20260821230525, 20260822202032 y 20260824120039. Constraints y bootstrap funcional pasaron con limpieza de fixtures; no se expusieron secretos, no hubo OAuth ni Fase 3. Commit pendiente.
