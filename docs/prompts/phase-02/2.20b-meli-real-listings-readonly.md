# FASE 2 — 2.20B MERCADO LIBRE REAL LISTINGS READ-ONLY

Base `bcb50b5`. Estado: Store `E.A.ZOCOOL`, Connection Mercado Libre activa y secretos server-only; `/users/me` real e identity match PASS; refresh no requerido.

Objetivo: primera lectura real read-only de publicaciones de la Connection persistida. Resolver server-side Organization → Store → Connection activa → secretos; no aceptar autoridad desde browser. Usar endpoint oficial basado en seller/user id verificado, paginación y máximo 20 items. Preferir multiget oficial para detalle. No scraping, MercadoCuentas, endpoints privados ni mutaciones.

Reutilizar la lógica segura de token 2.20A. Si expira, detenerse antes de refresh real si no existe protección concurrente segura. Extraer en memoria únicamente campos mínimos: id, title, status, price, currency, stock, vendidos, tipo, permalink, thumbnail, catálogo, SKU si está oficialmente disponible y condición. Investigar fuente oficial de SKU; no inventar mapping. No crear tablas/migrations de productos/listings/inventario.

Implementar sólo DTO interno provider-agnostic y primitive/service server-only reutilizable, sin capas nuevas ni UI. Agregar tests de búsqueda, paginación, respuesta inválida, normalización, opcionales, redacción e identity/Connection binding si cambia TypeScript. Ejecutar tests focalizados, typecheck y lint; build sólo si cambia route/UI. Una única ejecución real con máximo 20 items, sin POST/PUT/PATCH/DELETE ni otras mutaciones.

Actualizar sólo documentación de API/listings existente y este prompt, sin secretos. No Git closure. El checkpoint debe informar endpoints oficiales, token/refresh, conteos, muestra no sensible de hasta 10, estrategia SKU, DTO, cero migrations/writes/mutaciones, validaciones y Git.

## Resultado — 2.20B real listings read-only

Se implementó `MercadoLibreListingsService` server-only. Antes de leer credenciales verifica el binding Organization + Store + Connection, proveedor Mercado Libre, estado activo y `external_account_id` persistido. La primitive no recibe esas autoridades desde navegador y no implementa refresh: si el access token vence, se detiene sin llamada al proveedor ni escritura.

La única ejecución real hizo `GET /users/{seller_id}/items/search?limit=20&offset=0` seguido del multiget oficial `GET /items?ids=...&attributes=...`. Devolvió una página de 1/1 publicación y no hubo cursor siguiente. La muestra normalizada mantuvo únicamente metadatos públicos mínimos; no hubo `SELLER_SKU` en la publicación consultada. Conforme al contrato oficial, el atributo `SELLER_SKU` es la fuente de SKU y `seller_custom_field` no se usa como sustituto.

Postcheck remoto read-only: Stores 1, Connections 1 activa, Assignments 0, `integration_secrets` 1 y memberships 1. No hubo refresh, rotación de secreto, migración, escritura en Supabase, mutación de negocio, UI, sincronización ni exposición de tokens, códigos, estados, ciphertext o claves.

Validaciones locales: tests focalizados 6/6 PASS; typecheck PASS; lint PASS. Build no aplica porque no cambió ruta ni UI. Pendiente únicamente el cierre Git autorizado por una fase posterior.

## Cierre — 2.20B real listings read-only

# CIERRE 2.20B — REAL LISTINGS READ-ONLY

Estado aprobado: Store `E.A.ZOCOOL`; Connection activa; lectura real de listings PASS; 1 publicación informada y recuperada; límite 20; búsqueda oficial y detalle multiget oficial; SKU sólo desde `SELLER_SKU`; `seller_custom_field` separado; refresh, escrituras remotas y mutaciones Mercado Libre: NO.

Validación previamente aprobada: tests focalizados 6/6 PASS; typecheck PASS; lint PASS; build no requerido; higiene de secretos PASS.

Para el cierre: auditar exclusivamente los ocho archivos 2.20B; no repetir tests, typecheck, lint, build, reset/push de Supabase ni lectura real. Stagear los archivos explícitamente, crear `feat(meli): add readonly listings integration`, hacer push a `origin/develop` y confirmar `HEAD == origin/develop` con árbol limpio. No crear persistencia de catálogo/listings, workers, webhooks, sincronización, mutaciones de publicaciones, OAuth nuevo ni Fase 3.
