# Database Runtime Validation — Inicio

Status: COMPLETED

Purpose:
Ejecutar por primera vez las migraciones de Fase 2 contra PostgreSQL/Supabase local y validar constraints reales.

Context:

- Fase 2 code/design complete.
- Runtime DB validation pendiente.
- Docker ahora disponible.
- Supabase CLI 2.114.0 instalada.
- no existe link remoto.
- no se iniciará OAuth.

Restrictions:

- local only;
- no login;
- no link;
- no db push;
- no proyecto remoto;
- no Fase 3.

Result:
BLOCKED BY REGISTRY 429 ON FIRST ATTEMPT. El retry posterior 2.7B inició correctamente el stack local, ejecutó ambos resets y validó las constraints reales. Bootstrap First Owner, RLS y el fallback Clerk permanecen sin cambios. No hubo login, link, db push ni acceso remoto; Fase 3 no inició.

Commit:
PENDING

---

# DATABASE RUNTIME VALIDATION — INICIO CONTROLADO
# FASE 2: RUNTIME DB VALIDATION PENDING → VALIDACIÓN LOCAL REAL
# ARCHIVAR ESTE PROMPT ANTES DE CUALQUIER ACCIÓN

Proyecto: e-ngenieria Hub
Rama: develop
Commit base: `1444268 docs(phase-2): close persistence readiness`

Estado previo aprobado: FASE 2 CLOSED — CODE/DESIGN COMPLETE; RUNTIME DB VALIDATION PENDING.

Docker disponible: Docker 29.7.2 y Docker Compose v5.4.0. Supabase CLI del proyecto: 2.114.0 exacta como devDependency.

Estado remoto: NO `supabase login`; NO `supabase link`; NO `db push`; NO proyecto remoto conectado desde el repo.

Migraciones existentes:

- `supabase/migrations/20260821230525_phase_2_store_foundation.sql`
- `supabase/migrations/20260822202032_phase_2_connections.sql`

## Objetivo

1. Archivar este prompt.
2. Registrar formalmente en documentación el INICIO de Runtime DB Validation.
3. Iniciar Supabase local mediante Docker.
4. Ejecutar las migraciones desde cero.
5. Validar constraints reales en PostgreSQL.
6. Probar aislamiento tenant/cross-tenant.
7. Analizar y resolver de forma segura el Bootstrap First Owner si es posible.
8. Actualizar documentación y Obsidian.
9. Detenerse antes de commit/push.
10. NO tocar Supabase remoto.
11. NO iniciar Fase 3.

## Reglas operativas

Antes de levantar Supabase local, actualizar mínimamente `docs/plan-y-gobierno.md`, `docs/codex-handoff.md`, `docs/meli-database.md` y `docs/prompts/phase-02/README.md` con `DATABASE RUNTIME VALIDATION — STATUS: ACTIVE`; registrar que es la primera ejecución real del schema contra PostgreSQL local por Supabase CLI y Docker, para ejecutar migraciones, validar constraints y aislamiento tenant, y diseñar Bootstrap First Owner sin entorno remoto ni Fase 3.

Quedan preautorizados: lectura/escritura local, documentación, Obsidian, comandos Docker no destructivos relacionados con Supabase local, `bunx supabase start`, `status` y `db reset`, pruebas, checks de aplicación, Git de solo lectura, logs y SQL contra la DB local descartable. No están autorizados: `supabase login`, `link`, `db push`, proyecto remoto, producción, staging remoto, secretos reales, force push, `main` u operaciones destructivas fuera del entorno local.

Prechecks obligatorios: `docker --version`, `docker compose version`, `docker info`; detener si el daemon no responde. Desde el repo: `bunx supabase --version`, `bunx supabase status`, revisión de `supabase/config.toml`, sin modificar configuración innecesaria, sin `project_ref` remoto ni secretos versionados. Releer ambas migraciones completas, comprobar el orden lógico `hub_memberships`, `stores`, `store_assignments`, `connections`, y confirmar que no fueron modificadas accidentalmente.

Ejecutar `bunx supabase start` (se permite descargar imágenes oficiales) y registrar resultado, servicios, PostgreSQL y warnings. Los valores locales que revele la CLI pueden ser claves de desarrollo: no copiarlos a documentación, Obsidian, checkpoint ni repositorio. Luego ejecutar `bunx supabase db reset`; registrar éxito o fallo. Ante fallo SQL, no modificar migraciones automáticamente: informar archivo, statement, error, causa probable, cambio mínimo e impacto; no usar `db push` como workaround.

Con reset correcto, consultar PostgreSQL real para confirmar tablas, PK, NOT NULL, CHECK, UNIQUE, FK e índices de `hub_memberships`, `stores`, `store_assignments` y `connections`.

## Validaciones runtime obligatorias

- `hub_memberships`: inserción válida; duplicado `(organization_id, clerk_user_id)` falla; rol inválido falla; Owner, Manager, Employee y Client son aceptados. Usar IDs ficticios deterministas.
- `stores`: store válida; nombre vacío falla; status `active` y `disabled` válidos; status desconocido falla; mismo nombre dentro de una Organization permitido.
- `store_assignments`: Organization A/B, Membership A/B, Store A/B; A→A funciona; A→Store B con organization_id inconsistente falla por FKs compuestas.
- `ON DELETE RESTRICT`: eliminar Store o Membership referenciada por assignment falla; limpiar datos de prueba en orden seguro si corresponde.
- `connections`: Connection válida A/Store A/provider `mercado-libre`/active/account fake; conexión con organization A y Store B falla por FK compuesta. Providers válidos: mercado-libre, shopify, tiendanube, woocommerce; inválido falla. Status `active`/`disabled` válidos, otro falla, default `disabled` se confirma. La unicidad parcial global activa `(provider, external_account_id)` falla incluso cross-Organization; al desactivar la primera permite una segunda activa; múltiples disabled se permiten. Múltiples `external_account_id = NULL` se permiten. `scopes` acepta text[] y confirma default, usando valores fake como `read:test` y `write:test`.

Evaluar la compatibilidad de repositories contra DB real sin alterar arquitectura. Se permite env temporal seguro sólo si no se versiona y está ignorado; no registrar claves locales. Si es limpio, agregar tests de integración separados (tenant-scoped lists, Store cross-tenant invisible, assignments y connections tenant-safe, active account uniqueness) sin hacer que `bun run test` dependa de Docker; no crear scripts por costumbre. Si no es viable sin cambios excesivos, documentar el gap.

## Bootstrap First Owner

Analizar la carrera de dos `org:admin`: SELECT sin Owner + INSERT Owner. Considerar índice único parcial (sólo si el dominio exige un único Owner total), advisory transaction lock por `organization_id`, u otra estrategia transaccional. Revisar documentación: “primer Owner” no decide cardinalidad futura. Si Owner puede ser uno o más, no crear unique Owner; recomendar lock advisory que serialice bootstrap. No implementar SQL/RPC ni migration nueva sin checkpoint si lo requiere. Mantener fallback Clerk `org:admin → Owner`, `org:member → Employee` y explicar qué falta para retirarlo.

Validar que repositories usan query builder y no SQL concatenado; SQL manual sólo para tests; service-role local no se versiona. No habilitar RLS, rate limiting ni defensas generales de prompt injection; registrar hallazgos. RLS queda como defense-in-depth futuro.

Al terminar las pruebas manuales, ejecutar un segundo `bunx supabase db reset`, `bunx supabase status` y documentar si queda RUNNING o STOPPED. No ejecutar `supabase stop --no-backup` salvo necesidad concreta.

Actualizar resultados reales en `docs/meli-database.md`, `docs/meli-multi-tenancy.md`, `docs/plan-y-gobierno.md`, `docs/codex-handoff.md`, `docs/prompts/phase-02/README.md` y crear `docs/database-runtime-validation.md` si aporta claridad. Si todo pasa, clasificar Fase 2 como CODE/DESIGN COMPLETE, Database Runtime LOCAL VALIDATED y Remote Database NOT VALIDATED / NOT LINKED; nunca production-ready ni Fase 3. El documento de resultado no contiene URLs, claves, tokens, passwords, SQL ni logs completos sensibles.

Si está disponible Obsidian MCP, actualizar `current-state`, `decisions` y `open-questions` en español UTF-8, de forma condensada: Docker, ejecución local, estado de migraciones/constraints, Bootstrap Owner, remoto sin link, Fase 3 no iniciada; no guardar claves/JWT/passwords/URLs sensibles/SQL/logs completos. Git/repo sigue siendo source of truth.

Tras cambios permitidos ejecutar `bun run test`, `bun run typecheck`, `bun run lint`, `bun run build`, `git diff --check`, `git status --short` y `git diff`, sin formato masivo. Actualizar este archivo a `Status: COMPLETED` sólo si el checkpoint finaliza, con resultado de Docker/Supabase/reset/migraciones/constraints/tenant isolation/uniqueness/repos/Bootstrap/RLS/remoto; `Commit: PENDING`.

No hacer `git add`, `git commit`, `git push`, ni iniciar OAuth/callback/tokens/API Mercado Libre/StoreIntegrationResolver/Fase 3, RLS o rate limiting. El checkpoint final debe informar los 49 puntos del mandato, incluidos resultados de cada constraint, reset repetido, Bootstrap/Owner cardinality/estrategia, fallback Clerk, RLS, hardening, docs/Obsidian, validaciones de aplicación, estado Git/Supabase y confirmaciones de no login/link/db push/remote/Fase 3/operaciones Git.

## References

Evidence: [database-runtime-validation.md](../../database-runtime-validation.md). Schema: [meli-database.md](../../meli-database.md)
