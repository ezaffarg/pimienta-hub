# FASE 2 — 2.20E FIRST REAL LISTING SYNC

Base `359093c`. Se autoriza la primera persistencia real y controlada: Mercado Libre read-only → `ExternalListingSummary` → `ListingSyncService` → `public.listings`. El objetivo es exactamente dos lecturas/syncs de hasta 20 publicaciones para demostrar que el primer sync deja `listings = 1` y el segundo la mantiene en 1, sin duplicado.

Precheck obligatorio: árbol Git limpio y sincronizado; Owner 1, Store E.A.ZOCOOL 1, Connection Mercado Libre activa 1, assignments 0, secreto cifrado 1 y listings 0. La Connection, Organization, Store y secretos se resuelven exclusivamente server-side. No se aceptan IDs ni tokens del navegador. Si el token estuviera vencido y no hay protección segura de concurrencia para refresh, detenerse; no improvisar refresh.

Usar la primitive 2.20B read-only y persistir sólo summaries normalizados. `SELLER_SKU` ausente es válido (`seller_sku = null`); nunca usar `seller_custom_field`. Se permiten únicamente dos INSERT/UPSERT en `public.listings` mediante `ListingSyncService`. No guardar payload crudo, tokens, respuestas OAuth ni secretos; no generar audit event por este sync read-only.

No borrar por ausencia, reconciliar, crear migrations, tocar la Connection, operar Mercado Libre en modo write, modificar precio/stock/status, crear UI, workers, cron, webhooks o Fase 3. Si la foundation falla, deja datos parciales, requiere cambio de código o muestra leakage, detenerse antes de corregir.

Actualizar sólo `docs/meli-database.md` y este prompt. No ejecutar tests, typecheck, lint, build ni cierre Git si no hay cambios de código. Confirmar finalmente bindings, datos mínimos no sensibles, idempotencia, timestamps, conteos, ausencia de mutaciones Mercado Libre, higiene y `git diff --check`; no `git add`, commit ni push.

## Resultado — STOP por access token vencido

El precheck remoto pasó: Owner 1, Store E.A.ZOCOOL 1, Connection Mercado Libre activa 1, assignments 0, `integration_secrets` 1 y listings 0. La resolución se hizo server-side. Antes de llamar al provider o persistir se comprobó la expiración de las credenciales cifradas en memoria: el access token está vencido.

La foundation actual no dispone de una protección de concurrencia aprobada para refresh rotation. Por lo tanto, conforme al stop condition, no se ejecutó refresh, ninguna lectura adicional de Mercado Libre, sync, INSERT/UPSERT ni otro write remoto. Listings continúa en 0. Se retiraron las sondas temporales; no hubo exposición de secretos ni cambios de código persistente, y no se ejecutó cierre Git.

## Reanudación tras 2.20R

El bloqueo original por credenciales expiradas quedó resuelto mediante el
reconnect real target-bound cerrado en 2.20R. La Connection real conserva la
misma Store y la misma identidad externa, `credential_version` es 2 y el lease
quedó libre. La lectura posterior `GET /users/me` pasó con identidad coincidente.

La primera sincronización real vuelve a estar autorizada usando exclusivamente
la credencial persistida actual y el runtime canónico. Permanecen prohibidos un
nuevo OAuth, reconnect, credenciales legacy, migrations, escrituras en Mercado
Libre, cambios funcionales y cierre Git antes del checkpoint.

## Resultado — primer listing sync real exitoso

El precheck confirmó la base aprobada, Store 1, Connection Mercado Libre activa
1, secreto 1, `credential_version=2`, lease libre y Listings 0. El access token
persistido superaba la ventana de seguridad del runtime, por lo que se reutilizó
directamente y no hubo refresh. La identidad oficial ya verificada después de
2.20R permaneció ligada a la misma Connection; no se duplicó `GET /users/me`.

La ejecución canónica hizo seller search oficial con límite 20 y multiget de
detalle, recuperó 1 ID y normalizó 1 summary. `ListingSyncService` persistió 1
listing tenant-scoped. El primer postcheck confirmó Store/Connection/provider,
datos esenciales completos, unicidad y 0 duplicados.

Una segunda sincronización controlada recuperó el mismo listing y ejecutó el
mismo upsert. La fila y `external_listing_id` permanecieron estables, el
timestamp de sync avanzó y Listings continuó en 1. El postcheck final conservó
Store 1, Connection activa 1, secreto 1, `credential_version=2`, lease libre,
bindings correctos y 0 duplicados. Mercado Libre recibió sólo lecturas; no hubo
OAuth, reconnect, refresh, migration, cambios productivos ni cierre Git.

Resultado: `FIRST_REAL_LISTING_SYNC_SUCCESS`. Recomendación:
`READY_TO_CLOSE_2.20E`.
