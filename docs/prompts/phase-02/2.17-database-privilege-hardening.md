# Fase 2 — 2.17 Database Privilege Hardening + Remote Migration Readiness

> Security checkpoint. Base: checkpoint 2.16 pendiente de commit. No ejecutar `db push` remoto todavía.

## Estado de entrada

- Foundation migration: `20260824184934_oauth_security_foundations.sql`.
- Foundations: `oauth_attempts`, `integration_secrets`, `audit_events`, RPCs de onboarding, unicidad definitiva de Connection, secretos cifrados y audit foundation.
- Base local validada; 89/89 tests, typecheck, lint y build PASS.
- La migration remota no está aplicada y no hubo escrituras remotas.
- Supabase advierte RLS disabled. Antes de cualquier aplicación remota se requiere hardening explícito de acceso DB.

## Mandato

Browser, `anon` y `authenticated` no deben acceder directamente a foundations sensibles ni ejecutar RPCs internas. El backend e-Hub server-only continúa usando `service_role`. Supabase Auth no se usa y Clerk continúa siendo el proveedor de autenticación.

1. Auditar grants efectivos, owner, `PUBLIC`, `anon`, `authenticated`, `service_role`, `SECURITY DEFINER`/`INVOKER` y `search_path` de `oauth_attempts`, `integration_secrets`, `audit_events`, `stores`, `connections`, `store_assignments`, `hub_memberships` y todas las RPC mutantes de Fase 2, especialmente `bootstrap_first_owner` y los onboarding admin/client. No asumir defaults.
2. Habilitar RLS sin policies para `oauth_attempts`, `integration_secrets` y `audit_events`: acceso directo de anon/authenticated deny-by-default, manteniendo el acceso server-only requerido.
3. Evaluar también las tablas core. Si RLS deny-by-default es compatible con todo el acceso actual por `service_role`, recomendarlo/aplicarlo; no hacerlo si rompe primitives, tests o acceso existente, y explicar una duda.
4. Revocar acceso directo innecesario de `PUBLIC`, `anon` y `authenticated` sobre tablas server-only; preservar únicamente `service_role` y owner/migration role necesarios.
5. Revocar `EXECUTE` de `PUBLIC`, `anon` y `authenticated` para toda function/RPC interna de provisioning, onboarding o mutación; otorgarlo sólo a `service_role` cuando corresponda. Nunca asumir que `CREATE FUNCTION` es seguro por defecto.
6. Revisar toda function `SECURITY DEFINER`: usar `SECURITY INVOKER` si no es necesaria; si lo es, fijar `search_path` seguro, schema-qualify objetos, validar inputs y limitar grants. No modificar una function validada sin motivo explicado.
7. `bootstrap_first_owner`, onboarding admin y onboarding Client deben quedar exclusivamente ejecutables desde e-Hub server + `service_role`; Client role no autoriza al navegador a ejecutar la RPC. El flujo futuro es browser → server autenticado/autorizado → RPC server-only.
8. `integration_secrets`, `oauth_attempts` y `audit_events` no tienen políticas browser-facing. Ciphertext no justifica su exposición. La futura UI de auditoría accederá mediante boundary server-side y `audit:read` seguirá siendo autorización e-Hub.
9. Añadir validación local real, no sólo metadata: anon y authenticated no pueden hacer `SELECT` sobre las tres tablas sensibles ni `EXECUTE` sobre onboarding/bootstrapping; `service_role` conserva las operaciones server-only necesarias. Confirmar RLS activo y sin policies client-side.
10. Crear una migration forward-only `timestamp_database_privilege_hardening.sql`; no reescribir la migration 2.16. Ejecutar `supabase db reset --local` y repetir la matriz.

## Límites y stop conditions

No ejecutar `supabase db push`, no aplicar migrations remotas ni hacer escrituras remotas. No implementar OAuth real, callback, authorize, tokens reales, Store/Connection/assignment reales, UI, Audit UI, Export Center, retiro de Clerk fallback ni Fase 3. No usar `git add`, commit ni push.

Detenerse si `service_role` deja de ejecutar primitives, RLS rompe la arquitectura server-only, una RPC requiere exposición client-side, aparecen grants que requieren rediseño, una `SECURITY DEFINER` no puede endurecerse con seguridad, se requieren policies tenant-aware ahora o Supabase Auth.

Como cambia SQL/security, ejecutar tests relevantes DB/seguridad, `bun run typecheck`, lint sólo si cambia TS, build sólo si cambia código Next/TS relevante y `git diff --check`. Actualizar únicamente `docs/meli-database.md` y este mandato, salvo cambio real de frontera OAuth.

## Checkpoint requerido

Reportar migration, RLS de las tres sensibles y core sí/no con motivo; grants de `PUBLIC`/anon/authenticated/service_role; RPCs, `EXECUTE` y `SECURITY DEFINER`/`search_path`; denegaciones directas para bootstrap, onboards y lecturas sensibles; reset y pruebas locales; validaciones de aplicación; `git diff --check`; docs; y confirmar explícitamente: remote migrations `NO`, remote writes `NO`, OAuth real `NO` y git add/commit/push `NO`.

## Cierre — audit + commit 2.16/2.17

Proyecto: e-ngenieria Hub. Rama: `develop`. Base remota Git: `f465e7a`.

Estado aprobado: 2.16 implementada localmente; 2.17 endurecida y validada localmente. Migrations: `20260824184934_oauth_security_foundations.sql` y `20260824191800_database_privilege_hardening.sql`. Reset local PASS; 89/89 tests PASS; typecheck PASS; último lint PASS y build 2.16 PASS. Migrations remotas no aplicadas, sin escrituras remotas y OAuth real no iniciado.

Antes de cerrar, auditar `git status --short`, `git diff --stat`, `git diff` y `git diff --check`. Todos los cambios deben pertenecer exclusivamente a 2.16/2.17: migrations, código server-only OAuth/security foundation, repositories/services/RBAC relacionados, tests, `env.example.txt` con sólo nombres de variables y documentación mínima. Deben quedar fuera `.env.local`, secretos, tokens, service-role key, master encryption key real, HAR, screenshots, Playwright y temporales. Buscar en staged/unstaged valores reales de `SUPABASE_SERVICE_ROLE_KEY`, `INTEGRATION_SECRETS_MASTER_KEY`, access/refresh tokens, authorization codes o JWTs; los nombres/documentación son válidos. Si aparece un secreto o archivo ajeno, detenerse.

No repetir tests, typecheck, lint, build ni `supabase db reset` salvo que la auditoría modifique código/SQL. Hacer staging explícito sólo de archivos 2.16/2.17; nunca `git add .` ni `git add -A`. Revisar `git diff --cached --stat`, `git diff --cached --check` y el staged diff. Crear `feat(integrations): add secure oauth foundations` y ejecutar únicamente `git push origin develop`; no ejecutar `supabase db push`.

Después verificar `git rev-parse HEAD`, `git rev-parse origin/develop` y `git status --short`. El estado canónico es: OAuth foundations, OAuth attempts, encrypted integration secrets, historical Connection uniqueness, admin/client onboarding atómico y audit foundation implementados localmente; RLS habilitado localmente en todas las tablas Hub; acceso directo anon/authenticated y ejecución directa client de RPCs denegados; migrations remotas pendientes; OAuth real no iniciado; Stores y Connections reales en 0.

No ejecutar remote SQL/migration/reset, OAuth real, authorize/callback/token exchange/`GET /users/me`, crear Store/Connection/assignment/audit real, retirar Clerk fallback ni iniciar Fase 3. El checkpoint final debe informar hash, mensaje, archivos y migrations, secret hygiene, staged diff check, push, HEAD/origin, working tree limpio, 89/89, reset PASS, RLS ready, RPC hardened, remote migrations NOT APPLIED, remote writes NO y OAuth real NO.
