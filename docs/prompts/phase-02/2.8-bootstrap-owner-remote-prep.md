# 2.8 — Bootstrap First Owner + Remote Supabase Preparation

Status: COMPLETED

Purpose:
Resolver de forma concurrency-safe el bootstrap inicial de Owner y preparar el proyecto para futura vinculación con Supabase remoto.

Context:

- Local DB validated.
- Remote DB unlinked.
- Clerk role fallback sigue transitorio.
- OAuth todavía no comenzó.

Result:
Bootstrap First Owner implementado y validado localmente: Clerk server-side exige sesión, Organization activa y `org:admin`; PostgreSQL serializa con advisory transaction lock. Remote no enlazado; Fase 3 no iniciada.

Commit:
PENDING

---

# FASE 2 — CHECKPOINT 2.8: Bootstrap First Owner + preparación remota

Se formaliza que una Organization puede tener **uno o más Owners** persistentes. No se crea `UNIQUE (organization_id) WHERE role = 'Owner'`: la operación especial es sólo Bootstrap First Owner. La estrategia autorizada es una transacción PostgreSQL que obtiene `pg_advisory_xact_lock` derivado de `organization_id`, vuelve a verificar Owner y crea el primero sólo si no existe. Una función/RPC y migration nueva mínimas están autorizadas si son necesarias; no se modifican migrations 2.2/2.5, no se crean tablas adicionales, no se implementa OAuth/Fase 3/RLS/rate limiting ni se elimina el fallback Clerk.

La capa server-only debe derivar autenticación, Organization activa, `clerkUserId` y role `org:admin` desde Clerk; no acepta Organization, usuario ni rol del browser. El RPC fija `Owner`, es idempotente y no actualiza silenciosamente una membership existente no-Owner sin decisión explícita. Deben probarse concurrencia misma Organization, Organizations distintas, no-admin, Owner existente, schema que permite Owners múltiples futuros y errores de DB. Los tests DB no deben volver obligatorio Docker para `bun run test`.

Preparar sólo documentación para Supabase remoto: proyecto dedicado development/test, creación manual por Dashboard, región, password fuera de Git, no tablas manuales, no Supabase Auth de aplicación, no pegar keys. Auditar placeholders y confirmar service role server-only; no login/link/db push/proyecto remoto. Mantener RLS y Security Hardening diferidos. Actualizar documentación, Obsidian y el resultado de este prompt; ejecutar tests, typecheck, lint, build, diff checks; no git add/commit/push.

## References

Canonical: [meli-database.md](../../meli-database.md), [meli-multi-tenancy.md](../../meli-multi-tenancy.md). Evidence: [database-runtime-validation.md](../../database-runtime-validation.md)
