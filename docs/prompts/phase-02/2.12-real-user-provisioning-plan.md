# FASE 2 — CHECKPOINT 2.12
# REAL USER PROVISIONING PLAN + FALLBACK CUTOVER PREPARATION
# PLANIFICAR DATOS REALES — NO EJECUTAR PROVISIONING TODAVÍA
# USAR GPT-5.6 TERRA + MEDIUM
# ARCHIVAR ESTE PROMPT ANTES DE CUALQUIER ACCIÓN

Proyecto:
e-ngenieria Hub

Rama:
develop

Commit base:
64b067b feat(authz): add controlled membership provisioning

Estado aprobado:

LOCAL DATABASE:
VALIDATED

REMOTE SUPABASE:
LINKED + VALIDATED

ROLE AUTHORITY:
hub_memberships PRIMARY

PROVISIONING PRIMITIVES:
IMPLEMENTED

STORE ASSIGNMENT PRIMITIVES:
IMPLEMENTED

REAL MEMBERSHIPS:
NOT CREATED

REAL STORE ASSIGNMENTS:
NOT CREATED

CLERK FALLBACK:
TRANSITIONAL

RLS:
DEFERRED

OAUTH:
NOT STARTED

PHASE 3:
NOT STARTED

Norma documental vigente:

Repo = Source of Truth.
Obsidian = índice + memoria resumida.
Prompts = historial operativo.
No duplicar contenido completo.
Usar referencias cruzadas.

OBJETIVO DE 2.12:

1. archivar este prompt;
2. auditar Organization y usuarios reales disponibles en Clerk;
3. auditar Stores reales existentes o pendientes;
4. construir una MATRIZ DE PROVISIONING PROPUESTA;
5. clasificar Owner / Manager / Employee / Client;
6. definir Store assignments propuestos;
7. detectar usuarios faltantes o inconsistencias;
8. preparar estrategia de cutover del fallback;
9. preparar rollback;
10. NO escribir memberships reales;
11. NO escribir assignments reales;
12. NO retirar fallback;
13. detenerse para APROBACIÓN EXPLÍCITA DEL USUARIO.

======================================================================
0. ARCHIVAR ESTE PROMPT PRIMERO
======================================================================

ANTES de cualquier acción:

crear:

docs/prompts/phase-02/2.12-real-user-provisioning-plan.md

Guardar el texto COMPLETO de ESTE mandato.

Metadata:

# 2.12 — Real User Provisioning Plan + Fallback Cutover Preparation

Status: ACTIVE

Purpose:
Construir y validar el plan exacto de memberships persistentes y Store
assignments reales antes de escribir datos o retirar el fallback Clerk.

Result:
PENDING

Commit:
PENDING

Agregar References según convención documental vigente.

No continuar hasta confirmar que existe.

======================================================================
1. MODO DE ESTE CHECKPOINT
======================================================================

ESTE CHECKPOINT ES READ-ONLY RESPECTO DE DATOS REALES.

Está autorizado:

- leer código;
- leer documentación;
- consultar Clerk;
- consultar Supabase remoto en READ ONLY;
- comparar identidades;
- construir propuestas;
- crear documentación;
- crear fixtures locales si fuera estrictamente necesario;
- ejecutar tests.

NO está autorizado:

- INSERT/UPDATE/DELETE de datos reales remotos;
- crear hub_memberships reales;
- crear store_assignments reales;
- cambiar roles reales;
- eliminar memberships;
- retirar fallback;
- invitar usuarios;
- crear usuarios Clerk;
- crear Stores reales.

======================================================================
2. AUDITAR ESTADO ACTUAL
======================================================================

Leer:

AGENTS.md
REGLAS.md
docs/agent-workflow.md
docs/codex-handoff.md
docs/plan-y-gobierno.md
docs/meli-database.md
docs/meli-multi-tenancy.md
docs/prompts/phase-02/README.md

Revisar:

membership-provisioning
store-assignment-provisioning
persistent-role
store-scope
authorization
server-context
repositories
bootstrap-first-owner

Confirmar contratos reales antes de diseñar plan.

======================================================================
3. IDENTIFICAR ORGANIZATION REAL
======================================================================

Consultar Clerk de forma read-only mediante mecanismos ya disponibles
en el proyecto/entorno.

Identificar:

- active/relevant Organization;
- Organization ID interno;
- nombre visible;
- memberships Clerk existentes;
- roles Clerk actuales.

NO copiar tokens ni session data.

El Organization ID puede registrarse sólo si ya es parte normal
del modelo operativo y no constituye secreto.

No cambiar Organization.

======================================================================
4. INVENTARIO DE USUARIOS CLERK
======================================================================

Construir inventario read-only de miembros actuales de la Organization.

Para cada usuario, recoger únicamente lo necesario:

- clerk_user_id;
- nombre/display name si está disponible;
- email principal si es necesario para que el usuario humano pueda identificarlo;
- Clerk Organization role actual;
- estado relevante.

NO guardar información personal innecesaria.

NO duplicar perfil Clerk entero en documentación.

======================================================================
5. PRIVACIDAD EN DOCUMENTACIÓN
======================================================================

No poner una lista completa de emails/personales reales
en documentos Git si no es necesario.

Preferir una matriz de aprobación con:

- alias/nombre reconocible;
- clerk_user_id parcial o referencia segura;
- role propuesto;
- Store scope propuesto.

Si los identificadores reales completos son necesarios para ejecución,
mantenerlos fuera de Git hasta el checkpoint de provisioning real.

NO guardar secretos.

======================================================================
6. INVENTARIO DE hub_memberships REMOTOS
======================================================================

Consultar Supabase remoto en READ ONLY.

Determinar:

- memberships reales actuales;
- si sigue vacío;
- roles persistentes existentes;
- Organizations involucradas.

NO insertar.
NO actualizar.
NO eliminar.

Distinguir:

TEST FIXTURES
vs
REAL DATA.

No asumir que continúa vacío sin comprobar.

======================================================================
7. INVENTARIO DE STORES
======================================================================

Consultar Stores remotas en READ ONLY.

Determinar:

- Stores existentes;
- Organization;
- status;
- IDs;
- si son reales o fixtures.

NO crear Stores todavía.

Si actualmente no hay Stores reales:
registrarlo.

Esto es importante porque no se pueden asignar
Employee/Client a Stores inexistentes.

======================================================================
8. ROLES DE NEGOCIO
======================================================================

Roles canónicos:

Owner
Manager
Employee
Client

No crear roles adicionales.

No reutilizar Clerk org:admin/org:member como roles e-Hub finales.

Clerk role:
control de Organization/bootstrap.

hub_membership role:
autoridad e-Hub.

======================================================================
9. OWNER(S)
======================================================================

Identificar candidato(s) a Owner.

Reglas:

- debe pertenecer a la Organization Clerk;
- Organization debe conservar al menos un Owner;
- múltiples Owners son permitidos;
- no usar bootstrap para agregar Owners posteriores.

NO crear el Owner todavía.

Si no puede determinarse inequívocamente:
marcar NEEDS USER DECISION.

======================================================================
10. MANAGERS
======================================================================

Construir candidatos a Manager.

Recordar:

Manager:
- role persistente;
- all-stores dentro del tenant;
- permissions según RBAC;
- no requiere store_assignments explícitos.

No promover org:member automáticamente.

Cada Manager debe ser decisión explícita.

======================================================================
11. EMPLOYEES
======================================================================

Construir candidatos a Employee.

Para cada Employee debe existir propuesta explícita de:

Employee
→ Store A
→ Store B
...

Si no hay assignment:

Employee obtiene zero Store Scope.

No inferir Stores automáticamente a partir del fallback Clerk.

======================================================================
12. CLIENTS
======================================================================

Construir candidatos a Client.

Para cada Client:

Client
→ Store(s) propia(s)

Un Client puede potencialmente tener más de una Store.

Debe quedar explícita cada asociación.

No asumir:

1 cliente = exactamente 1 Store.

======================================================================
13. MATRIZ DE PROVISIONING
======================================================================

Crear un documento específico si no existe uno apropiado:

docs/provisioning-plan.md

o nombre equivalente coherente.

Debe ser un PLAN, no datos ejecutables.

Estructura recomendada:

# Real Provisioning Plan

Status:
DRAFT — USER APPROVAL REQUIRED

## Organization

## Membership Matrix

| Person/Alias | Clerk membership | Current source | Proposed e-Hub role | Action | Approval |
| ... |

Action puede ser:

CREATE
ALREADY PERSISTENT
NEEDS DECISION
SKIP

No escribir secretos.

======================================================================
14. STORE ASSIGNMENT MATRIX
======================================================================

Agregar:

## Store Assignment Matrix

| Person/Alias | Role | Store | Assignment action | Approval |
| ... |

Para Owner/Manager:

Assignment action:
NOT REQUIRED — ALL STORES

Para Employee/Client:

CREATE / KEEP / REMOVE / NEEDS DECISION

Pero NO ejecutar ninguna acción.

======================================================================
15. STORES AUSENTES
======================================================================

Si todavía no existen Stores reales suficientes:

NO inventarlas.

Crear:

## Missing Stores

con información necesaria para crear esas Stores
en un futuro checkpoint.

No crear Stores ahora.

Esto puede implicar que provisioning real se divida en:

2.13 Store creation
2.14 Membership provisioning

si corresponde.

No decidir numeración prematuramente;
proponerla en checkpoint.

======================================================================
16. USUARIOS AUSENTES EN CLERK
======================================================================

Si una persona necesaria no pertenece todavía a Organization:

marcar:

CLERK MEMBERSHIP REQUIRED

No crear ni invitar automáticamente.

No provisionar hub_membership huérfana.

======================================================================
17. CONFLICTOS
======================================================================

Detectar explícitamente:

- mismo usuario propuesto con dos roles incompatibles;
- Employee sin Store;
- Client sin Store;
- Store asociada a Organization distinta;
- usuario no miembro de Clerk Organization;
- membership persistente existente con role distinto al propuesto;
- Store inexistente;
- Owner ambiguity.

Clasificar:

BLOCKER
NEEDS DECISION
SAFE

======================================================================
18. PLAN DE ESCRITURAS FUTURAS
======================================================================

Generar un plan exacto de las futuras operaciones,
SIN EJECUTARLAS.

Ejemplo conceptual:

1. provision Owner X
2. provision Manager Y
3. provision Employee Z
4. assign Employee Z → Store A
5. provision Client Q
6. assign Client Q → Store B

El orden debe respetar invariantes.

No generar SQL manual para ejecutar.

Usar las primitives server-only ya implementadas en el futuro checkpoint.

======================================================================
19. IDEMPOTENCIA DEL PLAN
======================================================================

Clasificar cada operación futura como:

CREATE
SKIP EXISTING
NEEDS UPDATE
BLOCKED

Evitar que la ejecución posterior cree duplicados.

No asumir que un retry debe producir datos adicionales.

======================================================================
20. ÚLTIMO OWNER
======================================================================

El plan de cutover debe preservar:

AT LEAST ONE PERSISTENT OWNER.

No eliminar/degradar Owner durante provisioning.

No implementar cambios de rol Owner en este checkpoint.

======================================================================
21. FALLBACK USAGE INVENTORY
======================================================================

Analizar dónde sigue usándose:

roleSource = clerk-fallback

y cómo podremos saber si desapareció tras provisioning.

No exponer roleSource públicamente.

No crear analytics complejo.

Preparar criterio:

fallback usage should become zero for provisioned users.

======================================================================
22. CUTOVER CRITERIA
======================================================================

Actualizar checklist de retiro del fallback.

Debe incluir:

Remote DB validated:
✓

Bootstrap validated:
✓

Persistent role resolver:
✓

Provisioning primitives:
✓

Store assignment primitives:
✓

Real membership matrix approved:
✗ hasta aprobación usuario

Real Stores ready:
✗/✓ según inventario

Real memberships created:
✗

Real assignments created:
✗

Fallback usage for provisioned users = 0:
✗

Cutover tests:
✗

Rollback plan:
debe prepararse en 2.12

Final approval:
✗

NO retirar fallback todavía.

======================================================================
23. ROLLBACK PLAN
======================================================================

Preparar plan conceptual para caso de fallo después del provisioning.

Debe contemplar:

- fallback sigue disponible durante transición;
- no borrar memberships inmediatamente;
- no borrar assignments por rollback automático;
- restaurar acceso de forma controlada;
- mantener al menos un Owner.

No implementar scripts destructivos.

No generar SQL peligroso.

======================================================================
24. CUTOVER STRATEGY PROPUESTA
======================================================================

Preparar estrategia por etapas:

Stage A:
provision persistent memberships.

Stage B:
provision Store assignments.

Stage C:
verify each user resolves:
roleSource = persistent.

Stage D:
functional access tests.

Stage E:
observe zero unexpected fallback usage.

Stage F:
explicit approval.

Stage G:
remove Clerk fallback in separate checkpoint.

Esto debe estar documentado.

NO ejecutar stages A–G ahora.

======================================================================
25. CLIENT ACCESS SAFETY
======================================================================

Antes de provisioning futuro,
confirmar que Client persistente sigue sin obtener:

- global products permissions;
- global Store access;
- admin operations.

Client sólo accede a sus Store assignments
y capabilities autorizadas posteriormente.

No modificar RBAC ahora.

======================================================================
26. EMPLOYEE ACCESS SAFETY
======================================================================

Employee:

- role persistente;
- Store Scope únicamente explícito;
- zero assignments = zero stores.

Confirmar que el plan nunca depende del fallback
para darle acceso a Stores.

======================================================================
27. MANAGER ACCESS SAFETY
======================================================================

Manager:

all stores del tenant,
pero permisos siguen definidos por RBAC.

No necesita assignments.

No ampliar permisos.

======================================================================
28. OWNER ACCESS SAFETY
======================================================================

Owner:

all stores del tenant.

Puede provisionar mediante primitives 2.11.

No usar navegador como autoridad de actor.

No implementar UI admin aún.

======================================================================
29. NO CREAR UI DE ADMINISTRACIÓN
======================================================================

No crear:

- forms;
- tables;
- dashboard admin;
- user management UI.

Primero necesitamos aprobar datos reales y flujo.

UI puede venir después.

======================================================================
30. NO CAMBIAR SCHEMA
======================================================================

Esperado:

NO migration.

Si durante auditoría se concluye que falta schema
para completar provisioning:

DETENERSE.

No crear migration automáticamente.

======================================================================
31. DOCUMENTACIÓN CANÓNICA
======================================================================

Actualizar:

docs/meli-multi-tenancy.md
docs/plan-y-gobierno.md
docs/codex-handoff.md
docs/prompts/phase-02/README.md

Actualizar meli-database.md sólo si cambia estado relacionado con datos,
no para duplicar el plan.

Crear/actualizar:

docs/provisioning-plan.md

como documento canónico del plan de usuarios reales,
si corresponde.

Usar referencias cruzadas.

No duplicar tablas completas en Obsidian.

======================================================================
32. OBSIDIAN
======================================================================

Actualizar resumido:

current-state
decisions
open-questions

Registrar:

Real provisioning:
PLANNING

Matrix:
USER APPROVAL REQUIRED

Fallback:
TRANSITIONAL

Cutover:
NOT AUTHORIZED

Rollback:
PLANNED / PENDING según resultado

Phase 3:
NOT STARTED

Enlazar conceptualmente:

repo → docs/provisioning-plan.md

No copiar datos personales completos.

======================================================================
33. VALIDACIONES DE CÓDIGO
======================================================================

Como este checkpoint idealmente es read-only + docs,
no modificar código salvo necesidad crítica.

Si sólo hay docs:

git diff --check

es suficiente.

Si se modifica código inesperadamente:

detenerse y explicar antes de continuar.

No ejecutar tests por costumbre si no cambia código.

======================================================================
34. NO HACER
======================================================================

NO:

git add
git commit
git push

crear/editar memberships reales
crear/editar assignments reales
crear Stores reales

invitar usuarios Clerk
crear usuarios Clerk

retirar fallback

crear migration
db push
remote reset

OAuth
Mercado Libre API
tokens
callbacks
webhooks
sync
StoreIntegrationResolver

activar RLS
rate limiting

iniciar Fase 3.

======================================================================
35. STOP CONDITIONS
======================================================================

DETENERSE si:

- no puede distinguir usuarios de forma segura;
- hay usuarios que no pertenecen a Organization;
- faltan Stores reales requeridas;
- existe conflicto de roles;
- aparece una membership persistente inesperada;
- hay riesgo de exponer datos personales;
- hace falta modificar schema;
- cualquier operación requiere escribir remoto.

No improvisar.

======================================================================
36. CHECKPOINT PARA APROBACIÓN HUMANA
======================================================================

Entregar:

1. prompt archivado;
2. Organization auditada;
3. cantidad de Clerk members;
4. cantidad de hub_memberships reales;
5. cantidad de Stores reales;
6. candidatos Owner;
7. candidatos Manager;
8. candidatos Employee;
9. candidatos Client;
10. usuarios Clerk faltantes;
11. Stores faltantes;
12. memberships existentes conflictivas;
13. Membership Matrix creada;
14. Store Assignment Matrix creada;
15. conflictos BLOCKER;
16. decisiones pendientes;
17. plan futuro de writes;
18. idempotencia del plan;
19. last Owner safety;
20. fallback usage strategy;
21. cutover criteria;
22. rollback plan;
23. Client safety;
24. Employee safety;
25. Manager safety;
26. Owner safety;
27. nueva migration = NO/NEEDS DECISION;
28. código modificado = NO esperado;
29. docs creadas/modificadas;
30. Obsidian actualizado;
31. git diff --check;
32. confirmar real DB writes = NO;
33. confirmar real memberships = NO;
34. confirmar real assignments = NO;
35. confirmar fallback removal = NO;
36. confirmar OAuth = NO;
37. confirmar Fase 3 = NO;
38. confirmar git add/commit/push = NO.

MUY IMPORTANTE:

Al final mostrar claramente al usuario una MATRIZ RESUMIDA
que pueda aprobar/corregir antes de cualquier provisioning real.

Después detenerse.

### Resultado de la continuación

- Target verified: `ffcudwwrzttkumbdvada` (`e-hub-db`).
- Current Clerk Admin confirmado por decisión humana como Current Real Owner.
- Owner cardinality: ONE OR MORE; safety invariant: AT LEAST ONE OWNER.
- Inventario remoto read-only: `hub_memberships = 0`, `stores = 0`, `store_assignments = 0`, `connections = 0`.
- Future Owner action: CREATE persistent membership; execution remains NOT YET AUTHORIZED.
- Stores reales: EMPTY — STORE CREATION REQUIRED antes de Employees/Clients.
- Fallback: TRANSITIONAL. Cutover, OAuth y Phase 3: NOT AUTHORIZED / NOT STARTED.

## Continuación — Owner confirmation + remote inventory

# CONTINUAR 2.12 — CONFIRMAR OWNER + COMPLETAR INVENTARIO REMOTO READ-ONLY
# NO PROVISIONAR TODAVÍA
# NO CREAR 2.12B
# USAR EL PROMPT 2.12 EXISTENTE

Proyecto:
e-ngenieria Hub

Rama:
develop

Estado actual:

2.12 — IN PROGRESS

Ya realizado:

- Organization Clerk auditada.
- Clerk members = 1.
- único role Clerk observado = org:admin.
- candidatos Manager/Employee/Client = ninguno definido.
- provisioning-plan.md creado.
- fallback = TRANSITIONAL.
- cutover = NOT AUTHORIZED.
- código no modificado.
- no remote writes.
- no git add/commit/push.

DECISIÓN HUMANA CONFIRMADA:

El único Clerk org:admin actual corresponde al Owner real actual
de e-ngenieria Hub.

Aprobar:

Current Clerk Admin
→ e-Hub role: Owner
→ APPROVED FOR FUTURE PROVISIONING

Decisión de cardinalidad:

- actualmente existe 1 Owner real;
- la Organization puede tener ONE OR MORE Owners;
- en el futuro se puede incorporar otro Owner;
- Owners adicionales requieren acción administrativa explícita;
- NO imponer máximo permanente de un Owner;
- debe mantenerse siempre la invariancia:
  AT LEAST ONE OWNER.

IMPORTANTE:

ESTE PASO SIGUE SIENDO READ-ONLY RESPECTO DE DATOS REMOTOS.

No crear todavía:

- hub_membership real;
- Store;
- store_assignment;
- Manager;
- Employee;
- Client.

============================================================
0. ARCHIVAR CONTINUACIÓN
============================================================

Agregar el texto COMPLETO de este mandato a:

docs/prompts/phase-02/2.12-real-user-provisioning-plan.md

bajo:

## Continuación — Owner confirmation + remote inventory

No crear archivo nuevo.

============================================================
1. ACTUALIZAR OWNER DECISION
============================================================

Actualizar:

docs/provisioning-plan.md

para reflejar:

Alias:
Current Clerk Admin

Clerk role:
org:admin

Current business status:
CURRENT REAL OWNER

Proposed e-Hub persistent role:
Owner

Decision:
APPROVED FOR FUTURE PROVISIONING

Execution:
NOT YET AUTHORIZED

Además registrar:

Owner cardinality:
ONE OR MORE

Current real Owners:
1

Additional Owners:
allowed in future by explicit administrative action

Safety invariant:
AT LEAST ONE OWNER

No guardar email completo ni datos personales innecesarios.

============================================================
2. NO USAR SERVICE ROLE SI NO HACE FALTA
============================================================

La lectura remota debe reutilizar el mecanismo
que ya funcionó durante 2.9.

Preferir:

bunx supabase db query --linked

o la sintaxis real soportada por Supabase CLI 2.114.0.

Puede consultarse --help antes.

No leer ni imprimir .env.local.

No imprimir:

service role
DB URL
password
tokens.

============================================================
3. VERIFICAR TARGET
============================================================

Confirmar read-only:

linked project ref =
ffcudwwrzttkumbdvada

Si no coincide:
DETENERSE.

============================================================
4. INVENTARIO REMOTO DE hub_memberships
============================================================

Ejecutar consulta READ ONLY.

Obtener únicamente:

- count total;
- organization_id;
- clerk_user_id;
- role;

solo si existen filas.

No consultar timestamps si no hacen falta.

No hacer INSERT/UPDATE/DELETE.

Clasificar cada fila como:

REAL
EXPECTED TEST FIXTURE
UNEXPECTED

No asumir que las tablas siguen vacías.

============================================================
5. INVENTARIO REMOTO DE STORES
============================================================

Consulta READ ONLY.

Obtener:

- count total;
- id;
- organization_id;
- name;
- status;

si existen Stores.

No modificar nada.

Clasificar:

REAL
TEST
UNEXPECTED

============================================================
6. INVENTARIO DE store_assignments
============================================================

Consulta READ ONLY.

Determinar:

- count total;
- si existen assignments;
- membership/store/organization relacionados.

No hace falta documentar UUID completos en Git.

============================================================
7. INVENTARIO DE connections
============================================================

Consulta READ ONLY.

Sólo confirmar:

- count;
- si existen Connections reales.

No incluir external_account_id completo si existe dato real.

No guardar scopes ni metadata sensible innecesaria.

============================================================
8. SI LAS CUATRO TABLAS ESTÁN VACÍAS
============================================================

Registrar explícitamente:

hub_memberships = 0
stores = 0
store_assignments = 0
connections = 0

Esto significaría:

- todavía no existe Owner persistente;
- no existen Stores reales;
- no hay assignments reales;
- no existen Connections reales.

NO cambiar ese estado.

============================================================
9. SI EXISTEN DATOS
============================================================

NO tocarlos.

Actualizar provisioning-plan.md únicamente con clasificación mínima.

Si aparece cualquier dato inesperado:

marcar:

BLOCKER — USER REVIEW REQUIRED

y detener provisioning planning.

============================================================
10. PLAN DEL OWNER
============================================================

Si DB confirma que NO existe membership persistente
para Current Clerk Admin:

marcar futura operación:

CREATE persistent membership
role = Owner

Actor:
Current Clerk Admin / future persistent Owner path

Approval:
APPROVED

Execution:
NOT YET AUTHORIZED

Si ya existe Owner persistente para ese mismo Clerk user:

marcar:

SKIP EXISTING

No crear duplicado.

No usar bootstrap automáticamente en este planning checkpoint.

============================================================
11. OWNER FUTURO
============================================================

Documentar explícitamente:

El Owner actual NO es el único Owner permitido por diseño.

Futuro flujo administrativo:

Persistent Owner autorizado
→ selecciona target Clerk member válido
→ provisionMembership(role = Owner)
→ Organization mantiene ONE OR MORE Owners

No implementar ese flujo adicional ahora.

No cambiar schema.

No crear constraint de máximo un Owner.

============================================================
12. PROTECCIÓN DEL ÚLTIMO OWNER
============================================================

Mantener como regla de seguridad:

Una Organization no debe quedar con cero Owners persistentes.

En futuros cambios de rol/eliminación:

debe verificarse que permanezca al menos un Owner.

No implementar demotion/remove Owner en este checkpoint.

No crear migration para esto.

Mantener como invariancia de servicio:

AT LEAST ONE OWNER.

============================================================
13. STORE CREATION DEPENDENCY
============================================================

Si stores = 0:

marcar en provisioning-plan.md:

Real Store inventory:
EMPTY

Blocker:

Employees/Clients no pueden recibir assignments
hasta crear Stores reales.

Proponer siguiente orden:

A. provision persistent Owner actual
B. create real Stores
C. define Managers/Employees/Clients
D. create memberships
E. create Employee/Client assignments
F. verify persistent roleSource
G. cutover preparation

NO ejecutar ninguno ahora.

============================================================
14. MANAGER / EMPLOYEE / CLIENT
============================================================

Mantener:

Managers:
UNDEFINED

Employees:
UNDEFINED

Clients:
UNDEFINED

No inferir usuarios.

No usar org:member como Employee automáticamente.

============================================================
15. ACTUALIZAR MATRIZ
============================================================

Actualizar Membership Matrix:

| Alias | Clerk role | Persistent state | Proposed role | Future action | Approval |
| Current Clerk Admin | org:admin | <estado remoto> | Owner | CREATE/SKIP | APPROVED |

Agregar nota:

Current business role:
Owner

Owner cardinality:
ONE OR MORE

Current real Owner count:
1

Store Matrix:

si no hay Stores:

EMPTY — STORE CREATION REQUIRED

No inventar nombres de Stores.

============================================================
16. CUTOVER
============================================================

Mantener:

Fallback:
TRANSITIONAL

No puede retirarse sólo por provisionar Owner.

Todavía faltarán:

- Stores reales;
- usuarios reales;
- assignments;
- roleSource verification;
- functional access tests;
- fallback observation;
- cutover approval;
- rollback readiness.

============================================================
17. DOCUMENTACIÓN
============================================================

Actualizar mínimamente:

docs/provisioning-plan.md
docs/codex-handoff.md
docs/plan-y-gobierno.md
docs/prompts/phase-02/2.12-real-user-provisioning-plan.md

Actualizar otros documentos sólo si cambia una decisión canónica.

Usar referencias cruzadas.

No duplicar contenido completo.

============================================================
18. OBSIDIAN
============================================================

Si MCP está disponible en esta sesión:

actualizar resumidamente:

Current Owner:
1

Current Clerk Admin:
APPROVED AS FUTURE PERSISTENT OWNER

Owner cardinality:
ONE OR MORE

Owner safety:
AT LEAST ONE OWNER

Remote inventory:
<estado real>

Stores:
<estado real>

Real writes:
NOT AUTHORIZED

Cutover:
NOT AUTHORIZED

Si MCP no está disponible:

documentarlo.
No inventar actualización.

============================================================
19. VALIDACIÓN DOCUMENTAL
============================================================

Ejecutar:

git diff --check
git status --short
git diff

Esperado:

sólo docs/.

Si aparece código:
DETENERSE.

============================================================
20. NO HACER
============================================================

NO:

git add
git commit
git push

INSERT
UPDATE
DELETE
TRUNCATE
remote reset
db push

crear Owner persistente real
crear segundo Owner
crear Store real
crear Manager
crear Employee
crear Client
crear assignment

retirar fallback

OAuth
Phase 3.

============================================================
21. CHECKPOINT
============================================================

Entregar:

1. Owner decision actualizada;
2. confirmar Current Clerk Admin = current real Owner;
3. Owner cardinality;
4. current real Owner count;
5. additional Owners future policy;
6. last Owner safety;
7. project ref verificado;
8. hub_memberships count;
9. Stores count;
10. store_assignments count;
11. connections count;
12. datos inesperados sí/no;
13. persistent Owner existente sí/no;
14. future Owner action CREATE/SKIP;
15. Store creation required sí/no;
16. Managers state;
17. Employees state;
18. Clients state;
19. provisioning order propuesto;
20. cutover state;
21. provisioning-plan actualizado;
22. handoff actualizado;
23. Obsidian status;
24. git diff --check;
25. confirmar remote writes = NO;
26. confirmar real provisioning = NO;
27. confirmar second Owner creation = NO;
28. confirmar fallback removal = NO;
29. confirmar OAuth = NO;
30. confirmar Phase 3 = NO;
31. confirmar git add/commit/push = NO.

Después detenerse.
