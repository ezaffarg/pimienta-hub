# Cierre Subfase 2.4

Status: **COMPLETED**

Purpose:
Cerrar y versionar Store Scope persistente sin iniciar Connections.

Context:
- Owner/Manager tienen all-stores dentro de su Organization;
- Employee/Client usan explicit assignments;
- Permission y Store Scope permanecen separados;
- Security Hardening Backlog quedó registrado;
- DB real sigue sin ejecutar.

Result:
Store Scope, repositorios tenant-safe, tests, documentación y backlog de hardening fueron auditados; validación correcta y cierre Git pendiente.

Commit:
PENDING

## Prompt

# FASE 2 — CIERRE DE SUBFASE 2.4
# STORE SCOPE PERSISTENTE
# ARCHIVAR ESTE PROMPT ANTES DE EJECUTAR CUALQUIER ACCIÓN

Proyecto: e-ngenieria Hub. Rama: `develop`. Commit base: `eabeac6 feat(database): add tenant repositories and persistent roles`.

Estado: 2.4 completada localmente, sin staging/commit/push; Store Scope implementado, 53 tests, typecheck, lint, build y `git diff --check` correctos; Obsidian MCP disponible; Security Hardening Backlog registrado; DB real sin ejecutar; 2.5 no iniciada.

Objetivo: archivar, auditar Store Scope y backlog, actualizar documentación/memoria si corresponde, staging explícito, commit `feat(authz): enforce persistent store scope`, push a `develop` y detenerse. No iniciar 2.5.

Se preautorizan lectura/escritura en scope, validaciones, Git read-only, `git add` de paths explícitos, el commit y push `origin develop`, y actualización resumida de Obsidian. Detenerse ante archivos ajenos, secretos, acciones destructivas, force push, main, Supabase remoto, Docker, db push/link, cambios arquitectónicos o ampliación de scope.

Auditar `git status --short`, `git diff --stat` y `git diff`, clasificando Store Scope, repositories, tests, documentación, prompts o no relacionado. Paths esperados: resolver/test, repositories/test, multi-tenancy, database, plan, handoff, README prompts, prompt 2.4 y este cierre. Detenerse ante archivos injustificables.

Confirmar la unión explícita `all-stores` frente a `assigned-stores` con conjunto vacío seguro; Owner/Manager all Stores solo del tenant y sin assignments; Employee/Client solo assignments, sin assignment deny; missing/unknown/cross-tenant deny; Store ID del browser nunca amplía scope. La jerarquía es Authentication → Organization Scope → Hub Role → Permission → Store Scope → Resource. Permission y Store Scope son independientes; policy permanece Owner todos, Manager products read/write/users read, Employee products read y Client sin permisos globales.

Confirmar que `StoreRepository.listByOrganizationAndIds()` exige y filtra `organizationId`, y que `StoreRepository.create()` recibe Organization separado del payload para derivarse de contexto server-side. El fallback Clerk `org:admin → Owner`, `org:member → Employee` es transitorio y no concede assignments/all-stores. Bootstrap Owner sigue deferred, sin migración nueva.

Verificar el Security Hardening Backlog en el plan: SQL/RPC parametrizado, Zod/input/longitud/payload/arrays/paginación, mass assignment, rate limiting, RLS adicional, prompt injection/AI, XSS, CSRF/Origin, uploads y logging/privacy. No implementarlo ni marcarlo completo. Confirmar DB/migración sin ejecutar, Docker sin instalar, sin start/reset/push/link.

Actualizar Obsidian resumidamente con 2.4, política canónica, separación permissions/scope, backlog, bootstrap pendiente y 2.5 siguiente; sin código, SQL, prompt, secretos ni diffs. Ejecutar tests, typecheck, lint, build y `git diff --check`; 53 tests esperados. No format masivo por incidencias preexistentes.

Actualizar los prompts a COMPLETED con resultado fiel y Commit PENDING; revisar staged diff completo/whitespace y confirmar ausencia de secretos, Connections, OAuth, RLS, rate limiting, migración nueva y StoreIntegrationResolver. Tras commit/push, verificar log/status, HEAD/origin develop alineados y árbol limpio. Registrar hash real en Obsidian. No implementar 2.5 ni elementos futuros.

## Result

El resolver server-only distingue `all-stores` de `assigned-stores`; Owner/Manager reciben el primero dentro de Organization y Employee/Client requieren assignments explícitos. Missing/unknown/cross-tenant se deniegan, y permisos continúan independientes del scope. Se registró el backlog de hardening sin implementarlo. Obsidian MCP está disponible. Validación: 53 tests, typecheck, lint, build y `git diff --check` correctos; warnings conocidos de Vitest/Vite ESM-CJS, Google Sans Flex, metadataBase y LF→CRLF. No hay DB ejecutada ni funcionalidades de 2.5.

## Commit

PENDING
