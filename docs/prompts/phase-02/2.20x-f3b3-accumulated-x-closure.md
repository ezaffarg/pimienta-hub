# 2.20X-F3-B3 — Cierre del bloque X acumulado

## Objetivo

Auditar, validar y publicar en `develop` el trabajo local acumulado de 2.20X y
F3-B, preservando un diff mínimo y sin iniciar el deployment remoto F3-C.

## Baseline

- Proyecto y repositorio: Pimienta Hub / `pimienta-hub`.
- Checkout local: `Z:\Proyectos\VSCode\pimienta-hub`.
- R1 product branding: cerrado.
- R2 structural/GitHub/local-folder rename: cerrado.
- Base del cierre: `00e5c26408dab155fc1b125f07b44e0f50865784` en
  `develop`, inicialmente igual a `origin/develop`.
- `.vscode/tasks.json` es local, untracked y fuera de scope; debe conservarse
  sin staging ni commit.

## Alcance autorizado

- Intake durable y callback publico de eventos Mercado Libre `items`.
- Procesamiento incremental, freshness CAS, retries y missed feeds.
- Orquestacion, observabilidad y stale-run reclaim.
- Health endpoint, boundary interno M2M del scheduler y proxy tecnico.
- Cinco migrations X y sus cinco matrices SQL locales.
- Tests y documentacion canonica/historica X.
- Runtime Docker/Coolify local de F3-B.

## Gates de cierre

- Clasificar todos los cambios y detenerse ante archivos desconocidos.
- Auditar autoridad tenant, `service_role`, callback publico, secreto M2M y
  ausencia de credenciales reales.
- Ejecutar `git diff --check` y los tests focalizados X requeridos.
- Reutilizar los baselines recientes de suite completa, typecheck, lint y
  Docker cuando no haya cambios funcionales nuevos durante el cierre.
- Stagear explicitamente; no usar `git add .` ni `git add -A`.
- Crear como maximo dos commits coherentes, o uno si separar el bloque es
  fragil, y publicar en `origin/develop`.
- Confirmar al final que no quedan cambios tracked y que
  `.vscode/tasks.json` permanece como archivo local untracked.

## Exclusiones

No ejecutar operaciones contra Supabase remoto, Coolify, Hostinger, Clerk o
Mercado Libre. No desplegar, aplicar migrations remotas, configurar DNS/TLS,
registrar el scheduler real ni iniciar F3-C en este bloque.

## Estado

Este documento abre el cierre pre-deployment del working tree X acumulado.
F3-C remoto permanece pendiente hasta completar y publicar este checkpoint.
