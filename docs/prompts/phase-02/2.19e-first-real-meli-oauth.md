# FASE 2 — 2.19E PRE-FLIGHT REAL MERCADO LIBRE OAUTH
# GPT-5.6 TERRA + MEDIUM
# VERIFICAR CONFIGURACIÓN REAL
# NO EJECUTAR OAUTH TODAVÍA

Proyecto:
e-ngenieria Hub

Rama:
develop

Base:
d7fb41c feat(meli): implement oauth runtime

Estado:

OAuth runtime:
READY

OAuth real:
NOT STARTED

Stores:
0

Connections:
0

Current persistent Owner:
1

OBJETIVO:

Validar que la configuración local necesaria para el primer OAuth
real está presente y es coherente, SIN imprimir secretos y SIN
iniciar todavía la autorización.

============================================================
0. ARCHIVAR
============================================================

Crear:

docs/prompts/phase-02/2.19e-first-real-meli-oauth.md

Guardar este mandato completo.

============================================================
1. VARIABLES
============================================================

Verificar únicamente PRESENCIA/FORMATO, nunca valores:

MERCADO_LIBRE_CLIENT_ID
MERCADO_LIBRE_CLIENT_SECRET
MERCADO_LIBRE_REDIRECT_URI
INTEGRATION_SECRETS_MASTER_KEY
MERCADO_LIBRE_PKCE_ENABLED

Reportar sólo:

SET / MISSING / INVALID FORMAT

Nunca mostrar contenido.

============================================================
2. MASTER KEY
============================================================

Validar que:

INTEGRATION_SECRETS_MASTER_KEY

pueda decodificarse al tamaño esperado por AES-256.

No imprimirla.

Si no cumple:
DETENERSE.

============================================================
3. CLIENT ID / SECRET
============================================================

Confirmar:

CLIENT_ID presente.
CLIENT_SECRET presente.

No probar token exchange.

No llamar Mercado Libre.

============================================================
4. REDIRECT URI
============================================================

Leer:

MERCADO_LIBRE_REDIRECT_URI

y reportar únicamente su URL no secreta.

Confirmar que corresponde exactamente a la callback implementada:

/api/integrations/mercado-libre/callback

No modificarla automáticamente.

============================================================
5. DEV ENVIRONMENT
============================================================

Determinar cómo será accesible esa callback durante el primer OAuth.

Si redirect URI apunta a localhost y Mercado Libre no lo acepta:
DETENERSE.

Si requiere HTTPS público:
indicarlo.

No instalar túneles automáticamente.

No cambiar hosting.

============================================================
6. PKCE
============================================================

Reportar:

MERCADO_LIBRE_PKCE_ENABLED =
true / false

No cambiarlo automáticamente.

Si false:
continuar sólo si es compatible con la configuración actual
de la Developers Application.

Si no puede confirmarse:
marcar NEEDS USER CHECK.

============================================================
7. START ROUTE
============================================================

Confirmar que existe:

POST /api/integrations/mercado-libre/connect

y que build actual lo incluye.

No ejecutarla.

============================================================
8. CALLBACK ROUTE
============================================================

Confirmar:

GET /api/integrations/mercado-libre/callback

No ejecutarla.

============================================================
9. REMOTE DATABASE PRECHECK
============================================================

READ ONLY.

Confirmar:

persistent Owners = 1
stores = 0
connections = 0
oauth_attempts = 0
oauth_pending_authorizations = 0
integration_secrets = 0

Si difiere:
DETENERSE.

============================================================
10. NO OAUTH
============================================================

NO:

POST connect
abrir authorize URL
callback
token exchange
GET /users/me
remote OAuth write

Este checkpoint es pre-flight solamente.

============================================================
11. VALIDACIÓN
============================================================

No repetir tests/build.

Ejecutar únicamente lo necesario para verificar config.

git diff --check si se modifica documentación.

============================================================
12. CHECKPOINT
============================================================

Entregar únicamente:

1. Client ID = SET/MISSING;
2. Client Secret = SET/MISSING;
3. Redirect URI;
4. redirect corresponde a callback sí/no;
5. master key = VALID/INVALID/MISSING;
6. PKCE = true/false;
7. PKCE requiere revisión DevCenter sí/no;
8. connect route ready sí/no;
9. callback route ready sí/no;
10. persistent Owner count;
11. Stores;
12. Connections;
13. oauth_attempts;
14. oauth_pending_authorizations;
15. integration_secrets;
16. si estamos READY FOR FIRST REAL OAUTH;
17. si no, indicar exactamente UNA acción humana pendiente;
18. OAuth ejecutado = NO;
19. git add/commit/push = NO.

Después detenerse.

## Resultado — 2.19H remote final onboarding RPC validation

- Target remoto confirmado: `ffcudwwrzttkumbdvada`; `20260824210000_finalize_pending_oauth_onboarding.sql` fue la única migration del dry-run y quedó aplicada, con historial local/remoto alineado.
- La RPC final existe, tiene `SECURITY DEFINER`, `search_path=pg_catalog`, ejecución denegada a PUBLIC/anon/authenticated y permitida sólo a `service_role`.
- El caso transaccional de Store nueva alcanzó sus efectos esperados antes del rollback. La prueba se detuvo al validar `reconnect`: PostgreSQL informó referencia ambigua a `connection_id` en `ON CONFLICT (connection_id)` de la RPC. No se aplicó ninguna corrección en esta subfase.
- La transacción fue revertida: fixtures residuales 0; Stores 0; Connections 0; Assignments 0; `integration_secrets` 0; `audit_events` 0; Persistent Owners 1.
- La pending OAuth real continúa sin consumir y está expirada. No hubo onboarding real, nuevo OAuth, Git closure ni exposición de secretos.

## 2.19H — remote final onboarding RPC validation

# FASE 2 — 2.19H REMOTE FINAL ONBOARDING RPC VALIDATION
# GPT-5.6 TERRA + MEDIUM
# APLICAR UNA SOLA MIGRATION REMOTA
# NO EJECUTAR ONBOARDING REAL

Proyecto: e-ngenieria Hub
Rama: develop
Base: 163a457
Migration pendiente: `20260824210000_finalize_pending_oauth_onboarding.sql`

Estado remoto previo conocido: proyecto `ffcudwwrzttkumbdvada`; Persistent Owners 1; Stores, Connections, Assignments e `integration_secrets` en 0; OAuth real ejecutado una vez; `oauth_attempts` 1 consumido; una `oauth_pending_authorizations` existente, potencialmente expirada por TTL y sólo informativa.

Objetivo: aplicar únicamente la migration `20260824210000_finalize_pending_oauth_onboarding.sql` y validar remotamente RPC final, atomicidad, privilegios, `SECURITY DEFINER`, `search_path` seguro, grants y comportamiento transaccional con rollback. No ejecutar onboarding real.

1. Confirmar working tree limpio, `HEAD == origin/develop` y base `163a457`; detenerse si no coincide.
2. Confirmar target Supabase `ffcudwwrzttkumbdvada`; detenerse si no coincide.
3. En modo read-only confirmar Persistent Owners 1; Stores, Connections, Assignments e `integration_secrets` en 0; consultar sólo si la pending existente está `ACTIVE` o `EXPIRED`, sin consumirla; confirmar que la migration `20260824210000` no está aplicada.
4. Ejecutar `supabase db push --dry-run`; debe mostrar exclusivamente `20260824210000_finalize_pending_oauth_onboarding.sql`; detenerse ante cualquier otra migration.
5. Si el dry-run es exacto, ejecutar `supabase db push`, sin aplicar otra migration.
6. Confirmar `20260824210000` como `APPLIED` y local/remoto alineados.
7. Confirmar existencia de la RPC final, `SECURITY DEFINER = YES`, `search_path` seguro y explícito; execute denegado a PUBLIC, anon y authenticated, permitido para service_role. No ampliar grants.
8. Validar atomicidad con una transacción que finalice en `ROLLBACK`, sin usar la pending real. Crear fixtures sólo dentro de ella. Caso Store nueva: Stores +1, Connections +1, `integration_secrets` +1, `audit_events` +2, pending consumida, Assignments +0; eventos `store.created` e `integration.connected`; luego rollback.
9. Si es posible en el mismo rollback: conexión deshabilitada se reactiva conservando la Connection original, actualiza secretos, genera `integration.reconnected` y no crea Store nueva.
10. Validar si es práctico: conexión activa devuelve `already_connected`, sin duplicado.
11. Validar si es práctico: provider y external account en otra Organization devuelve `conflict`, sin transferencia ni Store nueva.
12. No mostrar access token, refresh token, ciphertext, authorization code, state, master key ni service-role key.
13. Tras todos los rollbacks: Stores, Connections, Assignments e `integration_secrets` deben continuar en 0; ningún fixture residual; Persistent Owners 1; la pending OAuth real no consumida ni modificada.
14. Si la pending real expiró, reportar `REAL PENDING = EXPIRED`; no es fallo y no repetir OAuth.
15. Actualizar únicamente `docs/meli-database.md` y este documento si corresponde.
16. No repetir tests, typecheck, lint, build ni local db reset; ejecutar solamente `git diff --check` si cambia documentación.
17. No consumir la pending real ni crear Store, Connection, integration secret o audit event real de negocio; no nuevo OAuth.
18. No ejecutar `git add`, `git commit` ni `git push`.
19. Checkpoint: project ref; dry-run; db push; migration APPLIED; RPC; SECURITY DEFINER; search_path; execute PUBLIC/anon/authenticated/service_role; tests transaccionales new Store, audits, reconnect, active y cross-tenant; fixtures residuales; conteos finales; Owners; pending real ACTIVE/EXPIRED y no consumida; onboarding real NO; docs; `git diff --check`; git closure NO.

Después detenerse.

## Resultado — audit event consistency review

- No se confirmó inconsistencia de la RPC: el conteo previo incluyó sólo `integration.connected` y omitió `store.created`.
- Para un onboarding nuevo, la transacción inserta exactamente dos eventos: `store.created` (`store`, Store ID) e `integration.connected` (`connection`, Connection ID). La metadata de conexión queda limitada a provider, connection ID y store ID; la de Store está vacía.
- Para reconnect no se crea Store ni `store.created`; se registra únicamente `integration.reconnected`.
- La prueba local revertida confirmó: Stores 1, Connections 1, secrets 1, audits 2, pending consumida y assignments 0. Metadata libre de claves sensibles.
- La migration no cambió. La pending real sigue activa según consulta remota read-only; no se hizo onboarding real, migration remota, OAuth ni Git closure.
+## Cierre local — 2.19G final onboarding foundation

# CIERRE LOCAL 2.19G — FINAL ONBOARDING FOUNDATION
# GPT-5.6 LUNA
# COMMIT + PUSH
# NO REMOTE DB PUSH
# NO ONBOARDING REAL

Proyecto:
e-ngenieria Hub

Rama:
develop

Base:
d7fb41c

Estado aprobado:

Migration:
20260824210000_finalize_pending_oauth_onboarding.sql

RPC final:
IMPLEMENTADA LOCALMENTE

Admin onboarding:
ATÓMICO

New Store:
- store.created
- integration.connected

Reconnect:
- integration.reconnected

Token transfer:
CIPHERTEXT ONLY

Master key en SQL:
NO

Plaintext tokens en SQL:
NO

Tests relevantes:
7/7 PASS

Typecheck:
PASS

Lint:
PASS

Local reset:
PASS

Prueba transaccional:
Stores = 1
Connections = 1
integration_secrets = 1
audit_events = 2
pending consumed = 1
Assignments = 0

Atomicidad:
PASS

Secret hygiene:
PASS

Remote migration:
NOT APPLIED

Real onboarding:
NOT EXECUTED

============================================================
0. ARCHIVAR CIERRE
============================================================

Agregar este mandato completo al final de:

docs/prompts/phase-02/2.19e-first-real-meli-oauth.md

bajo:

## Cierre local — 2.19G final onboarding foundation

============================================================
1. AUDITORÍA MÍNIMA
============================================================

Ejecutar:

git status --short
git diff --stat
git diff
git diff --check

Confirmar que el scope corresponda únicamente a:

- migration 20260824210000_finalize_pending_oauth_onboarding.sql
- adapter/primitives server-only de finalización
- tests relacionados
- docs mínimos
- next.config.ts con allowedDevOrigins temporal
- documento operativo 2.19E/2.19F/2.19G

IMPORTANTE:

next.config.ts contiene un cambio temporal necesario para el túnel:

allowedDevOrigins:
key-rides-happy-revealed.trycloudflare.com

Debe quedar incluido por ahora porque seguimos usando ese entorno
para completar el onboarding real.

Si aparece cualquier otro archivo inesperado:
DETENERSE.

============================================================
2. SECRET HYGIENE
============================================================

Verificar que no exista ningún valor real de:

MERCADO_LIBRE_CLIENT_SECRET
INTEGRATION_SECRETS_MASTER_KEY
access_token
refresh_token
authorization code
state
ciphertext completo

.env.local:
NO debe estar staged.

============================================================
3. NO REPETIR VALIDACIONES
============================================================

NO ejecutar nuevamente:

tests
typecheck
lint
build
supabase db reset

Ya están aprobados.

Sólo repetir si se modifica código/SQL durante la auditoría.

============================================================
4. STAGING EXPLÍCITO
============================================================

Agregar individualmente únicamente los archivos pertenecientes
a este bloque de trabajo.

NO usar:

git add .
git add -A

Después ejecutar:

git diff --cached --stat
git diff --cached --check

============================================================
5. COMMIT
============================================================

git commit -m "feat(integrations): finalize pending oauth onboarding"

============================================================
6. PUSH
============================================================

git push origin develop

NO ejecutar:

supabase db push

NO ejecutar onboarding real.

============================================================
7. VERIFICAR
============================================================

Confirmar:

HEAD == origin/develop

working tree:
clean

============================================================
8. ESTADO FINAL
============================================================

Registrar:

Final onboarding RPC:
IMPLEMENTED

Atomicity:
VALIDATED LOCALLY

Encrypted token transfer:
VALIDATED

Audit:
store.created + integration.connected

Remote migration:
PENDING

Real onboarding:
NOT EXECUTED

============================================================
9. NO HACER
============================================================

NO:

nuevo OAuth
POST /connect
callback
token exchange
GET /users/me

supabase db push
remote SQL
remote business writes

crear Store real
crear Connection real
crear integration_secret real
consumir pending real

Phase 3

============================================================
10. CHECKPOINT
============================================================

Entregar únicamente:

1. commit hash;
2. commit message;
3. archivos incluidos;
4. migration incluida;
5. next.config.ts incluido = sí/no;
6. secret hygiene;
7. staged diff check;
8. push result;
9. HEAD == origin/develop;
10. working tree clean;
11. tests previos = 7/7 PASS;
12. local reset previo = PASS;
13. atomicidad = PASS;
14. remote migration = NOT APPLIED;
15. onboarding real = NO.

Después detenerse.

## Checkpoint técnico — 2.19G

- La pending real fue comprobada en modo solo lectura: activa, no expirada, `mercado-libre`, `admin_connect`, vinculada a Owner y Organization, con identidad externa y envelopes cifrados presentes.
- Las RPCs anteriores no podían consumir una pending ni transferir sus envelopes; se creó localmente la migration `20260824210000_finalize_pending_oauth_onboarding.sql` y el adaptador server-only correspondiente.
- La RPC usa pending ID opaco y Store.name, no recibe identidad externa ni tokens desde caller. El path admin crea/reutiliza Connection de forma transaccional, transfiere ciphertext sin descifrar, audita y consume pending; no crea assignment.
- `supabase db reset` local y una prueba transaccional revertida validaron `created`: Store, Connection, `integration_secrets` y audit event creados; pending consumida; assignments en cero. El proyecto remoto y la pending real no se modificaron.
- Tests relevantes, typecheck y lint: PASS. No hubo build, OAuth, `git add`, commit ni push.
+## 2.19G — audit event consistency review

# CONTINUACIÓN 2.19G — AUDITAR INCONSISTENCIA DE AUDIT EVENTS
# GPT-5.6 TERRA + MEDIUM
# NO COMMIT / NO REMOTE APPLY / NO ONBOARDING REAL

Proyecto:
e-ngenieria Hub

Estado técnico actual:

Migration:
20260824210000_finalize_pending_oauth_onboarding.sql

RPC final:
IMPLEMENTADA LOCALMENTE

Tests relevantes:
7/7 PASS

Typecheck:
PASS

Lint:
PASS

Local reset:
PASS

Remote migration:
NO

Onboarding real:
NO

Hay una inconsistencia en el checkpoint:

Se declaró que para un onboarding nuevo se generan:

- store.created
- integration.connected

pero la prueba transaccional reportó:

audit = 1

OBJETIVO:

Determinar exactamente qué comportamiento implementa actualmente
la RPC y dejar una política coherente antes de cerrar 2.19G.

============================================================
0. ARCHIVAR CONTINUACIÓN
============================================================

Agregar este mandato completo al documento operativo actual:

docs/prompts/phase-02/2.19e-first-real-meli-oauth.md

bajo:

## 2.19G — audit event consistency review

============================================================
1. INSPECCIONAR
============================================================

Revisar únicamente:

- migration 20260824210000_finalize_pending_oauth_onboarding.sql
- adapter/primitives server-only modificados en 2.19G
- tests correspondientes
- documentación de audit model si hace falta para confirmar semántica

No releer toda Fase 2.

============================================================
2. DETERMINAR COMPORTAMIENTO REAL
============================================================

Para el caso:

pending admin_connect
+ ninguna Connection previa
+ creación de nueva Store
+ nueva Connection

confirmar exactamente:

- cuántos audit_events se insertan;
- action de cada uno;
- resource_type/resource_id de cada uno;
- metadata allowlisted.

No ejecutar remote writes.

============================================================
3. POLÍTICA CANÓNICA
============================================================

Adoptar esta política salvo impedimento técnico real:

Cuando el onboarding CREA una nueva Store:

1. store.created
2. integration.connected

Deben ser DOS eventos separados.

Razón:

son dos acciones de negocio distintas y necesitamos trazabilidad
Owner/Manager de creación de Store y conexión del proveedor.

Cuando se REACTIVA una Connection existente:

NO store.created.

Registrar únicamente el evento correspondiente a la integración,
por ejemplo integration.reconnected si la taxonomía existente lo soporta,
o mantener la acción canónica ya definida si no existe otra.

No inventar taxonomía nueva innecesariamente.

============================================================
4. SI ACTUALMENTE HAY SOLO 1 EVENTO
============================================================

Corregir la migration forward-only aún NO aplicada en remoto.

Como esta migration todavía no fue aplicada remotamente,
puede editarse antes del commit si eso es coherente con las reglas
actuales del repositorio.

No crear una segunda migration únicamente para corregir una migration
local todavía no desplegada.

Actualizar test para exigir:

new Store onboarding:
Stores = 1
Connections = 1
integration_secrets = 1
audit_events relacionados = 2
pending consumed = 1
assignments = 0

============================================================
5. ATOMICIDAD
============================================================

Los DOS eventos deben pertenecer a la misma transacción.

Si falla cualquiera de:

Store
Connection
secret transfer
audit event
pending consumption

ROLLBACK completo.

============================================================
6. SECRET HYGIENE
============================================================

Audit metadata nunca debe contener:

access token
refresh token
ciphertext
authorization code
state
client secret
raw provider response

============================================================
7. VALIDACIÓN EFICIENTE
============================================================

Si hubo cambio SQL/test:

ejecutar únicamente:

- tests relevantes de 2.19G;
- supabase db reset local;
- prueba transaccional local;
- git diff --check.

Typecheck/lint sólo si cambia TypeScript.

No build.

============================================================
8. PENDING REAL
============================================================

No intentar preservar la pending real por apuro.

Si expira:
ACEPTADO.

NO repetir OAuth todavía.

El nuevo OAuth se hará solamente después de que:

- 2.19G quede cerrado;
- migration esté aplicada y validada en remoto.

============================================================
9. NO HACER
============================================================

NO:

supabase db push remoto
onboarding real
Store remoto
Connection remota
integration_secret remoto
nuevo OAuth
git add
git commit
git push

============================================================
10. CHECKPOINT
============================================================

Entregar:

1. audit events reales antes del ajuste;
2. inconsistencia confirmada sí/no;
3. política final adoptada;
4. audit events para new Store;
5. audit events para reconnect;
6. migration modificada sí/no;
7. tests modificados sí/no;
8. tests relevantes PASS/FAIL;
9. local reset PASS/FAIL;
10. prueba transaccional:
    - Stores;
    - Connections;
    - secrets;
    - audits;
    - pending consumed;
    - assignments;
11. atomicidad PASS/FAIL;
12. secret hygiene PASS/FAIL;
13. remote migration = NO;
14. onboarding real = NO;
15. pending real activa/expirada, sólo informativo;
16. git diff --check;
17. git add/commit/push = NO.

Después detenerse.

## Cierre — first real OAuth post-check

- Resultado browser: `READY_FOR_ONBOARDING`; display name seguro: `E.A.ZOCOOL`.
- Post-check remoto de solo lectura: un `oauth_attempt` consumido y una `oauth_pending_authorization` no consumida ni expirada.
- Binding validado entre attempt, pending, Organization y membership persistente Owner; provider `mercado-libre`, purpose `admin_connect` e identidad externa presente (sin registrar su valor).
- Pending contiene access token y refresh token cifrados, con expiración de access token y `expires_at` presentes. No se inspeccionaron ni registraron plaintext, authorization code, client secret, state completo ni ciphertext.
- Stores, Connections, Store Assignments e `integration_secrets` permanecen en cero; no hubo onboarding final ni limpieza.
- OAuth real se ejecutó una sola vez. No se ejecutaron replay, tests, typecheck, lint, build, `git add`, commit ni push.
+## 2.19G — First real Mercado Libre onboarding

# FASE 2 — 2.19G FIRST REAL MERCADO LIBRE ONBOARDING
# GPT-5.6 TERRA + MEDIUM
# CONSUMIR LA PENDING REAL Y CREAR EL PRIMER STORE/CONNECTION
# UNA SOLA OPERACIÓN TRANSACCIONAL

Proyecto:
e-ngenieria Hub

Rama:
develop

Estado actual aprobado:

OAuth real:
SUCCESS

Result:
READY_FOR_ONBOARDING

displayName:
E.A.ZOCOOL

Persistent Owners:
1

Stores:
0

Connections:
0

Assignments:
0

integration_secrets:
0

oauth_attempts:
1 consumed

oauth_pending_authorizations:
1 active / unconsumed

OBJETIVO:

Finalizar por primera vez el onboarding real de Mercado Libre
a partir de la pending authorization existente.

Resultado esperado:

pending authorization
→ Store
→ Connection
→ integration_secrets
→ audit event
→ pending consumed

Todo atómico.

NO repetir OAuth.

============================================================
0. ARCHIVAR PROMPT
============================================================

Agregar este mandato completo al final de:

docs/prompts/phase-02/2.19e-first-real-meli-oauth.md

bajo:

## 2.19G — First real Mercado Libre onboarding

============================================================
1. PRECHECK
============================================================

READ ONLY.

Confirmar:

oauth_pending_authorizations = 1

La pending debe estar:

- provider = mercado_libre;
- purpose = admin_connect;
- unconsumed;
- unexpired;
- bound al Owner persistente;
- bound a la Organization correcta;
- external_account_id PRESENT;
- encrypted access token PRESENT;
- encrypted refresh token PRESENT.

No mostrar valores sensibles.

Confirmar además:

Stores = 0
Connections = 0
integration_secrets = 0

Si difiere:
DETENERSE.

============================================================
2. PENDING TTL
============================================================

Verificar inmediatamente si la pending sigue dentro de sus 20 minutos.

Si está expirada:
DETENERSE.

NO reusar una pending expirada.
NO repetir OAuth automáticamente.

Reportar que se necesita nuevo OAuth.

============================================================
3. STORE NAME
============================================================

Para este primer onboarding usar como nombre visible de Store:

E.A.ZOCOOL

salvo que exista una razón técnica real para pedir otro nombre.

IMPORTANTE:

Store.name es display humano.

NO usar Store.name como identity/idempotency key.

La identidad fuerte sigue siendo:

provider + external_account_id.

============================================================
4. ONBOARDING ATÓMICO
============================================================

Revisar las RPC/primitives actuales.

El onboarding final debe ser una sola transacción que haga:

1. validar pending;
2. validar actor/Organization;
3. validar provider;
4. validar purpose;
5. validar expiración;
6. resolver provider + external_account_id;
7. crear Store si corresponde;
8. crear Connection;
9. transferir/copiar encrypted credentials hacia integration_secrets;
10. crear audit event;
11. consumir pending.

No debe existir estado parcial.

============================================================
5. IMPORTANTE — ATOMIC TOKEN TRANSFER
============================================================

Los tokens ya están cifrados en oauth_pending_authorizations.

NO descifrarlos en SQL.

La operación transaccional puede transferir/copiar
el envelope cifrado hacia integration_secrets.

La DB nunca debe necesitar:

INTEGRATION_SECRETS_MASTER_KEY.

Si el diseño actual requiere enviar plaintext tokens otra vez:
DETENERSE.

============================================================
6. SI FALTA RPC FINAL
============================================================

Sabemos que 2.19B dejó pendiente una RPC final para:

pending
→ onboarding
→ secrets
→ consume

Si todavía NO existe:

NO improvisar onboarding desde múltiples llamadas.

Implementar primero una migration forward-only con la RPC final.

Debe soportar inicialmente:

admin onboarding

y dejar coherencia con client onboarding.

Si hace falta nueva migration:
esto está AUTORIZADO en 2.19G,
pero NO aplicarla remoto todavía.

Primero checkpoint local.

============================================================
7. ADMIN PATH
============================================================

Para purpose:

admin_connect

Owner/Manager:

ALLOW.

Crear:

Store
Connection
integration_secrets
audit event

NO crear store_assignment.

============================================================
8. CONNECTION UNIQUENESS
============================================================

Resolver exclusivamente mediante:

provider + external_account_id

Casos:

NO existing Connection:
→ crear Store + Connection

ACTIVE same tenant:
→ already_connected
→ no duplicar Store
→ pending consumida/invalida según diseño seguro

DISABLED same tenant:
→ reactivar Connection original
→ actualizar secrets
→ NO crear nueva Store

CROSS-TENANT:
→ conflicto opaco
→ NO transferir
→ NO crear Store

============================================================
9. AUDIT EVENT
============================================================

Crear evento seguro para conexión exitosa.

Ejemplo semántico:

integration.connected

Debe incluir como máximo metadata allowlisted útil:

provider
connectionId
storeId

NO:

tokens
ciphertext
authorization code
state
client secret
external raw provider response

Actor:
Owner actual.

Organization:
actual.

Store:
Store creada.

============================================================
10. SERVER-SIDE FINALIZATION PRIMITIVE
============================================================

La app debe finalizar usando una primitive server-only.

Browser puede enviar:

pending authorization handle/ID opaco
store display name

pero NUNCA debe ser autoridad para:

Organization
actor
role
provider
external_account_id
tokens.

El server debe revalidar el binding.

============================================================
11. REAL EXECUTION POLICY
============================================================

Si la infraestructura final ya existe y está validada localmente:

antes de ejecutar el onboarding real,
DETENERSE y entregar checkpoint técnico.

NO ejecutar todavía el write real en la misma corrida
si hubo que crear/modificar código o migration.

Primero queremos revisar implementación.

Si NO hizo falta código/migration nueva
y ya existe exactamente la primitive transaccional requerida,
puede preparar la ejecución pero debe DETENERSE antes del write real.

============================================================
12. TESTS SI HAY CAMBIOS
============================================================

Si se modifica TS/SQL:

ejecutar:

tests relevantes
bun run typecheck
bun run lint

Si hay migration:

supabase db reset local
validar transacción local

Build sólo si se modifican routes/UI.

No ejecutar build si sólo cambian server primitives/SQL.

============================================================
13. NO REMOTE APPLY TODAVÍA
============================================================

Si se crea migration:

NO:

supabase db push remoto

NO ejecutar onboarding real.

Primero checkpoint.

============================================================
14. SI NO HACE FALTA CAMBIO
============================================================

Si ya existe una RPC/primitiva que satisface exactamente
todos los requisitos anteriores:

reportar:

READY TO EXECUTE FIRST REAL ONBOARDING

pero NO ejecutarla todavía.

============================================================
15. DOCUMENTACIÓN MÍNIMA
============================================================

Actualizar únicamente:

docs/meli-database.md
docs/meli-mercadolibre-OAuth/API.md
docs/prompts/phase-02/2.19e-first-real-meli-oauth.md

sólo si corresponde.

============================================================
16. NO GIT CLOSURE
============================================================

NO:

git add
git commit
git push

============================================================
17. CHECKPOINT
============================================================

Entregar concisamente:

1. pending sigue activa = sí/no;
2. pending purpose;
3. pending binding;
4. Store name propuesto;
5. RPC final ya existía = sí/no;
6. nueva migration necesaria = sí/no;
7. nombre de migration si existe;
8. atomicidad garantizada = sí/no;
9. token transfer encrypted-only = sí/no;
10. admin path;
11. disabled reconnect path;
12. active already_connected path;
13. cross-tenant path;
14. audit event;
15. assignment creado = NO;
16. tests;
17. typecheck;
18. lint;
19. local reset si aplica;
20. remote migration = NO;
21. onboarding real ejecutado = NO;
22. Stores final = 0;
23. Connections final = 0;
24. integration_secrets final = 0;
25. pending consumida = NO;
26. READY TO EXECUTE FIRST REAL ONBOARDING = YES/NO;
27. git add/commit/push = NO.

Después detenerse.
+## 2.19F — temporary Next.js dev origin fix

# 2.19F — FIX TEMPORAL NEXT.JS DEV ORIGIN

# GPT-5.6 LUNA

# CAMBIO MÍNIMO

# NO OAUTH TODAVÍA

Proyecto:
e-ngenieria Hub

Host público actual:

https://key-rides-happy-revealed.trycloudflare.com

Diagnóstico aprobado:

Next.js 16.2.12 está rechazando el WebSocket/HMR del host
trycloudflare porque next.config.ts no declara allowedDevOrigins.

OBJETIVO:

Aplicar exclusivamente el cambio mínimo necesario para permitir
el host temporal actual durante desarrollo.

============================================================
0. ARCHIVAR
============

Agregar este mandato al documento operativo actual:

docs/prompts/phase-02/2.19e-first-real-meli-oauth.md

bajo:

## 2.19F — temporary Next.js dev origin fix

============================================================

1. MODIFICAR
============================================================

Editar únicamente:

next.config.ts

Agregar dentro de la configuración Next existente:

allowedDevOrigins: [
'key-rides-happy-revealed.trycloudflare.com'
]

Usar únicamente hostname.

NO incluir:

https://
wildcards
*.trycloudflare.com

No alterar ninguna otra configuración.

============================================================
2. NO TOCAR
============

NO modificar:

Clerk
src/proxy.ts
.env.local
Mercado Libre OAuth
Supabase
middleware
dependencies

============================================================
3. VALIDACIÓN
==============

Ejecutar:

bun run typecheck

y:

git diff --check

No ejecutar tests completos.
No ejecutar build.

Mostrar diff final.

============================================================
4. NO GIT CLOSURE
==================

NO:

git add
git commit
git push

============================================================
5. CHECKPOINT
==============

Entregar:

1. next.config.ts modificado = sí/no;
2. allowedDevOrigins configurado;
3. hostname permitido;
4. otros archivos de código modificados = ninguno;
5. typecheck;
6. git diff --check;
7. OAuth ejecutado = NO;
8. git add/commit/push = NO.

Después detenerse.

## Cierre — pre-flight real OAuth

- **READY FOR FIRST REAL OAUTH:** YES.
- Configuración local validada sin exponer valores: Client ID y Client Secret presentes; `INTEGRATION_SECRETS_MASTER_KEY` Base64URL válida para AES-256 (32 bytes); PKCE explícitamente `false`.
- La redirect URI HTTPS exacta configurada es `https://key-rides-happy-revealed.trycloudflare.com/api/integrations/mercado-libre/callback`; coincide con la callback server-side y con la URI registrada manualmente en Mercado Libre Developers.
- Confirmación humana de DevCenter: Authorization Code y Refresh Token habilitados; PKCE deshabilitado y coherente con la configuración local; Client Credentials no se usa; la URL de callbacks de notificaciones no reutiliza la callback OAuth.
- Precheck remoto de solo lectura: persistent Owners = 1; Stores, Connections, `oauth_attempts`, `oauth_pending_authorizations` e `integration_secrets` = 0.
- El runtime y las rutas `POST /api/integrations/mercado-libre/connect` y `GET /api/integrations/mercado-libre/callback` permanecen listos según el checkpoint 2.19D; no se ejecutaron.
- OAuth real, `POST /connect`, authorization URL, callback, token exchange, `GET /users/me` y escrituras remotas de negocio: **NO ejecutados**.
- Tests y build no se repitieron; no hubo cambios de código. Git add, commit y push: **NO ejecutados**.
+## 2.19F — First real Mercado Libre OAuth

# FASE 2 — 2.19F FIRST REAL MERCADO LIBRE OAUTH
# GPT-5.6 TERRA + MEDIUM
# EJECUTAR UNA SOLA AUTORIZACIÓN REAL CONTROLADA
# NO CREAR STORE/CONNECTION

Proyecto:
e-ngenieria Hub

Rama:
develop

Base:
d7fb41c

Estado:

2.19E:
READY FOR FIRST REAL OAUTH = YES

Configuración local:

Client ID:
SET

Client Secret:
SET

Master key:
VALID AES-256

PKCE:
false

Redirect URI:

https://key-rides-happy-revealed.trycloudflare.com/api/integrations/mercado-libre/callback

DevCenter:

Authorization Code:
ENABLED

Refresh Token:
ENABLED

PKCE:
DISABLED

OAuth runtime:
READY

Persistent Owner:
1

Stores:
0

Connections:
0

Assignments:
0

oauth_attempts:
0

oauth_pending_authorizations:
0

integration_secrets:
0

OBJETIVO:

Ejecutar por primera vez el flujo OAuth real de Mercado Libre:

Owner autenticado
→ POST /connect
→ authorization URL
→ autorización humana en Mercado Libre
→ callback
→ state validation
→ token exchange
→ GET /users/me
→ encrypted pending authorization

Resultado esperado:

READY_FOR_ONBOARDING
o READY_FOR_RECONNECT si correspondiera.

NO crear todavía:

Store
Connection
Assignment
integration_secrets permanentes

============================================================
0. ARCHIVAR PROMPT
============================================================

Agregar este mandato completo al final de:

docs/prompts/phase-02/2.19e-first-real-meli-oauth.md

bajo:

## 2.19F — First real Mercado Libre OAuth

No crear otro documento salvo necesidad real.

============================================================
1. PRECHECK LOCAL
============================================================

Confirmar:

- app local corriendo;
- Cloudflare tunnel activo;
- URL pública actual coincide exactamente con Redirect URI;
- sesión Clerk disponible por el host HTTPS público;
- Organization activa;
- persistent Owner válido.

Si el túnel cambió de URL:
DETENERSE.

No actualizar DevCenter automáticamente.

============================================================
2. PRECHECK REMOTO
============================================================

READ ONLY.

Confirmar antes del OAuth:

hub_memberships = 1
persistent Owners = 1
stores = 0
connections = 0
store_assignments = 0
oauth_attempts = 0
oauth_pending_authorizations = 0
integration_secrets = 0

Si difiere:
DETENERSE.

============================================================
3. NO MOSTRAR SECRETOS
============================================================

Nunca imprimir ni registrar:

MERCADO_LIBRE_CLIENT_SECRET
INTEGRATION_SECRETS_MASTER_KEY
authorization code
access token
refresh token
ciphertext completo

============================================================
4. INICIAR DESDE HOST HTTPS
============================================================

La sesión de usuario debe estar abierta usando:

https://key-rides-happy-revealed.trycloudflare.com

NO iniciar el flujo desde localhost.

Verificar que Clerk funciona en ese host.

Si Clerk no conserva una sesión válida:
DETENERSE y reportar.

============================================================
5. START OAUTH
============================================================

Ejecutar una sola vez:

POST /api/integrations/mercado-libre/connect

como Owner autenticado.

No repetir si responde correctamente.

Esperado:

authorizationUrl válida.

No imprimir state completo si puede evitarse.

============================================================
6. OAUTH ATTEMPT
============================================================

Después del POST /connect:

verificar remote DB.

Esperado:

oauth_attempts = 1

Validar:

provider = mercado_libre
purpose correcto
actor membership correcto
organization correcta
unconsumed
unexpired

No mostrar verifier sensible.

============================================================
7. AUTORIZACIÓN HUMANA
============================================================

Abrir la authorization URL generada.

La autorización debe realizarla el usuario humano.

NO automatizar:

login
password
2FA
consent

El agente debe detenerse y pedir al usuario que complete
la autorización en Mercado Libre si necesita interacción humana.

============================================================
8. CONSENTIMIENTO
============================================================

El usuario autoriza la aplicación real.

No compartir credenciales con Codex.

Después del consentimiento,
Mercado Libre debe redirigir exactamente a:

https://key-rides-happy-revealed.trycloudflare.com/api/integrations/mercado-libre/callback

============================================================
9. CALLBACK
============================================================

Observar el resultado del callback.

Esperado:

state válido
→ attempt consumed
→ token exchange
→ GET /users/me
→ identity verified
→ encrypted pending authorization

No imprimir respuestas OAuth crudas.

============================================================
10. IDENTITY
============================================================

Validar únicamente de forma segura:

external_account_id:
PRESENT

displayName/nickname:
puede mostrarse

No mostrar access token ni refresh token.

============================================================
11. PENDING AUTHORIZATION
============================================================

Verificar remote DB.

Esperado:

oauth_pending_authorizations = 1

Confirmar:

- external identity presente;
- organization correcta;
- actor correcto;
- provider correcto;
- purpose correcto;
- encrypted access token presente;
- encrypted refresh token presente si ML lo devolvió;
- TTL cercano a 20 minutos;
- consumed_at = null.

No mostrar ciphertext.

============================================================
12. OAUTH ATTEMPT FINAL
============================================================

Confirmar:

oauth_attempts = 1

y:

consumed_at != null

Replay:
NO ejecutar segundo callback.

============================================================
13. BUSINESS TABLES
============================================================

Confirmar que permanecen:

stores = 0
connections = 0
store_assignments = 0
integration_secrets = 0

No ejecutar onboarding final.

============================================================
14. ACTIVE/DISABLED CONNECTION
============================================================

Como el precheck tenía Connections = 0,
resultado esperado:

READY_FOR_ONBOARDING

Si aparece:

already_connected
READY_FOR_RECONNECT
connection_conflict

DETENERSE y reportar.

============================================================
15. RESPONSE TO BROWSER
============================================================

Confirmar que la respuesta visible NO contiene:

access token
refresh token
authorization code
client secret
ciphertext

Puede contener:

READY_FOR_ONBOARDING
displayName

============================================================
16. FAILURE POLICY
============================================================

Si ocurre cualquier error real:

NO repetir automáticamente el OAuth.

NO iniciar un segundo attempt.

Capturar únicamente:

status
error code normalizado
etapa donde falló

No mostrar secretos.

Luego DETENERSE.

============================================================
17. NO CLEANUP TODAVÍA
============================================================

Si el OAuth funciona:

NO consumir la pending authorization.
NO borrar attempt.
NO borrar pending.

Necesitamos inspeccionar el estado para el checkpoint.

============================================================
18. DOCUMENTACIÓN
============================================================

Actualizar únicamente:

docs/prompts/phase-02/2.19e-first-real-meli-oauth.md

con resultado resumido y sin secretos.

No documentar:

tokens
authorization code
state completo
ciphertext
secret values.

============================================================
19. VALIDACIÓN
============================================================

No ejecutar:

tests
typecheck
lint
build
db reset
db push

No hubo cambio de código.

Sólo:

git diff --check

si se modificó documentación.

============================================================
20. NO GIT CLOSURE
============================================================

NO:

git add
git commit
git push

Primero checkpoint.

============================================================
21. CHECKPOINT
============================================================

Entregar únicamente:

1. host HTTPS usado;
2. Clerk session por host público = PASS/FAIL;
3. POST /connect = PASS/FAIL;
4. authorization URL generated = YES/NO;
5. oauth_attempts después del start;
6. autorización humana = COMPLETED/NOT COMPLETED;
7. callback alcanzado = YES/NO;
8. state validation = PASS/FAIL;
9. attempt consumed = YES/NO;
10. token exchange = PASS/FAIL;
11. GET /users/me = PASS/FAIL;
12. external account id = PRESENT/ABSENT;
13. displayName si es seguro mostrarlo;
14. pending authorization creada = YES/NO;
15. pending TTL;
16. encrypted access token = PRESENT/ABSENT;
17. encrypted refresh token = PRESENT/ABSENT;
18. browser token leakage = NO/YES;
19. result = READY_FOR_ONBOARDING / otro;
20. Stores;
21. Connections;
22. Assignments;
23. integration_secrets;
24. oauth_attempts final;
25. oauth_pending_authorizations final;
26. persistent Owners;
27. remote business writes = NO;
28. OAuth real = YES;
29. docs modified;
30. git diff --check;
31. git add/commit/push = NO.

Después detenerse.

## 2.19I — forward fix reconnect RPC ambiguity

# FASE 2 — 2.19I FIX FORWARD DE RECONNECT RPC
# GPT-5.6 TERRA + MEDIUM
# MIGRATION FORWARD MÍNIMA
# NO ONBOARDING REAL

Proyecto: e-ngenieria Hub. Rama: develop. Base: `163a457`.

La migration `20260824210000_finalize_pending_oauth_onboarding.sql` ya está aplicada en remoto. La RPC `finalize_admin_pending_integration_onboarding` aprobó el path New Store dentro de rollback y falló en reconnect: `ON CONFLICT (connection_id)` es ambiguo porque `connection_id` también es una salida/variable de la función. Active existing y Cross-tenant siguen sin validar. Persistent Owners 1; Stores, Connections, Assignments e `integration_secrets` en 0; pending real EXPIRED; onboarding real NO.

Objetivo: crear una migration forward mínima y posterior a `20260824210000`, que corrija exclusivamente la ambigüedad de reconnect. No editar la migration ya aplicada, no renombrar columnas/tablas ni alterar schema de negocio. Inspeccionar el UPSERT afectado y preferir una solución PostgreSQL explícita, como `ON CONFLICT ON CONSTRAINT <constraint_name>` cuando corresponda.

La corrección debe preservar: reconnect de Connection disabled sobre Store y Connection originales; actualización de envelopes cifrados; evento `integration.reconnected`; consumo de pending; cero Store/assignment nuevos. No debe alterar active existing (`already_connected`, sin duplicados ni overwrite indebido) ni cross-tenant (conflict opaco, sin Store, secrets ni reassignment).

La nueva definición debe conservar `SECURITY DEFINER`, `search_path=pg_catalog`, referencias calificadas y grants: PUBLIC/anon/authenticated denegados; service_role permitido. No tocar cifrado ni introducir tokens plaintext, master key, authorization code o state.

Ejecutar `supabase db reset` y pruebas locales transaccionales con `ROLLBACK`: New Store (+1 Store, +1 Connection, +1 secret, +2 audits, pending consumida, assignments 0); Reconnect disabled (sin Store/Connection nueva, original reactivada, secret actualizado, un `integration.reconnected`, pending consumida); Active existing (`already_connected`, sin duplicados); Cross-tenant (`conflict`, sin transferencia ni Store nueva). Ejecutar tests relevantes. Si cambian sólo SQL/docs, no ejecutar typecheck/lint/build.

No aplicar la nueva migration a remoto, no ejecutar SQL manual remoto, no reutilizar pending real, no iniciar OAuth y no hacer `git add`, commit ni push. Actualizar únicamente `docs/meli-database.md` y este documento si corresponde; entregar checkpoint y detenerse.

### Resultado 2.19I

La ambigüedad quedó confirmada en el UPSERT de `public.integration_secrets`: `connection_id` colisionaba con la columna de salida de la RPC. Se creó la migration forward local `20260824220000_fix_finalize_oauth_reconnect_conflict.sql`, que reemplaza únicamente ese target por `ON CONFLICT ON CONSTRAINT integration_secrets_pkey` y reafirma los grants de la función.

`supabase db reset` local aplicó todas las migrations, incluida la nueva. Una matriz transaccional con rollback validó New Store, reconnect disabled, active existing y cross-tenant; no dejó fixtures. Los siete tests focalizados de `oauth-foundations` pasaron. No se ejecutaron typecheck, lint ni build porque no cambió TypeScript; no se aplicó migration remota, no se realizó onboarding ni OAuth, y no hubo Git closure.

## Cierre — 2.19I reconnect RPC fix

# CIERRE 2.19I — FIX RECONNECT RPC
# GPT-5.6 LUNA
# COMMIT + PUSH
# NO DB PUSH REMOTO
# NO OAUTH REAL

Proyecto: e-ngenieria Hub. Rama: develop. Base: `163a457`.

Estado aprobado: nueva migration `20260824220000_fix_finalize_oauth_reconnect_conflict.sql`; causa `ON CONFLICT (connection_id)` ambiguo en `integration_secrets`; fix `ON CONFLICT ON CONSTRAINT integration_secrets_pkey`; migration previa `20260824210000` no modificada. Local reset, New Store, Reconnect disabled, Active existing y Cross-tenant: PASS. Tests focalizados 7/7 PASS. Secret hygiene PASS. Remote migration NOT APPLIED; onboarding real NO; pending real EXPIRED.

1. Ejecutar `git status --short`, `git diff --stat`, `git diff` y `git diff --check`. Deben aparecer únicamente `docs/meli-database.md`, este documento operativo y `supabase/migrations/20260824220000_fix_finalize_oauth_reconnect_conflict.sql`; detenerse si aparece otro archivo.
2. Confirmar que no existe access token, refresh token, authorization code, state completo, ciphertext, master key o service-role key; `.env.local` no se stagea.
3. No repetir tests, typecheck, lint, build ni `supabase db reset`.
4. Stagear individualmente sólo los tres archivos, sin `git add .` ni `git add -A`; ejecutar `git diff --cached --stat` y `git diff --cached --check`.
5. Crear `fix(database): resolve oauth reconnect conflict`.
6. Ejecutar `git push origin develop`; no ejecutar `supabase db push`, OAuth ni onboarding real.
7. Confirmar `HEAD == origin/develop` y working tree limpio.
8. Entregar hash, mensaje, archivos, migrations incluida y previa intacta, higiene, staged diff check, push, sincronización, working tree, reset/tests previos, remote migration NOT APPLIED, onboarding NO y pending EXPIRED.

Después detenerse.
