# Fase 2 — 2.20H: First real Mercado Libre token refresh

## Mandato operativo

Ejecutar exactamente una rotación real de las credenciales de la Connection Mercado Libre de E.A.ZOCOOL usando `MercadoLibreCredentialService.getValidAccessToken()`. El access token inicial está vencido y el refresh token sólo existe cifrado. La foundation remota ya está validada con lease por Connection, `credential_version`, CAS, TTL de lease de 60 s, timeout HTTP de 15 s y ventana de seguridad de 120 s.

Resolver server-side Organization → Store E.A.ZOCOOL → Connection Mercado Libre activa → `integration_secrets`; no aceptar autoridad del navegador. Confirmar Owner 1, Store 1, Connection activa 1, Assignments 0, secreto 1 y Listings 0. Confirmar lease inicial clear y registrar sólo el número de `credential_version`, nunca tokens, ciphertexts, UUID de lease innecesario, client secret, master key ni service-role key.

Ejecutar una sola vez la primitive: lectura, expiry check, claim, double-check, un único `POST /oauth/token` con `grant_type=refresh_token`, validación de access/refresh token rotado, cifrado server-side, CAS complete y release. Si claim devuelve `BUSY`, si el double-check encuentra un token ya vigente, si el response no trae refresh token, si falla CAS o queda lease activo, detenerse sin reintentar. No llamar manualmente al provider fuera de la primitive.

Después del complete, verificar de forma read-only expiry futura, refresh cifrado presente, versión N+1, lease limpio, secreto 1 y Connection activa. Ejecutar una única consulta oficial `GET /users/me` con el nuevo token; confirmar identity match y displayName E.A.ZOCOOL sin mostrar `external_account_id` completo. No ejecutar listing search, multiget, `ListingSyncService`, persistencia de listings, OAuth nuevo ni otras mutaciones Mercado Libre. La única escritura remota autorizada es la rotación segura de `integration_secrets`.

Si el provider devuelve refresh inválido/revocado, no iniciar OAuth: informar `RECONNECT_REQUIRED` y confirmar preservación de credenciales según el estado observado. No modificar código, migrations, Store, Connection, Assignments, pending authorizations ni el prompt 2.20E. No ejecutar tests, typecheck, lint, build, reset, db push, git add, commit ni push; sólo `git diff --check` y la documentación mínima de este checkpoint.

## Estado

Refresh real: pendiente de ejecución controlada. Listing sync real: no iniciado. Prompt 2.20E preexistente: preservado.

## Resultado — STOP antes de rotación confirmada

La única invocación controlada terminó con `real_refresh_failed`. No se reintentó. El postcheck remoto read-only confirmó `credential_version=1`, access token expirado, lease clear, envelope de refresh presente, Connection activa y estado real sin cambios. No se confirmó llamada a `/oauth/token`, no se confirmó escritura de credentials y no se ejecutó `GET /users/me`. El runner temporal fue eliminado. El refresh real sigue pendiente.
