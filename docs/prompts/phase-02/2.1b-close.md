# Cierre de Subfases 2.1 + 2.1B

Status: **COMPLETED — commit pendiente**

## Purpose

Cerrar y versionar el diseño de schema/migraciones y el tooling de Supabase CLI sin iniciar el schema funcional.

## Context

- 2.1 definió el schema SQL y estrategia de migraciones.
- 2.1B instaló Supabase CLI `2.114.0` como devDependency exacta.
- `supabase/` fue inicializado.
- No existe schema funcional ni migraciones funcionales.
- No se inició 2.2.

## Restrictions

No implementar schema, crear tablas o migraciones funcionales, ejecutar Supabase local/remoto ni iniciar 2.2.

## Prompt

### Cerrar Subfases 2.1 + 2.1B — schema documental y tooling Supabase CLI

Proyecto e-ngenieria Hub, rama esperada `develop`, con base `f8a13d5`. La 2.1 documental y 2.1B están completadas; CLI `2.114.0` exacta instalada, tooling local inicializado, sin schema/migraciones funcionales ni 2.2. El objetivo es únicamente archivar, auditar, validar, stagear, commit y push de los cambios legítimos.

0. Antes de cualquier acción, archivar este mandato completo como `docs/prompts/phase-02/2.1b-close.md` con Status ACTIVE, Purpose, Context, Restrictions, Prompt, Result PENDING y Commit PENDING; confirmar su existencia antes de continuar.
1. Actualizar `docs/prompts/README.md` y, si corresponde, el workflow para establecer que todos los prompts operativos de Codex se archivan en `docs/prompts/` antes de la primera acción: auditorías, diseño, implementación, decisiones, bloqueos, reintentos, correcciones, validaciones, checkpoints, cierres, commits, pushes y handoffs. No guardar conversaciones completas; conservar los mandatos. Si hay bloqueo, mantener prompt y actualizar Result.
2. Mantener `phase-01/`, `phase-02/` y nombres ordenados/descriptivos (`2.1-schema-migraciones`, `2.1b-supabase-cli`, `2.1b-close`, `2.2-persistencia`, etc.), con sufijos `-retry`, `-fix`, `-close`, `-audit` o `-decisions`; no sobrescribir prompts históricos.
3. Auditar status, stat y diff, clasificando cada cambio como 2.1, 2.1B, regla de archivo o no relacionado. Los archivos legítimos abarcan package/lock, config/ignore de Supabase, docs de database/plan/handoff/multi-tenancy/workflow/prompts y los tres prompts. Si aparece código funcional ajeno, detenerse y registrar el bloqueo.
4. Confirmar `supabase: "2.114.0"` exacta en devDependencies y `bunfig.toml` intacto con antigüedad mínima de siete días.
5. Validar `bunx supabase --version` y `--help`; no ejecutar start, reset, push, link ni login.
6. Revisar config e ignore: `project_id` local permitido, sin `project_ref` remoto, passwords, tokens, service-role keys ni secretos; config versionable y migrations futuras versionables. El bloque auth estándar de la CLI no significa que e-Hub use Supabase Auth: Clerk sigue como AuthN.
7. Confirmar ausencia de schema, migrations funcionales, tablas de negocio, repositories, Store Scope funcional y OAuth. Es válido que `supabase/migrations/` no exista; no crearla.
8. Ejecutar test, typecheck, lint, build y diff check; 31 tests esperados. Warnings Vitest/Vite, Google Sans Flex y metadataBase no bloquean.
9. Antes de staging, actualizar Result de los prompts 2.1, 2.1B y cierre, manteniendo Commit PENDING y preservando el prompt original.
10. Hacer staging explícito solo de cambios 2.1/2.1B/regla de archivo; no usar `git add .` o `git add -A`. Revisar staged diff/stat/check y confirmar ausencia de secretos, código ajeno y schema.
11. Crear un único commit `chore(database): prepare Supabase migration tooling`, representando diseño, CLI pinneada, configuración local, documentación y prompts, no creación de schema.
12. Tras el commit, no crear un bucle de commits para insertar su hash en el mismo commit. Actualizar metadata solo conforme a la convención del README; si hiciera falta persistirla, usar un segundo commit documental mínimo.
13. Confirmar rama `develop` y hacer `git push origin develop`, nunca a main.
14. Verificar log, status, alineación de HEAD/origin y árbol limpio.
15. Si existe una actualización documental permitida del cierre, marcarlo COMPLETED con resultado, commit o referencia de convención, sin bucles infinitos.
16. Entregar checkpoint de archivado, regla global, archivos, hash/mensaje, CLI/devDependency/bunfig/config/Clerk, validaciones, metadata, remoto/árbol, cierre 2.1/2.1B y confirmaciones de no schema/migrations/Docker/start/reset/push/link/2.2.

## Result

Cierre preparado: la regla global de archivado previo quedó documentada; Supabase CLI `2.114.0` exacta, `supabase/config.toml` y su ignore local están listos para versionar; tests, typecheck, lint, build y diff check pasaron. No hay schema, migraciones funcionales, Docker, ejecución local/remota ni 2.2.

## Commit

PENDING
