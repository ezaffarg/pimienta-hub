# FASE 2 — 2.20T SYNC-RUN ORCHESTRATION & PERSISTENT OBSERVABILITY
# GPT-5.6 SOL + MEDIUM
# IMPLEMENTACIÓN LOCAL
# NO MERCADO LIBRE REAL
# NO OAUTH / REFRESH / RECONNECT
# NO REMOTO
# NO GIT CLOSURE TODAVÍA

Proyecto:
e-ngenieria Hub

Rama:
develop

Base:
2322b0edd647d6046d062535549ae6f1b5470530

Estado confirmado:

2.20S = CLOSED

Auditoría 2.20T:
READY_TO_IMPLEMENT_2_20T

============================================================
0. ARCHIVO OPERATIVO
============================================================

Continuar en:

docs/prompts/phase-02/2.20t-sync-run-orchestration-observability.md

Si no existe:
crearlo y archivar este mandato completo antes de modificar código.

============================================================
1. OBJETIVO
============================================================

Implementar el corte mínimo aprobado de 2.20T:

- tabla listing_sync_runs;
- start RPC atómica;
- progress/checkpoint persistente;
- finalize RPC atómica;
- exclusión de doble ejecución;
- idempotency key;
- audit events;
- repository;
- orquestador server-only;
- integración mínima con backfill 2.20S.

NO implementar:

- scheduler;
- worker/queue;
- heartbeat;
- auto-expiration;
- resumability;
- persistent cursor;
- missing reconciliation;
- soft-delete;
- child error table;
- generic job platform.

============================================================
2. MIGRATION
============================================================

Crear UNA migration forward-only.

Tabla:

listing_sync_runs

Campos REQUIRED aprobados:

id
organization_id
store_id
connection_id
actor_membership_id
kind
idempotency_key
status
started_at
completed_at nullable
last_checkpoint_at

discovered_count
requested_count
fetched_count
persisted_count
failed_count
pages_count
batches_count

error_code nullable
error_summary nullable
updated_at

No agregar campos OPTIONAL/DEFER salvo necesidad demostrada.

============================================================
3. KIND
============================================================

Corte inicial:

kind = listing_backfill

Usar CHECK o contrato equivalente simple.

No crear catálogo genérico de job kinds.

============================================================
4. STATUS
============================================================

Estados permitidos:

running
succeeded
partial
failed

No agregar:

pending
cancelled
expired

en este corte.

Reglas mínimas:

running:
completed_at NULL

terminal:
completed_at NOT NULL

Validar coherencia con CHECKs cuando sea razonable.

============================================================
5. COUNTERS
============================================================

Todos los contadores:

>= 0

Defaults:
0

Mantener nombres coherentes entre DB y TypeScript.

No permitir valores negativos.

============================================================
6. TENANT BINDING
============================================================

Mantener el patrón canónico:

Organization
→ Store
→ Connection
→ ListingSyncRun

Usar FK compuesta equivalente a foundations existentes.

También vincular:

actor_membership_id
+
organization_id

para impedir actor cross-tenant.

No confiar en Organization/Store desde browser.

============================================================
7. IDEMPOTENCY KEY
============================================================

Usar UUID obligatorio.

Unique:

organization_id
connection_id
kind
idempotency_key

La idempotency key:

- deduplica start requests;
- NO autoriza;
- NO define tenant;
- NO sustituye server context.

Si se repite el mismo key de un run ya existente:
devolver de forma segura el run existente o outcome canónico,
sin crear otro.

Definir comportamiento determinista y testearlo.

============================================================
8. SINGLE RUNNING
============================================================

Crear índice único parcial equivalente:

único por:

connection_id + kind

WHERE status = 'running'

Objetivo:

sólo un listing_backfill running por Connection.

Un segundo run distinto mientras existe running:

→ already_running

No reutilizar credential refresh lease.

============================================================
9. START RPC
============================================================

Crear RPC server-only atómica para iniciar run.

Debe:

1. recibir inputs mínimos necesarios;
2. validar Organization/Store/Connection/actor;
3. verificar Connection activa según contrato vigente;
4. resolver idempotency;
5. detectar already_running;
6. insertar listing_sync_runs status=running;
7. insertar audit:
   listing.sync.started;
8. devolver run seguro.

Todo en una transacción.

Si falla audit/insert:
rollback.

Mantener:

SECURITY DEFINER si corresponde
search_path seguro
service_role-only
anon/authenticated denied.

============================================================
10. PROGRESS / CHECKPOINT
============================================================

Implementar mecanismo server-only para actualizar:

discovered
requested
fetched
persisted
failed
pages
batches
last_checkpoint_at

Preferencia:
RPC o repository controlado según patrón actual.

Debe validar:

organization_id
run status = running
counters monotónicos o contrato coherente.

No persistir cursor/scroll_id.

No prometer resumability.

Evitar una escritura excesiva por item.

Actualizar progreso:

- después de batch persistido;
- al cerrar página;

según el diseño de 2.20S.

============================================================
11. FINALIZE RPC
============================================================

Crear RPC atómica para cerrar un run.

Outcomes:

succeeded
partial
failed

Debe:

- exigir run actual = running;
- persistir counters finales;
- completed_at;
- last_checkpoint_at;
- error_code/error_summary si corresponde;
- audit terminal correcto:

listing.sync.succeeded
listing.sync.partial
listing.sync.failed

Todo atómico.

No permitir transición terminal → otro estado.

============================================================
12. ERROR MODEL
============================================================

Permitir únicamente códigos aprobados:

provider_rate_limited
provider_timeout
provider_unavailable
invalid_provider_response
credential_failure
persistence_failure
partial_item_failure

Si succeeded:
error_code/error_summary deben ser NULL.

Si partial/failed:
permitir código seguro.

error_summary:
texto controlado y acotado.

NO persistir error.message crudo.

NO tokens.
NO raw provider bodies.
NO headers.

============================================================
13. AUDIT
============================================================

Eventos:

listing.sync.started
listing.sync.succeeded
listing.sync.partial
listing.sync.failed

Resource:
listing_sync_run

resource id:
run id

Metadata mínima:

kind
provider
status

NO duplicar counters completos en metadata si ya viven en run.

Preservar lectura audit:
Owner + Manager según política vigente.

============================================================
14. REPOSITORY
============================================================

Crear repository específico para listing_sync_runs.

No crear GenericJobRepository.

Funciones mínimas:

start/get
checkpoint/update progress
finalize

Todas tenant-bound.

============================================================
15. ORQUESTADOR SERVER-ONLY
============================================================

Crear un orquestador específico de listing sync.

Responsabilidad:

1. start run;
2. ejecutar backfill 2.20S;
3. recibir progreso;
4. checkpoint seguro;
5. finalizar succeeded/partial/failed;
6. devolver resultado seguro.

ListingSyncService continúa siendo kernel de upsert.

No mover lógica del provider a DB layer.

============================================================
16. 2.20S PROGRESS CALLBACK
============================================================

Extender mínimamente el backfill 2.20S para permitir:

callback opcional de progreso

o mecanismo equivalente coherente.

Exponer:

counters
pages
batches

No romper callers existentes.

No hacer progreso por item individual.

============================================================
17. FALLOS DURANTE ORQUESTACIÓN
============================================================

Si provider/backfill falla:

intentar finalizar run como failed mediante contrato seguro.

Si hay partial failures pero ejecución completa:
finalizar partial.

Si todo PASS:
succeeded.

IMPORTANTE:

Una caída del proceso puede dejar running.

NO implementar auto-recovery todavía.

Documentar:

stale running run requires explicit future administrative recovery.

No desbloquear silenciosamente.

============================================================
18. NO RESUMABILITY
============================================================

No guardar:

offset
cursor
scroll_id

Una nueva ejecución empieza discovery desde cero.

El upsert de listings ya es idempotente.

Checkpoint = observabilidad,
NO resumability.

============================================================
19. RLS / GRANTS
============================================================

listing_sync_runs:

RLS enabled

No browser-facing policies.

Revoke:

PUBLIC
anon
authenticated

Permitir únicamente service_role por paths server-only.

RPCs:
service_role only.

Verificar post-migration.

============================================================
20. TESTS SQL
============================================================

Agregar matriz local determinista para:

A. start running
B. audit started
C. duplicate same idempotency key
D. different key while running → already_running
E. cross-tenant Connection
F. cross-tenant actor membership
G. progress checkpoint
H. counters invalid/negative
I. succeeded
J. partial
K. failed
L. terminal cannot transition again
M. audit terminal atomic
N. unique running invariant
O. RLS/grants
P. rollback on audit/transition failure

Fixtures sintéticos + rollback.

============================================================
21. TESTS TYPESCRIPT
============================================================

Cubrir:

- repository mapping;
- orchestrator succeeded;
- orchestrator partial;
- orchestrator failed;
- progress callbacks;
- idempotency reuse;
- already_running;
- tenant fail-closed;
- sanitized errors;
- listings remain idempotent across multiple runs.

No Mercado Libre real.

============================================================
22. MIGRATION / VALIDACIÓN LOCAL
============================================================

Como hay migration:

ejecutar:

supabase db reset

Luego:

matriz SQL local
tests focalizados
suite completa
typecheck
lint
git diff --check

Build:
NO salvo necesidad concreta.

============================================================
23. DOCUMENTACIÓN
============================================================

Actualizar:

docs/prompts/phase-02/2.20t-sync-run-orchestration-observability.md

docs/meli-database.md
docs/meli-api.md

sólo lo necesario.

Registrar explícitamente:

checkpoint != resumability

y:

stale running recovery = DEFERRED.

Repo = Source of Truth.

============================================================
24. NO HACER
============================================================

NO:

Mercado Libre real calls
listing sync real
OAuth
refresh
reconnect
remote DB
scheduler
worker
missing reconciliation
soft-delete
git add
commit
push

============================================================
25. STOP CONDITIONS
============================================================

DETENERSE si:

- requiere generic job platform;
- requiere scheduler para funcionar;
- actor no puede resolverse de forma segura;
- audit atomicity no puede preservarse;
- tenant isolation se debilita;
- migration requiere modificar foundations históricas destructivamente;
- secretos pueden terminar en error_summary;
- progreso exige persistir provider cursor.

============================================================
26. CHECKPOINT
============================================================

Entregar:

1. migration creada;
2. listing_sync_runs creada = sí/no;
3. campos REQUIRED completos = sí/no;
4. statuses = PASS/FAIL;
5. counters checks = PASS/FAIL;
6. tenant FK = PASS/FAIL;
7. actor FK = PASS/FAIL;
8. idempotency unique = PASS/FAIL;
9. unique running = PASS/FAIL;
10. start RPC = PASS/FAIL;
11. audit started atómico = PASS/FAIL;
12. progress/checkpoint = PASS/FAIL;
13. finalize succeeded = PASS/FAIL;
14. finalize partial = PASS/FAIL;
15. finalize failed = PASS/FAIL;
16. terminal transition protection = PASS/FAIL;
17. terminal audit atomic = PASS/FAIL;
18. error sanitization = PASS/FAIL;
19. RLS = PASS/FAIL;
20. grants = PASS/FAIL;
21. repository = PASS/FAIL;
22. orchestrator = PASS/FAIL;
23. 2.20S progress callback = PASS/FAIL;
24. stale running recovery implemented = NO;
25. resumability implemented = NO;
26. SQL matrix = X/X;
27. tests focalizados = X/X;
28. suite completa = X/X;
29. typecheck = PASS/FAIL;
30. lint = PASS/FAIL;
31. git diff --check = PASS/FAIL;
32. remote = NO;
33. Mercado Libre real calls = NO;
34. OAuth/refresh/reconnect = NO;
35. git add/commit/push = NO;
36. resultado:

2_20T_LOCAL_PASS
/
CODE_GAP_FOUND
/
NOT_READY

37. recomendación:

READY_FOR_REMOTE_2_20T_VALIDATION
/
NEEDS_FIX

Después detenerse.

## Resultado local

Implementación mínima completada sobre la base `2322b0e`. Se creó una única
migration forward-only con `listing_sync_runs`, FKs tenant/actor, idempotency
UUID, exclusión de doble `running`, RLS/grants server-only y RPCs atómicas de
start/finalize. El checkpoint RPC exige contadores monotónicos y no persiste
cursores.

El repository específico mapea start/get/checkpoint/finalize. El orquestador
server-only reutiliza el backfill 2.20S, persiste progreso por batch/página y
cierra `succeeded`, `partial` o `failed` con errores allowlisted. El backfill
expone `pages`, `batches` y callback opcional sin romper callers existentes.
No se agregó route, scheduler, worker, plataforma genérica, heartbeat,
auto-expiration, child errors, missing reconciliation ni soft-delete.

`supabase db reset`: PASS. Matriz SQL transaccional: 16/16 PASS y fixtures
persistidas 0. Tests focalizados: 32/32 PASS. Suite completa: 163/163 PASS.
Typecheck y lint: PASS. Build: no ejecutado por mandato. No hubo Mercado Libre
real, listing sync real, OAuth, refresh, reconnect, DB remota ni cierre Git.

**Checkpoint no es resumability.** Una nueva ejecución reinicia discovery y
reutiliza el upsert idempotente. Una caída del proceso puede dejar el run
`running`; stale running recovery permanece `DEFERRED` y exige una futura
acción administrativa explícita.

Resultado: `2_20T_LOCAL_PASS`. Recomendación:
`READY_FOR_REMOTE_2_20T_VALIDATION`.

## Continuación aprobada — cierre de gaps locales

El mandato de recuperación autorizó exclusivamente cerrar cobertura y dos gaps
locales antes de cualquier validación remota: rechazo explícito de counters
`NULL`, reuse histórico con Connection posteriormente disabled, límites DB de
errores, metadata terminal, fallos RPC y una prueba real local de idempotencia
de Listings entre runs. No autorizó scheduler, recovery automática,
resumability, llamadas a Mercado Libre, DB remota ni cierre Git.

La migration local no aplicada remotamente se ajustó de forma mínima. El start
valida primero los bindings y el actor; una key existente se reconoce sin crear
trabajo nuevo aunque la Connection esté disabled, mientras una key nueva exige
Connection activa. Checkpoint y finalize rechazan `NULL` y negativos de forma
determinista. Los errores RPC del repository se normalizan sin detalles DB.

La matriz actualizada cubre catálogo de error, summary prohibido, Connection
disabled, counters, metadata completa de `succeeded`/`partial`/`failed`, reuse
sin duplicado y upsert real cross-run. `supabase db reset`: PASS. Matriz local:
22/22 PASS, `ROLLBACK` y fixtures persistidas 0. Tests focalizados iniciales:
36/36 PASS. Stale running continúa bloqueando fail-closed y su recuperación
administrativa permanece futura; checkpoint continúa sin ser resumability.

Validación final: tests focalizados 36/36 PASS, suite completa 167/167 PASS,
typecheck PASS, lint PASS sin warnings y `git diff --check` PASS con avisos
LF→CRLF informativos. Build no ejecutado por mandato. No hubo DB remota,
Mercado Libre real, OAuth, refresh, reconnect, staging, commit ni push.

Resultado: `2_20T_LOCAL_HARDENING_PASS`. Recomendación:
`READY_FOR_REMOTE_2_20T_VALIDATION`.

## Continuación aprobada — validación remota controlada

Se autorizó aplicar exclusivamente la migration
`20260825220634_listing_sync_runs.sql` al proyecto remoto dedicado y validar
schema, seguridad y RPCs con fixtures sintéticas dentro de `BEGIN`/`ROLLBACK`.
Quedan prohibidos listing sync real, llamadas a Mercado Libre, OAuth, refresh,
reconnect, lectura de credenciales, reparación automática de migration history
y cierre Git.

## Resultado remoto controlado

El precheck confirmó history alineado hasta `20260825160000`, ausencia remota
de 2.20T y un dry-run que proponía exclusivamente
`20260825220634_listing_sync_runs.sql`. La migration se aplicó con el mecanismo
canónico y el history posterior quedó alineado.

La inspección remota confirmó 21 campos, constraints de kind/status/counters y
errores, FKs tenant/actor, idempotency unique, single-running parcial, estado
terminal, RLS sin policies y grants exclusivos de `service_role`. Las tres RPC
son `SECURITY DEFINER`, fijan `search_path=pg_catalog` y deniegan ejecución a
PUBLIC, `anon` y `authenticated`.

La matriz sintética remota pasó 22/22 dentro de `BEGIN`/`ROLLBACK`: start y
audit atómico, reuse, already-running, Connection disabled, checkpoints,
counters inválidos, tres finales, protección terminal, aislamiento tenant,
error allowlist y upsert cross-run. Persistencia sintética final: runs 0,
audits 0, Stores 0, Connections 0 y Listings 0.

Los datos reales quedaron sin cambios: Stores 1, Connections 1, Listings 1,
audit events 3, `integration_secrets` 1, `credential_version` 2 y lease libre.
No se leyó ni modificó material de credenciales. No hubo Mercado Libre real,
listing sync, OAuth, refresh, reconnect ni cierre Git. Stale running recovery
sigue `DEFERRED` y checkpoint no es resumability.

Resultado: `REMOTE_2_20T_VALIDATION_PASS`. Recomendación:
`READY_FOR_REAL_2_20T_ORCHESTRATOR_VALIDATION`.

## Continuación aprobada — primera validación real del orquestador

Se autorizó una única ejecución server-only de
`MercadoLibreListingSyncRunService` sobre la Connection real ya activa. El run
puede efectuar únicamente discovery y detail read-only mediante el backfill
2.20S, persistir checkpoints/upsert local y finalizar su audit. Se prohíben una
segunda ejecución, replays reales, fallos provocados, writes hacia Mercado
Libre, OAuth, reconnect, migrations, cambios de schema y cierre Git. Refresh
sólo queda permitido una vez si el access token estuviera vencido.

## Resultado de la primera validación real del orquestador

El precheck read-only confirmó una Connection Mercado Libre activa con binding
Store/Organization válido, un actor Owner, `integration_secrets` 1,
`credential_version` 2, lease libre, Listings 1, duplicados 0 y runs previos 0.
El access token estaba vencido, pero la única invocación autorizada se detuvo en
`run_start_failed`, antes de resolver credenciales, refresh o llamar a Mercado
Libre. No se reintentó.

El postcheck confirmó fail-closed: `listing_sync_runs` 0, runs running 0,
audits started/terminales 0, Listings 1, duplicados 0, credential version 2 y
lease libre. Los timestamps del provider de la Listing existente permanecen
presentes. El schema REST expone la tabla y las tres RPC; la causa interna no
quedó disponible porque el boundary runtime normaliza el fallo de start. No
hubo refresh, discovery, multiget, upsert, Mercado Libre writes, OAuth,
reconnect, migration ni cierre Git.

Resultado: `REAL_2_20T_ORCHESTRATOR_FAILED`. Recomendación: `NEEDS_FIX`.

## Continuación aprobada — diagnóstico seguro de start

Se autorizó auditar y endurecer únicamente el boundary
repository/orquestador que colapsó el primer fallo real a `run_start_failed`.
La observabilidad debe distinguir error/rechazo RPC, respuesta inválida y fallo
de mapping mediante códigos y metadata allowlisted, sin conservar mensajes DB,
SQL, IDs, headers ni secretos. Sólo se permiten prechecks remotos read-only;
quedan prohibidos un nuevo start, refresh, Mercado Libre, migrations y cierre
Git.

## Resultado del diagnóstico seguro de start

El error se colapsaba en dos puntos: `ListingSyncRunRepository.start()` enviaba
error RPC, respuesta nula/malformada y mapping inválido al mismo
`PersistenceError`; luego el orquestador convertía cualquier excepción en
`run_start_failed`. El repository ahora distingue de forma server-only
`RUN_START_RPC_FAILED`, `RUN_START_REJECTED`, `RUN_START_RESPONSE_INVALID` y
`RUN_START_MAPPING_FAILED`. La metadata queda limitada a stage, presencia de
data/error, un código RPC allowlisted y outcome canónico; no conserva mensaje
Postgres, SQL, IDs, headers ni secretos. El orquestador preserva esa distinción
segura dentro de su error normalizado.

Los prechecks remotos read-only confirmaron que runtime y CLI apuntan al mismo
proyecto, actor membership válido en la misma Organization, Connection/Store
binding activo, firma SQL y payload TypeScript idénticos, OpenAPI alineada,
grants de `service_role` y cero runs. No existe schema drift ni se requiere
migration.

La causa histórica exacta no puede recuperarse retroactivamente: la ausencia de
run/audit descarta un outcome o mapping posterior a commit y acota el intento a
error/rechazo RPC previo al insert, pero el código seguro original no fue
registrado. No se ejecutó otro start ni hubo writes remotos, refresh o Mercado
Libre.

Tests focalizados: 43/43 PASS. Suite completa: 174/174 PASS. Typecheck y lint:
PASS. Resultado diagnóstico: `ROOT_CAUSE_UNKNOWN` hasta una ejecución futura
explícitamente autorizada que use la nueva observabilidad.

## Continuación aprobada — retry real único

Se autorizó una sola nueva invocación real del orquestador con una nueva
idempotency key generada server-side y la taxonomía segura de start. No se
permite retry automático. Si start pasa y el token continúa vencido, puede
ejecutarse exclusivamente un refresh mediante el mecanismo vigente; después,
el backfill sólo puede leer Mercado Libre. Continúan prohibidos OAuth,
reconnect, migrations, recovery manual y cierre Git.

## Resultado del retry real único

El precheck confirmó Connection/Store/Organization y actor válidos, runs 0,
Listings 1, `integration_secrets` 1, `credential_version` 2, lease libre y
access token vencido. Se ejecutó exactamente una nueva invocación con una key
generada en memoria y no se reintentó.

El intento volvió a detenerse antes de crear el run. La nueva salida segura fue
`run_start_failed` con `safeStartCode`, stage y metadata RPC nulos. Esto
identifica un gap concreto: `client.rpc()` lanzó antes de devolver el contrato
`{data,error}` y ese throw no está capturado por el repository, por lo que el
orquestador cayó en su rama genérica. No se obtuvo ni conservó el error crudo.
Hace falta clasificar esa excepción como `RUN_START_RPC_FAILED` antes de otro
intento; la causa de transporte subyacente permanece desconocida.

El postcheck confirmó fail-closed: runs 0, running 0, audits nuevos 0, Listings
1, duplicados 0, credential version 2 y lease libre. No hubo refresh, provider
calls, Mercado Libre writes, OAuth, reconnect, migration, recovery ni cierre
Git.

Resultado: `RUN_START_DIAGNOSED_FAILURE`. Recomendación:
`ROOT_CAUSE_IDENTIFIED_NEEDS_FIX`.

## Continuación aprobada — captura de throw RPC

Se autorizó corregir localmente sólo el boundary de los RPC de sync runs para
clasificar una excepción pre-response de `client.rpc()` como
`RUN_START_RPC_FAILED`, con stage, flag de throw y clase de transporte segura.
Checkpoint y finalize deben aplicar la misma protección fail-closed. No se
autorizaron run real, refresh, Mercado Libre, remote writes, migrations ni
cierre Git.

## Resultado de la captura de throw RPC

El boundary de `ListingSyncRunRepository` ahora captura excepciones lanzadas
por `client.rpc()` antes de recibir `{ data, error }`. En start las normaliza
como `RUN_START_RPC_FAILED`, con `stage=rpc_call`, `thrown=true` y una clase de
transporte allowlisted (`network`, `timeout`, `fetch`, `client` o `unknown`).
No conserva el objeto lanzado, mensajes, URLs, headers, IDs ni secretos. Los
errores RPC devueltos conservan el camino separado `stage=rpc_response`.

Checkpoint y finalize aplican la misma protección fail-closed y exponen sólo
mensajes genéricos de persistencia. Los tests cubren throws con material
sensible simulado y verifican que ni el error ni la metadata normalizada lo
filtren.

La auditoría estática confirmó que este repositorio usa el mismo singleton
server-only de Supabase, las mismas variables de entorno y la misma forma
`await client.rpc(...)` que los repositorios con RPC funcionales. No se halló
una diferencia material de cliente o factory. Tampoco existe un RPC versionado
estrictamente read-only que permita aislar el transporte sin mutación. Por lo
tanto, la causa subyacente del throw permanece `UNKNOWN`; identificarla exige
una futura ejecución diagnóstica explícitamente autorizada con la nueva
captura.

Tests focalizados: 46/46 PASS. Suite completa: 177/177 PASS. Typecheck y lint:
PASS. No hubo ejecución real, acceso a DB remota, Mercado Libre, refresh,
OAuth, reconnect, migration ni cierre Git.

Resultado: `RPC_THROW_BOUNDARY_HARDENED`. Recomendación:
`READY_FOR_ONE_DIAGNOSTIC_REAL_START` bajo autorización explícita separada.

## Continuación aprobada — un start diagnóstico real

Se autorizó una única invocación real y aislada de
`ListingSyncRunRepository.start()`, sin orquestador, credenciales, refresh,
provider, backfill ni checkpoints. La ejecución quedó condicionada a que el
contrato vigente pudiera cerrar inmediatamente un start exitoso como `failed`
mediante una semántica canónica de abort diagnóstico/manual, para no dejar un
run `running` stale.

## Resultado del gate de start diagnóstico

El precheck remoto read-only confirmó Connection activa, actor membership
válida, binding Organization/Store correcto, cero runs `running` y una Listing
real ligada a la misma Connection.

El contrato vigente no permite representar un abort diagnóstico/manual. El
catálogo de `error_code` sólo contiene fallos reales de provider, respuesta,
credenciales, persistencia o ítems parciales. Usar uno de ellos para una
detención intencional registraría un hecho falso. La recuperación
administrativa de runs stale también permanece diferida y no se autorizó
cleanup SQL manual.

Por este motivo no se invocó `start_listing_sync_run`: hacerlo podía crear un
run `running` sin una transición terminal semánticamente válida. No hubo runs
ni audits nuevos, escrituras remotas, acceso a credenciales, refresh, Mercado
Libre, OAuth, reconnect ni cierre Git.

Resultado: `DIAGNOSTIC_START_WOULD_CREATE_STALE_RUN`. Recomendación:
`NEEDS_DIAGNOSTIC_CLEANUP_DESIGN`.

## Continuación aprobada — validación real completa del orquestador

Se autorizó una única ejecución real completa del orquestador 2.20T mediante
su caso de uso normal: start, credencial segura, backfill Mercado Libre
read-only, checkpoints y finalize. No hay retry automático. Si el access token
estuviera vencido, se permite un único refresh mediante lease, versionado y CAS
vigentes. Siguen prohibidos OAuth, reconnect, writes hacia Mercado Libre,
migrations, cleanup SQL manual y cierre Git.

## Resultado de la validación real completa del orquestador

El precheck remoto read-only confirmó Connection activa, actor membership
válida, binding Organization/Store correcto, cero runs `running`, Listings 1,
`integration_secrets` 1, `credential_version` 2 y lease libre. El access token
no estaba vigente, por lo que un único refresh habría quedado permitido sólo
después de un start exitoso.

La única invocación nueva terminó en `run_start_failed`, con
`startFailureCode` y diagnóstico nulos. No se ejecutó retry. El postcheck
confirmó runs 0, audits de sync 0, Listings 1, duplicados 0,
`credential_version` 2 y lease libre; no hubo acceso a credenciales, refresh ni
llamadas al provider.

La causa raíz quedó confirmada por el código local: el orquestador asigna el
objeto completo validado por `runInputSchema` a `scope`. En runtime ese objeto
también contiene `actorMembershipId` e `idempotencyKey`; al entrar en
`ListingSyncRunRepository.start()`, `listingScopeSchema.strict()` rechaza esas
propiedades adicionales antes del `try` que envuelve `client.rpc()`. Por eso la
RPC nunca se invocó, no existe metadata RPC y los intentos históricos también
terminaron pre-insert.

No se corrigió código ni se ejecutó otra invocación. No hubo writes remotos,
Mercado Libre, OAuth, reconnect, migration, cleanup manual ni cierre Git.

Resultado: `RUN_START_ROOT_CAUSE_IDENTIFIED`. Recomendación:
`ROOT_CAUSE_IDENTIFIED_NEEDS_FIX`.

## Continuación aprobada — fix de composición de ListingScope

Se autorizó corregir localmente sólo la fuga de metadata del orquestador hacia
el `ListingScope`, agregar su regresión directa y comprobar que la validación
strict continúa vigente. Se prohibieron run real, DB remota, Mercado Libre,
refresh, OAuth, reconnect, migrations y cierre Git.

## Resultado del fix de ListingScope

El orquestador ahora construye explícitamente `scope` sólo con
`organizationId`, `storeId` y `connectionId`. `actorMembershipId` e
`idempotencyKey` permanecen separados y llegan a los parámetros ya existentes
de `ListingSyncRunRepository.start()`.

La regresión verifica el payload exacto recibido por `start`, incluido que la
metadata no aparezca dentro de `scope`. Un test directo adicional confirma que
una propiedad arbitraria dentro del verdadero ListingScope sigue siendo
rechazada antes de RPC; `listingScopeSchema.strict()` no fue debilitado. La
taxonomía segura de errores RPC permanece sin cambios.

Tests focalizados: 28/28 PASS. Suite completa: 178/178 PASS. Typecheck y lint:
PASS. No hubo migration, DB remota, run real, Mercado Libre, refresh, OAuth,
reconnect ni cierre Git.

Resultado: `INPUT_SCOPE_ROOT_CAUSE_FIXED`. Recomendación:
`READY_FOR_ONE_FULL_REAL_ORCHESTRATOR_RETRY` bajo autorización explícita
separada.

## Continuación aprobada — retry real completo posterior al fix

Se autorizó una única ejecución real completa del orquestador con una nueva
idempotency key generada server-side. El flujo puede realizar un solo refresh
canónico si el access token está vencido y continuar con discovery/detail
Mercado Libre read-only, persistencia local, checkpoints y finalize. No hay
segundo retry; siguen prohibidos OAuth, reconnect, writes al provider,
migrations, cleanup manual y cierre Git.

## Resultado del retry real completo posterior al fix

El precheck read-only confirmó Connection activa, actor membership y binding
Organization/Store válidos, cero runs `running`, Listings 1,
`integration_secrets` 1, `credential_version` 2 y lease libre. El access token
estaba vencido.

Se ejecutó exactamente una invocación con una nueva idempotency key no
registrada. Start devolvió `started`: creó un run tenant-bound y el audit
`listing.sync.started`. El flujo alcanzó la resolución de credenciales y
terminó canónicamente como `failed` con `credential_failure`; el refresh no se
completó, `credential_version` permaneció en 2 y el lease terminó libre. El
contrato persistido no conserva una etapa más granular del fallo de credencial,
por lo que no se infiere una causa cruda.

No comenzó discovery ni hubo llamadas de listings al provider. Los contadores
finales quedaron en cero, `completed_at` y `last_checkpoint_at` presentes, y se
creó exactamente un audit `listing.sync.failed`. El postcheck confirmó cero
runs `running`, Listings estable en 1, duplicados 0, bindings de Store y
Connection válidos y timestamps de provider presentes.

No hubo writes hacia Mercado Libre, OAuth, reconnect, segundo retry, migration,
cleanup manual ni cierre Git.

Resultado: `REAL_2_20T_ORCHESTRATOR_FAILED`. Recomendación: `NEEDS_FIX` para
diagnosticar localmente la pérdida de etapa segura del fallo de credencial
antes de autorizar otra operación real.

## Continuación aprobada — observabilidad de credential stage

Se autorizó preservar localmente el stage seguro de
`MercadoLibreCredentialError` hasta el resumen controlado del run, sin cambiar
refresh, lease, CAS, rotación, provider client ni retry policy. Se prohibieron
nuevas operaciones reales, DB remota, OAuth, reconnect, migrations y cierre
Git.

## Resultado de observabilidad de credential stage

El stage ya existía en `MercadoLibreCredentialError`; se perdía exclusivamente
en `classifyRunFailure()`, que reducía cualquier error de credenciales al mismo
summary genérico. El orquestador ahora conserva `error_code=credential_failure`
y selecciona una plantilla controlada sólo para los stages primarios que el
código emite: `READ`, `DECRYPT`, `CLAIM`, `DOUBLE_CHECK`, `PROVIDER_REQUEST`,
`PROVIDER_RESPONSE`, `ENCRYPT` y `CAS_COMPLETE`. Cualquier otro valor usa el
summary genérico.

Los tests verifican status `failed`, stage end-to-end, fallback y ausencia de
mensajes crudos, access/refresh tokens y headers en el payload de finalize. No
se modificaron `MercadoLibreCredentialService`, lease, CAS, request de refresh,
rotación ni cleanup.

El fallo real anterior queda `NARROWED` al conjunto de stages primarios de
`MercadoLibreCredentialError`, pero no puede asignarse a uno concreto porque el
run histórico persistió únicamente el summary genérico. No se infiere una
etapa sin evidencia.

Tests focalizados: 47/47 PASS. Suite completa: 187/187 PASS. Typecheck y lint:
PASS. No hubo migration, remote writes, operación real, Mercado Libre, refresh,
OAuth, reconnect ni cierre Git.

Resultado: `CREDENTIAL_STAGE_OBSERVABILITY_READY`. Recomendación:
`READY_FOR_ONE_FINAL_REAL_2_20T_RETRY` bajo autorización explícita separada.

## Resultado del retry real final del orquestador

El precheck read-only confirmó Connection activa, bindings de Organization,
Store y actor válidos, cero runs `running`, Listings 1,
`integration_secrets` 1, `credential_version` 2 y lease libre. El access token
estaba vencido.

Se ejecutó exactamente una invocación nueva. Start devolvió `started`, creó un
run tenant-bound y un único audit `listing.sync.started`. El refresh se intentó
una vez y falló de forma segura en el stage allowlisted `CAS_COMPLETE`; el run
terminó `failed` con `error_code=credential_failure`, `completed_at` y
`last_checkpoint_at` presentes, y un único audit `listing.sync.failed`.
`credential_version` permaneció en 2 y el lease terminó libre.

No comenzó discovery ni se persistieron checkpoints de progreso: los counters
finales quedaron en cero. Listings permaneció en 1, sin duplicados, con
bindings y timestamps de provider intactos. No quedó ningún run `running`.
No hubo segundo retry, segundo refresh, OAuth, reconnect, writes de negocio
hacia Mercado Libre, migration, cleanup manual ni cierre Git.

Resultado: `REAL_2_20T_CREDENTIAL_FAILURE_DIAGNOSED`. Recomendación:
`CREDENTIAL_FIX_REQUIRED` para diagnosticar y corregir localmente el fallo en
`CAS_COMPLETE` antes de autorizar otra operación real.

## Diagnóstico local de `CAS_COMPLETE`

El path completo fue auditado sin operaciones remotas. El payload TypeScript
y la firma SQL coinciden; `CLAIM` y `DOUBLE_CHECK` conservan la versión, y el
CAS valida Organization, Connection, versión, lease y expiración antes de
reemplazar ambas credenciales, incrementar la versión una vez y liberar el
lease.

Tras un reset local limpio, el repository real sobre PostgREST completó el
escenario sintético `7 → 8`. La matriz A–J también pasó: success, mismatch de
versión, mismatch/ausencia de lease, mismatch de tenant, reemplazo atómico,
expiry, repetición fail-closed, cleanup y retorno sin secretos. El fallo real
no se reprodujo.

El run real conserva el stage seguro `CAS_COMPLETE`, pero no permite distinguir
retrospectivamente entre fallo de RPC y rechazo condicionado del CAS. Tampoco
existe evidencia preservada de versión/lease en el instante anterior al
complete. Por ello la root cause exacta permanece `UNKNOWN`; no se aplicó un
fix productivo ni una migration especulativa. La relación causal con 2.20K
también permanece `UNKNOWN`. La credencial real continúa
`POTENTIALLY_STALE` y no se intentó refresh, OAuth ni reconnect.

Resultado: `ROOT_CAUSE_UNKNOWN`.

## Continuación aprobada — observabilidad segura de `CAS_COMPLETE`

Se autorizó mejorar localmente y mediante diff mínimo la clasificación segura
de fallos de `CAS_COMPLETE`, sin cambiar refresh, CAS, lease, versionado,
provider, cifrado ni concurrencia. El alcance prohíbe operaciones reales,
writes remotos, OAuth, reconnect, migrations especulativas y cierre Git.

### Resultado

El repository distingue ahora `CAS_RPC_THROW`, `CAS_RPC_ERROR` y
`CAS_RESPONSE_INVALID`; el `false` booleano vigente se conserva como
`CAS_REJECTED`. El stage principal sigue siendo `CAS_COMPLETE` y
`listing_sync_runs.error_code` sigue siendo `credential_failure`; sólo el
summary controlado incorpora la clasificación secundaria allowlisted. No
existe mapping posterior a una respuesta booleana válida, por lo que no se
inventó una categoría de mapping.

No cambiaron refresh, CAS, lease, versionado, cifrado, concurrencia ni SQL.
Tests focalizados: 45/45 PASS. Suite completa: 194/194 PASS. Typecheck y lint:
PASS. No hubo remoto, Mercado Libre, refresh real, OAuth, reconnect ni cierre
Git. La credencial real continúa `POTENTIALLY_STALE`.

Resultado: `CAS_OBSERVABILITY_READY`.

## Continuación aprobada — recuperación mediante reconnect controlado

La credencial real se clasifica `POTENTIALLY_STALE` porque el refresh anterior
alcanzó la respuesta del provider antes de fallar en `CAS_COMPLETE`. Se
autorizó una única operación OAuth real de reconnect, target-bound a la
Connection existente, sin listing sync en este mandato.

La operación no se inició: el servidor local y el tunnel configurado estaban
inactivos, y el hostname de la Redirect URI vigente no resolvía. No se creó
pending ni se ejecutó OAuth, reconnect, refresh o listing sync. La credencial
permanece `POTENTIALLY_STALE`.

## Continuación aprobada — reconnect real controlado

El callback quedó operativo en el hostname temporal
`sufficiently-bathrooms-iso-unions.trycloudflare.com`. La credencial continúa
`POTENTIALLY_STALE` y se autorizó un único reconnect real target-bound. Listing
sync permanece fuera de alcance.

### Resultado

El único OAuth autorizado alcanzó `READY_FOR_RECONNECT` para `E.A.ZOCOOL`. La
pending target-bound pasó los gates de tenant, Store, provider e identidad; la
finalización canónica devolvió `reconnected`, reutilizó la Store y Connection
existentes y avanzó `credential_version` de 2 a 3. La pending quedó consumida,
el lease quedó `CLEAR` y se agregó exactamente un audit
`integration.reconnected`. Stores, Connections, Secrets y Listings permanecen
en 1, sin duplicados ni runs activos. No se ejecutó listing sync, refresh
adicional ni write de negocio al provider.

Resultado: `REAL_RECONNECT_RECOVERY_SUCCESS`.

## Continuación aprobada — validación final real del orquestador

La recuperación por reconnect terminó `SUCCESS`, con `credential_version = 3`
y credencial `CONSISTENT_FRESH`. Se autorizó una única ejecución real final del
orquestador 2.20T, read-only hacia Mercado Libre y sin OAuth ni reconnect.

### Resultado

La única invocación inició un run y completó discovery, detail fetch,
persistencia y checkpoint con contadores `1/1/1/1/0`, una página y un batch.
La finalización lanzó `run_finalize_failed`; el run quedó `running`, con
`last_checkpoint_at` presente, sin `completed_at` ni audit terminal. El audit
`listing.sync.started` existe exactamente una vez. Listings permanece en 1 sin
duplicados, `credential_version` permanece 3 y el lease está `CLEAR`. No se
ejecutó retry, refresh, OAuth, reconnect, cleanup ni write de negocio al
provider.

Resultado: `STALE_RUNNING_RUN_CREATED`.

## Continuación aprobada — diagnóstico de finalize y plan de recuperación

El sync real completó discovery, detail fetch, persistencia y checkpoint, pero
finalize devolvió `run_finalize_failed` y dejó el run real en `running`. No se
autorizó cleanup ni retry; el objetivo es diagnosticar el path de finalize y
diseñar una recuperación segura sin volver a consultar Mercado Libre.

### Resultado

Root cause `CALLER`: después del backfill, el orquestador pasaba como
`progress` el resultado completo, incluida la propiedad `failures`; el schema
estricto del repository rechazaba ese shape antes de invocar el RPC. El fix
mínimo copia únicamente los siete counters. La firma y definición SQL remotas
coinciden con local y el historial de migrations está alineado.

La regresión específica falló antes del fix y pasó después. Repository path
sintético: PASS; matriz SQL: 22/22; focalizados: 41/41; suite: 194/194;
typecheck y lint: PASS. El stale run remoto no fue modificado. Sus bindings,
counters `1/1/1/1/0`, checkpoint y audit started son válidos; no existe audit
terminal. Clasificación: `SAFE_TO_FINALIZE_EXISTING_RUN`, mediante finalize-only
en una autorización posterior, sin repetir sync ni llamadas a Mercado Libre.

## Continuación aprobada — recuperación remota del stale run existente

La root cause `CALLER` y el fix local quedaron validados; el run se clasificó
`SAFE_TO_FINALIZE_EXISTING_RUN`. Se autorizó una única finalización remota del
run existente como `succeeded`, usando sus counters persistidos y sin crear un
nuevo run, repetir listing sync ni llamar a Mercado Libre.

### Resultado

La única finalización remota autorizada reutilizó el mismo run y devolvió
`succeeded`. `completed_at` y `last_checkpoint_at` quedaron presentes; los
counters permanecieron `1/1/1/1/0`, con una página y un batch, y los errores
quedaron en `NULL`. El run conserva exactamente un audit
`listing.sync.started` y recibió exactamente un `listing.sync.succeeded`, sin
terminales duplicados. Quedaron 0 runs activos; Stores, Connections, Listings
y Secrets permanecen en 1, `credential_version = 3` y lease `CLEAR`. No hubo
nuevo sync, run, checkpoint, provider call, refresh, OAuth ni reconnect.

Resultado: `STALE_RUN_RECOVERY_SUCCESS`.

## Cierre formal autorizado

El run final quedó `succeeded`, con counters `1/1/1/1/0`, una página y un
batch. Los audits terminales pasaron con exactamente un
`listing.sync.started` y un `listing.sync.succeeded`, sin duplicados ni runs
activos. 2.20T quedó listo para cierre y se autorizó auditar, clasificar,
stagear por rutas explícitas, crear commits coherentes y hacer push de
`develop`.
