# Fase 2 — 2.20R Fresh Real Reconnect OAuth

## Mandato aprobado

Base `2ea6738d1e2e1f3431be1a9771e45a647622c5e5`. Se autoriza una sola
reautorización OAuth real para validar end-to-end el reconnect target-bound
cerrado en 2.20P/2.20Q. La pending legacy está expirada y no se reutiliza.

Antes de iniciar OAuth deben coincidir el hostname público vigente,
`allowedDevOrigins`, la redirect URI server-only y la URI registrada en Mercado
Libre Developers. Clerk debe ser visible y la sesión del dashboard válida. La
configuración se valida sólo por presencia y forma, sin mostrar secretos.

El precheck remoto esperado es Store 1, Connection activa 1, secreto 1,
Listings 0, `credential_version=1`, lease libre y pending legacy expirada. El
flujo debe crear exactamente un attempt con `purpose=reconnect`, realizar un
único token exchange y un único `GET /users/me`, y dejar una pending nueva
activa con `target_connection_id` apuntando a la Connection existente.

Sólo después de validar ese binding se finaliza exactamente una vez mediante la
primitive canónica. El resultado esperado es `reconnected`, misma Store y
Connection, secreto único reemplazado, versión 1→2, lease libre, Connection
activa, auditoría `integration.reconnected` y pending consumida. Después se
permite un único `GET /users/me` read-only con la credencial persistida, sin
refresh si sigue vigente.

Quedan prohibidos un segundo OAuth, connect normal, reutilizar o modificar la
pending legacy, listing sync, seller listings, item multiget, cambios
funcionales, Git closure y documentar state, authorization code, tokens,
ciphertexts o callbacks con parámetros. Si el hostname cambió o la Redirect URI
requiere intervención en Mercado Libre Developers, se ajusta sólo la
configuración mínima local y se detiene con `NEEDS_USER_ACTION`.

## Estado inicial confirmado

- 2.20P y 2.20Q: cerradas en Git.
- Local: tests 140/140, typecheck y matriz SQL 13/13 PASS.
- Remoto: migrations aplicadas, matriz SQL 8/8 y seguridad PASS.
- Datos reales: sin cambios durante el cierre técnico.
- Siguiente gate: precheck de entorno, túnel, Redirect URI, Clerk y remoto antes
  de iniciar una única autorización real.

## Precheck — STOP por entorno público inactivo

`HEAD`, `origin/develop` y la base aprobada coinciden. Bun y la configuración
server-only requerida están presentes; la master key tiene 32 bytes válidos y
PKCE permanece deshabilitado. El hostname de `MERCADO_LIBRE_REDIRECT_URI`
coincide con `allowedDevOrigins`.

El gate operativo se detuvo antes de Clerk y del precheck remoto porque no hay
servidor escuchando en `localhost:3000`, no existe proceso `cloudflared` activo
y el callback del hostname configurado no responde. No se inició OAuth, no se
creó attempt, no se llamó a Mercado Libre y no hubo escrituras remotas. Para
continuar se debe levantar la aplicación y un túnel vigente; si cambia el
hostname, deben alinearse `allowedDevOrigins`, la configuración local y la
Redirect URI registrada en Mercado Libre Developers antes de reanudar.

## Continuación — STOP antes de finalización real

El usuario confirmó que una única autorización nueva terminó en
`READY_FOR_RECONNECT` para E.A.ZOCOOL. El precheck remoto identificó exactamente
una pending reconnect nueva con `target_connection_id` presente y no consumida.
El target resuelve la Connection existente y pasan Organization, Store,
membership, provider, identidad externa y estado permitido.

La pending target-bound ya está expirada, por lo que no cumple el gate de
actividad. Se detuvo el flujo antes de invocar la RPC: no hubo finalización,
rotación de credenciales, auditoría nueva ni `GET /users/me`. El estado real
permanece Store 1, Connection activa 1, secreto 1, Listings 0,
`credential_version=1` y lease libre. No se inició otro OAuth ni se reutilizó la
pending legacy.

## Fast path — acción de navegador requerida

El precheck rápido pasó con servidor y túnel activos, hostname público compatible
con `allowedDevOrigins`, Store 1, Connection 1, secreto 1, Listings 0,
`credential_version=1` y lease libre. Codex no tiene un navegador conectado y
no puede usar la sesión Clerk existente sin intervención del usuario. No se
inició OAuth por otro medio ni se creó una pending.

La continuación queda a la espera de una única solicitud manual
`purpose=reconnect` desde el dashboard autenticado. Tras confirmar
`READY_FOR_RECONNECT`, el agente debe retomar inmediatamente en el precheck de
la pending nueva y, si el binding pasa, finalizar una sola vez.

## Cierre — reconnect real exitoso

La única autorización nueva aprobada terminó en `READY_FOR_RECONNECT` para
E.A.ZOCOOL. Antes de finalizar, la pending nueva estaba activa, no consumida y
no expirada; tenía `target_connection_id` y pasó los gates de Connection,
Organization, Store, provider e identidad externa.

La primitive canónica se ejecutó exactamente una vez y devolvió
`outcome=reconnected`, reutilizando la misma Store y la misma Connection. El
postcheck remoto confirmó Store 1, Connection activa 1, secreto 1,
`credential_version=2`, lease libre, auditoría `integration.reconnected`,
pending consumida y Listings 0. No se crearon duplicados.

Se hizo exactamente un `GET /users/me` read-only con la credencial persistida y
vigente: PASS, identidad coincidente y `displayName=E.A.ZOCOOL`. No hubo refresh,
OAuth adicional, listing sync, migration ni cierre Git. Resultado:
`REAL_RECONNECT_SUCCESS`; recomendación:
`READY_TO_CLOSE_2.20R_AND_RESUME_2.20E`.
