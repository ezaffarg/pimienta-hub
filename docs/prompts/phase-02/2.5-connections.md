# Subfase 2.5 — Connections sin OAuth

Status: **COMPLETED**

Purpose:
Crear modelo, migración y repositories server-only para Connections sin almacenar secretos ni implementar OAuth.

Secondary Purpose:
Normalizar memoria Obsidian del proyecto a UTF-8/español cuando sea seguro.

Context:
- Store Scope ya implementado.
- Store puede tener múltiples Connections.
- Provider es unión controlada por código.
- OAuth pertenece a una fase posterior.
- DB todavía no está ejecutada.

Restrictions:
- no tokens;
- no OAuth;
- no refresh;
- no llamadas Mercado Libre;
- no RLS;
- no db push;
- no link remoto;
- no Fase 3.

Result:
Se creó la migración `20260822202032_phase_2_connections.sql`, ConnectionRepository tenant-scoped y tests contractuales. La Connection usa provider canónico, FK compuesta Store/Organization, estados `active|disabled`, metadata sin secretos y unicidad global solo para cuentas activas. Obsidian se actualizó en español UTF-8 sin renombrar IDs internos. DB no ejecutada; OAuth pendiente. Validación: 55 tests, typecheck, lint, build y `git diff --check` correctos.

Commit:
PENDING

## Prompt

Fase 2.5 autorizada sobre `e14af7e`: crear una migración nueva y aditiva que agregue solamente `connections`, su modelo/repository server-only y tests sin DB real. Connection vincula Store 1:N con Provider controlado; no hay OAuth, tokens, refresh, API Mercado Libre, sincronización, RLS, StoreIntegrationResolver, Docker, Supabase remoto ni Fase 3.

El modelo requiere `id` UUID, `organization_id`, `store_id`, `provider`, `external_account_id` nullable, `status`, `scopes text[]`, `expires_at` nullable y timestamps. Debe usar FK compuesta `(store_id, organization_id) → stores(id, organization_id)` con `ON DELETE RESTRICT`; provider debe reutilizar el `IntegrationProvider` exacto; revisar estados documentados antes de imponer CHECK/índice parcial. La unicidad global `provider + external_account_id` solo se aplica para estados que realmente reservan la cuenta; no debilitarla por Organization ni inventar semántica.

ConnectionRepository debe ser server-only y tenant-scoped: list por Store, get por Connection dentro de Organization, create con Organization trusted separada del payload, y cualquier primitive necesaria para invariantes. Verificar Store dentro del tenant antes de crear; no exponer lookup global. Reutilizar Store Scope sin crear un nuevo sistema de permisos.

Agregar tests contractuales de migración/modelo, repositories, errores DB y scope Store/Organization; sin DB real ni Faker. Documentar Connections, Store 1:N, provider, ausencia de tokens, repositorios, status/uniqueness real, DB sin ejecutar, OAuth y resolver diferidos. Actualizar Obsidian solo en español UTF-8 si se puede sin tocar IDs/otros proyectos/templates; registrar decisión de idioma español y backlog pendiente.

Ejecutar tests, typecheck, lint, build, diff check/status/diff. No hacer `git add`, commit ni push. Marcar este prompt COMPLETED solo si corresponde y reportar migración, esquema, constraints, estado de Obsidian y exclusiones.

## Result

La migración agrega únicamente `connections`: UUID, Organization, Store, provider controlado, cuenta externa nullable, `active|disabled`, scopes, expiración y timestamps. La FK compuesta usa `ON DELETE RESTRICT`; el índice parcial reserva globalmente `provider + external_account_id` solo para `active`. No contiene tokens, secretos ni OAuth. ConnectionRepository exige Organization en list/get/create y el payload no acepta Organization. Obsidian MCP estuvo disponible; los contenidos operativos se registraron en español UTF-8, mientras sus labels internos multilenguaje no se renombraron por seguridad. 55 tests, typecheck, lint, build y diff check correctos.

## Commit

PENDING

## References

Canonical: [meli-database.md](../../meli-database.md), [meli-architecture.md](../../meli-architecture.md)
