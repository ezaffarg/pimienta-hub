# 2.8B — Bootstrap Owner Concurrency Validation

Status: COMPLETED

Purpose:
Validar limpiamente la estrategia concurrency-safe de Bootstrap First Owner con Organizations nuevas y simultaneidad real.

Context:

- implementación 2.8 ya existe;
- la prueba previa quedó contaminada por fixtures reutilizados;
- no debe rediseñarse mientras la implementación actual sea correcta.

Result:
LOCAL VALIDATED. Dos carreras independientes sobre una misma Organization devolvieron `created` y `already_bootstrapped`, con un Owner final. Dos Organizations distintas crearon su Owner en paralelo. Una membership Employee existente devolvió `membership_exists_non_owner`, sin promoción ni Owner. Un segundo Owner administrativo fue permitido por schema. Reset final aplicó las tres migrations desde cero; remoto no fue tocado.

Commit:
PENDING

---

Continuación de 2.8: validar, sin rediseñar, la RPC `bootstrap_first_owner` mediante reset local limpio, fixtures únicos, dos ejecuciones simultáneas por Organization, segunda corrida, Organizations distintas, Owner existente y membership no-Owner. Confirmar el lock transaccional `pg_advisory_xact_lock(hashtextextended(organization_id, 0))`, que no existe unique Owner y que el esquema permite Owners futuros múltiples. Agregar tests server-side si corresponde, mantener unit tests libres de Docker, documentar resultados y ejecutar reset final. No realizar Git write, Supabase remoto, OAuth, RLS, rate limiting ni Fase 3.

## Continuación de validación

Completar la segunda concurrencia para una Organization nueva, concurrencia entre Organizations distintas, Owner existente, membership Employee existente sin promoción y capacidad futura de varios Owners. Añadir tests server-side deterministas cuando sea razonable, documentar el procedimiento DB sin acoplar `bun run test` a Docker, ejecutar reset final, actualizar documentos/Obsidian y detenerse sin staging, commit ni push. Se prohíben remoto, OAuth, RLS, rate limiting y Fase 3.
