# Cierre Subfase 2.3

Status: **COMPLETED**

Purpose:
Cerrar Subfase 2.3, versionar repositories/roles persistentes y establecer
una política clara para evitar solicitudes de aprobación rutinarias innecesarias.

Context:
- repositories server-only implementados;
- persistent role resolution preparada;
- fallback Clerk sigue transitorio;
- bootstrap Owner diferido por concurrencia;
- Store Scope sigue en 2.4.

Result:
Repositorios y resolver persistente auditados; bootstrap Owner diferido; Store Scope queda en 2.4; validación requerida correcta y Obsidian actualizado.

Commit:
PENDING

## Prompt

# FASE 2 — CIERRE DE SUBFASE 2.3
# REPOSITORIES SERVER-ONLY + ROLES PERSISTENTES
# + POLÍTICA DE APROBACIONES RUTINARIAS
# ARCHIVAR ESTE PROMPT ANTES DE EJECUTAR CUALQUIER ACCIÓN

Proyecto: e-ngenieria Hub
Rama: develop
Commit base: `95d4828 feat(database): add tenant store foundation schema`

Estado actual: Subfase 2.3 completada localmente, sin staging/commit/push; 40 tests, typecheck, lint, build y `git diff --check` correctos; Obsidian disponible y actualizado; bootstrap del primer Owner diferido por requerir decisión/constraint de concurrencia; 2.4 no iniciada.

Objetivo: archivar este prompt; establecer política de aprobaciones rutinarias; auditar cambios de 2.3; validar; actualizar documentación/memoria; commit; push; detenerse. No iniciar 2.4.

### Política de aprobaciones rutinarias

Durante este mandato y futuros mandatos equivalentes dentro del workspace no solicitar confirmación adicional para operaciones rutinarias explícitamente autorizadas: lectura del repositorio/configuración/Git; cambios únicamente dentro del scope; creación de prompts archivados; documentación, tests y memoria Obsidian autorizados; `bun run test`, `typecheck`, `lint`, `build`, `git diff`, `git status`, `git log`, `git branch --show-current`; y `git add` de paths explícitos, commit y `git push origin develop` autorizados.

Siguen requiriendo bloqueo/aprobación: salir del workspace, elevación/admin, configuración global, instalaciones globales/Docker, cambios de `bunfig.toml`, desactivar seguridad, borrados no autorizados, reset/rebase destructivo, force push, main, secretos, login/link/db push de Supabase, migraciones remotas/destructivas, red no prevista, cambios de arquitectura o ampliación de scope. No convertir esto en Full Access.

Documentar en `docs/agent-workflow.md` y, si corresponde, `AGENTS.md`: «Los prompts operativos pueden preautorizar acciones rutinarias dentro del workspace. El agente no debe solicitar confirmación repetida para acciones explícitamente autorizadas por el mandato. Operaciones sensibles, destructivas, globales, remotas o fuera de scope continúan requiriendo aprobación.» Sin duplicación ni debilitamiento de seguridad.

Auditar con `git status --short`, `git diff --stat` y `git diff`. Clasificar como implementación, tests, documentación, Obsidian externo, política de workflow o no relacionado. Los paths esperados son repositorios/tests, resolver/tests, `docs/codex-handoff.md`, `docs/meli-database.md`, `docs/meli-multi-tenancy.md`, `docs/plan-y-gobierno.md`, `docs/agent-workflow.md`, README de prompts y los prompts 2.3. Detenerse ante archivos no justificables.

Confirmar: `HubMembershipRepository` server-only con lookup Organization+Clerk user, check de Owner, create controlado y errores; `StoreRepository` server-only con list/get tenant-scoped, sin get global inseguro y create con tenant; `StoreAssignmentRepository` con Organization y primitives tenant-safe, sin Store Scope. Confirmar `Organization + Clerk user → hub_membership → Hub Role`, roles Owner/Manager/Employee/Client y missing membership deny/null. `hub_memberships` es la fuente definitiva planificada.

El fallback exacto `org:admin → Owner`, `org:member → Employee` permanece únicamente como **TRANSITIONAL FALLBACK**, ya que schema/persistencia no están activos; no ampliar permisos, auto-crear memberships ni eliminarlo. Registrar **BOOTSTRAP FIRST OWNER: DEFERRED**, porque requiere constraint/schema adicional para concurrencia, en plan y Obsidian/open questions. Confirmar que la policy Fase 1 no cambió y Store Scope (Owner/Manager all Stores; Employee/Client assignments) queda en 2.4.

Verificar Obsidian MCP y actualizar resumidamente current-state, decisions y open-questions; sin código, diffs, prompts completos, secretos ni SQL completo; repo source of truth. Si falla, no bloquear commit.

Ejecutar `bun run test`, `bun run typecheck`, `bun run lint`, `bun run build` y `git diff --check`; se esperan 40 tests. No corregir warnings conocidos de Vitest/Vite ESM-CJS, Google Sans Flex, metadataBase ni LF→CRLF.

Actualizar este prompt y `2.3-repositories-roles.md` a `Status: COMPLETED`, con resumen fiel y `Commit: PENDING`, sin alterar el prompt archivado. Hacer staging solo de paths auditados, revisar `git diff --cached`, `--stat` y `--check`, y confirmar que no hay secretos, archivos ajenos, Store Scope, Connections, OAuth, nueva migración/schema ni RLS.

Crear el commit `feat(database): add tenant repositories and persistent roles`; no afirmar DB aplicada, Store Scope completo ni bootstrap Owner terminado. Si la rama es `develop`, ejecutar `git push origin develop`; no main ni force push. Verificar `git log --oneline -8` y `git status --short`, con HEAD y origin/develop alineados y working tree limpio. Actualizar Obsidian post-commit con hash real, 2.3 CLOSED, 2.4 NEXT, migración sin ejecutar y bootstrap pendiente.

No iniciar 2.4, Store Scope, Connections, OAuth, StoreIntegrationResolver, RLS, Docker, Supabase local/remoto, migraciones, cambios de `bunfig.toml`, seguridad, main ni bootstrap Owner sin decisión. Entregar checkpoint completo y detenerse.

## Result

El cierre auditó repositorios server-only tenant-scoped, resolver persistente, policy sin cambios y fallback Clerk transitorio. `BOOTSTRAP FIRST OWNER` continúa **DEFERRED** por requerir una decisión y constraint/schema de concurrencia. Store Scope sigue reservado para 2.4. Obsidian MCP está disponible y actualizado sin secretos ni copias de código/SQL. Validación: 40 tests, typecheck, lint, build y `git diff --check` correctos; persisten warnings conocidos de Vitest/Vite ESM-CJS, Google Sans Flex, metadataBase y LF→CRLF. La comprobación adicional `bun run format:check` falla por 302 incidencias preexistentes fuera del alcance; no se aplicó formateo masivo.

## Commit

PENDING

## References

Canonical: [meli-database.md](../../meli-database.md). Operational index: [README.md](./README.md)
