# Subfase 2.1B — Supabase CLI

Status: **COMPLETED — tooling preparado; commit pendiente**

## Purpose

Preparar tooling reproducible de migraciones sin crear schema funcional.

## Context

- La 2.1 definió schema y estrategia documental.
- `supabase@2.115.0` fue rechazado por `minimumReleaseAge = 7 días`.
- La política de supply-chain security se mantiene intacta.

## Decisions

- Supabase CLI será `devDependency`.
- Versión exacta aprobada: `2.114.0`.
- Bun es el package runner.
- No se desactiva `minimumReleaseAge`.
- Docker queda pendiente.
- No se linkea un proyecto remoto.
- No se crea schema funcional en 2.1B.

## Restrictions

No Store, tablas, migraciones funcionales, `db push`, `supabase start`, OAuth, repositories ni 2.2. No modificar `bunfig.toml`, usar bypass de antigüedad, instalar Docker ni ejecutar `git add`, commit o push.

## Prompt

### Fase 2 — Subfase 2.1B: Supabase CLI reproducible

Proyecto: e-ngenieria Hub; rama `develop`. La 2.1 documental está completada. El intento de instalar `supabase@2.115.0` fue bloqueado correctamente por `minimumReleaseAge = 7 días`; no desactivar esa política, no usar `--minimum-release-age 0` ni agregar exclusiones. No iniciar 2.2.

0. Antes de toda instalación, comando de Supabase o cambio de tooling, archivar este mandato en `docs/prompts/phase-02/2.1b-supabase-cli.md` con Status ACTIVE, Purpose, Context, Decisions, Restrictions, Prompt completo, Result PENDING y Commit PENDING.
1. Instalar exactamente `supabase@2.114.0` con `bun add -D supabase@2.114.0 --exact`; nunca `latest`, `^` ni `~`. No modificar `bunfig.toml`. Si esta versión también es rechazada por la política, detenerse, registrar el bloqueo y no usar bypass.
2. Si instala, validar `bunx supabase --version` (esperado `2.114.0`) y `bunx supabase --help`, comprobando que funciona como dependencia de proyecto sin instalación global.
3. Confirmar que solo cambian `devDependencies`, con versión exacta en `package.json` y lockfile, sin dependencias runtime innecesarias.
4. Si no existe `supabase/`, ejecutar `bunx supabase init`; revisar todos los archivos antes de continuar. No continuar si aparecen archivos inesperados o sensibles.
5. Revisar `supabase/config.toml`: solo configuración local/versionable; no `project_ref` remoto, access tokens, service role keys, passwords, credenciales ni secretos. No linkear remoto.
6. No crear migraciones funcionales ni `hub_memberships`, `stores`, `store_assignments` o `connections`. Si `supabase/migrations/` no existe porque la CLI la crea al generar la primera migración, no crearla artificialmente; documentar el estado.
7. Docker no está disponible: no instalarlo ni ejecutar `bunx supabase start` o `bunx supabase db reset`. El stack local requerirá Docker/runtime compatible; no usar un proyecto remoto como sustituto automático.
8. No ejecutar `supabase login`, `supabase link` ni `supabase db push`; no guardar refs, PATs ni passwords en Git.
9. Revisar `.gitignore` después de init: ignorar solo archivos locales/sensibles según convención oficial; versionar `supabase/config.toml` y futuras migraciones; no tocar reglas ajenas.
10. Evaluar scripts: preferir uso directo mediante Bun y no añadir scripts por costumbre ni ejecutar los que requieran Docker.
11. Actualizar mínimamente documentación de database, plan, handoff e índice de prompts para registrar el rechazo 2.115.0, política preservada, 2.114.0, devDependency, Bun, inicialización, Docker pendiente, ausencia de schema/link remoto y que 2.2 no inició.
12. Al finalizar o bloquearse, sustituir Result PENDING por el checkpoint real y mantener Commit PENDING.
13. Si tooling funciona, ejecutar CLI version/help, tests, typecheck, lint, build, `git diff --check`, status y diff. Si bloquea antes, no fingir validaciones.
14. No crear schema/tablas/migraciones funcionales, repositorios, Store Scope, bootstrap Owner, OAuth, conexiones funcionales, browser client, RLS; no modificar el mapping Clerk, ni hacer operaciones Git de escritura.
15. Entregar checkpoint con archivado previo, rechazo 2.115.0, integridad de bunfig, versión, package/lock, estructura/config/migrations, ignore/scripts/docs, Result, validaciones y confirmaciones de todos los límites; detenerse.

## Result

Supabase CLI `2.114.0` quedó instalada como `devDependency` exacta y validada mediante `bunx supabase --version` y `bunx supabase --help`. Antes de ese intento, `2.115.0` quedó rechazada por la política `minimumReleaseAge = 604800`; no se modificó `bunfig.toml` ni se usó bypass. `supabase init` creó únicamente `supabase/config.toml` y `supabase/.gitignore`; no hay migraciones, schema, link remoto ni secretos. Docker sigue pendiente. Tests, typecheck, lint y build finalizaron correctamente; los warnings conocidos permanecen sin cambios.

## Commit

PENDING
