# FASE 2 — 2.14 REAL STORE INVENTORY & CREATION PLAN
# GPT-5.6 LUNA
# PLANIFICACIÓN LIVIANA — SIN WRITES REMOTOS

Proyecto:
e-ngenieria Hub

Rama:
develop

Base:
d950313 docs(authz): close persistent owner provisioning

Estado actual:

Current Owner:
PERSISTENT

Persistent Owner count:
1

roleSource:
persistent

Stores:
0

Assignments:
0

Connections:
0

Global fallback:
TRANSITIONAL

OAuth:
NOT STARTED

Phase 3:
NOT STARTED

OBJETIVO:

Determinar exactamente qué datos necesita una Store real según
el schema/código ACTUAL y preparar una matriz mínima para que
el usuario indique cuáles son las Stores reales a crear.

NO crear Stores todavía.

============================================================
0. ARCHIVAR PROMPT
============================================================

Crear:

docs/prompts/phase-02/2.14-real-store-inventory.md

Guardar este mandato completo.

No actualizar múltiples documentos por rutina.

============================================================
1. AUDITORÍA MÍNIMA
============================================================

Leer solamente lo necesario para conocer el contrato Store:

- migration donde se define stores;
- repository Store;
- tipos/validaciones relacionadas;
- docs/provisioning-plan.md si contiene dependencias relevantes.

No releer toda la documentación histórica.

Determinar campos reales:

- obligatorios;
- opcionales;
- generados automáticamente;
- tenant-derived;
- audit/status fields si existen.

============================================================
2. CONFIRMAR MODELO STORE
============================================================

Responder con el contrato ACTUAL del repo.

Ejemplo conceptual:

Store
- id: generated
- organization_id: server-derived
- name: required
- ...
- status: ...

NO inventar campos que no existen.

NO agregar todavía:

Mercado Libre account
OAuth tokens
provider connection
seller_id

salvo que formen parte REAL del schema Store actual.

Recordar:

Store != Mercado Libre Connection.

============================================================
3. IDENTIDAD DE TENANT
============================================================

Confirmar que una Store futura debe quedar asociada a la
Organization del Owner autenticado.

organization_id nunca debe provenir como autoridad del browser.

No modificar código.

============================================================
4. INVENTARIO REMOTO
============================================================

Confirmar READ ONLY:

stores = 0

No hacer ninguna escritura.

No consultar otras tablas salvo que sea necesario.

============================================================
5. PREPARAR MATRIZ DE STORES
============================================================

Actualizar únicamente:

docs/provisioning-plan.md

Agregar una sección mínima:

## Real Store Creation Matrix

Usar únicamente las columnas que realmente sean necesarias
según el schema.

Ejemplo si el schema sólo necesita name:

| Alias/Cliente | Store name | Required data complete | Approval |
| ... |

NO llenar nombres inventados.

Dejar filas/plantilla preparadas para decisión humana.

============================================================
6. CLIENTE VS STORE
============================================================

Confirmar documentalmente:

- un cliente puede tener una o más Stores;
- Store representa una unidad operativa/comercial dentro del Hub;
- una Store puede tener posteriormente una o más Connections;
- Mercado Libre será una Connection/provider posterior;
- no duplicar integración dentro de Store.

No rediseñar arquitectura.

============================================================
7. DETERMINAR QUÉ NECESITAMOS DEL USUARIO
============================================================

Entregar una lista MUY CORTA de los datos que el usuario debe
proporcionar para cada Store.

Por ejemplo:

1. nombre identificable;
2. cliente/alias al que pertenece;
3. cualquier otro campo obligatorio REAL del schema.

No pedir información que todavía no sea necesaria.

============================================================
8. PROPONER CREACIÓN FUTURA
============================================================

Preparar solamente el orden futuro:

Owner persistente
→ create Store
→ verify tenant ownership
→ repeat idempotently for approved Stores

No implementar todavía.

Si el repository actual NO tiene una primitive segura para crear
Stores:

informarlo como NEEDS IMPLEMENTATION.

No crearla en 2.14.

============================================================
9. FLUJO LIVIANO DE VALIDACIÓN
============================================================

Como este checkpoint debe ser sólo lectura + documentación:

ejecutar únicamente:

git diff --check
git status --short

NO ejecutar:

tests
typecheck
lint
build

salvo que accidentalmente se haya modificado código.

============================================================
10. NO HACER
============================================================

NO:

crear Stores
remote writes
SQL manual
migration
db push

crear usuarios
memberships
assignments
connections

OAuth
Mercado Libre API

retirar fallback
Phase 3

git add
git commit
git push

============================================================
11. CHECKPOINT
============================================================

Entregar únicamente:

1. Store schema actual resumido;
2. campos obligatorios;
3. campos generados/server-derived;
4. stores remoto count;
5. Store vs Connection confirmado;
6. datos exactos que necesitás del usuario por Store;
7. matriz creada sí/no;
8. existe primitive segura para crear Store sí/no;
9. siguiente acción recomendada;
10. git diff --check;
11. confirmar remote writes = NO;
12. confirmar git add/commit/push = NO.

Después detenerse.

## Cierre — commit documental 2.14

Proyecto: e-ngenieria Hub
Rama: `develop`
Base: `d950313`

Estado 2.14 aprobado:

- stores remoto = 0
- schema Store auditado
- Store != Connection
- `StoreRepository.create()` existe
- create NO tiene idempotencia segura
- no remote writes
- sólo docs modificados

### Auditoría mínima

Ejecutar únicamente:

```text
git status --short
git diff --check
git diff
```

Confirmar que sólo estén modificados:

```text
docs/provisioning-plan.md
docs/prompts/phase-02/2.14-real-store-inventory.md
```

Si aparece cualquier otro archivo, detenerse.

No repetir tests, typecheck, lint ni build.

### Staging explícito

Agregar únicamente esos dos archivos. No usar `git add .` ni `git add -A`.

Ejecutar `git diff --cached --check`.

### Commit y push

Crear:

```text
docs(stores): plan real store creation
```

Hacer `git push origin develop` y confirmar `HEAD == origin/develop` y working tree limpio.

### Restricciones

No realizar remote writes, crear Stores, migrations, cambios de código, memberships, assignments, Connections, OAuth, fallback removal ni iniciar Fase 3.

### Checkpoint

Entregar únicamente:

- commit hash
- archivos incluidos
- push OK/FAIL
- HEAD == origin/develop
- working tree clean
- stores remoto sigue 0
- confirmar no remote writes

Después detenerse.
