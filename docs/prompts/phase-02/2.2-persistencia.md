# Subfase 2.2 — Schema mínimo persistente

Status: **COMPLETED — commit pendiente**

## Purpose

Crear la primera migración SQL real de Fase 2 con `hub_memberships`, `stores` y `store_assignments`.

## Context

- Schema y estrategia definidos en 2.1.
- Supabase CLI preparada en 2.1B.
- Docker todavía no está disponible.
- No existe proyecto remoto linkeado.
- `connections` queda diferido a 2.5.

## Restrictions

No Connections, repositories, Store Scope funcional, endpoints, OAuth, RLS, `db push` ni link remoto.

## Prompt

### Fase 2 — Subfase 2.2: crear schema mínimo persistente

Con `develop` en `4006d22`, Fase 1 y 2.0/2.1/2.1B cerradas, y 2.2 autorizada, crear la primera migración SQL real mediante la Supabase CLI `2.114.0`. Antes de cualquier acción, archivar este mandato completo en este archivo con Status ACTIVE, Purpose, Context, Restrictions, Prompt, Result PENDING y Commit PENDING.

Leer AGENTS, REGLAS, handoff, plan, database, multi-tenancy, decisiones 2.0/2.1/2.1B, `supabase/config.toml` y `package.json`; no cambiar decisiones aprobadas sin informar contradicción. Crear una única migración oficial bajo `supabase/migrations/` para `hub_memberships`, `stores` y `store_assignments`; no crear `connections`, tokens, OAuth tables, jobs, webhooks ni entidades de producto.

Usar UUID PostgreSQL con `gen_random_uuid()` y habilitar solamente `pgcrypto` si es necesario. `hub_memberships` contiene UUID PK, Organization/Clerk user `text`, role, timestamps, `CHECK` exacto Owner/Manager/Employee/Client, `unique (organization_id, clerk_user_id)` y `unique (id, organization_id)`. `stores` contiene UUID PK, Organization text, nombre no vacío, status `active|disabled`, timestamps, sin unique nombre por Organization y con `unique (id, organization_id)`. `store_assignments` usa `membership_id`, `store_id`, `organization_id`, `created_at`, PK compuesta membership/store, FKs compuestas a membership/store que incluyen tenant y `on delete restrict`; no role ni UUID propio. No crear assignments automáticos ni checks cross-table de rol.

Agregar solo índices útiles: evaluar stores `(organization_id, status)` y assignments `(store_id, membership_id)` sin duplicar PK/unique. No trigger para `updated_at`; repositorios futuros lo actualizarán. No RLS/policies, seeds, datos reales, bootstrap Owner, connections, secretos ni URLs privadas.

Sin Docker, validar estáticamente SQL, orden, sintaxis razonable, constraints y diff; no afirmar ejecución local ni instalar motores alternativos. No login/link/push/start/reset ni remoto. Actualizar database, plan, handoff e índice de prompts para indicar migración creada, aislamiento, RLS diferido, no ejecutada y Connections diferido. Actualizar Result fiel y dejar Commit PENDING. Ejecutar tests, typecheck, lint, build, diff check/status/diff. No hacer staging, commit, push ni iniciar 2.3. Entregar checkpoint con migración, DDL, seguridad, validaciones y límites.

## Result

Se creó `20260821230525_phase_2_store_foundation.sql` mediante Supabase CLI. Define `hub_memberships`, `stores` y `store_assignments`, con UUID, checks, unicidad e integridad compuesta por Organization; no contiene Connections, RLS, seeds ni secretos. Por ausencia de Docker, la migración no fue ejecutada contra PostgreSQL y la validación fue estática. Los 31 tests, typecheck, lint y build finalizaron correctamente; persisten los warnings conocidos.

## Commit

PENDING

## References

Canonical: [meli-database.md](../../meli-database.md), [meli-multi-tenancy.md](../../meli-multi-tenancy.md)
