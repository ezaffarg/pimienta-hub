# Fase 2 — 2.20G: Remote safe token refresh validation

## Mandato operativo

Aplicar únicamente la migration `20260825100000_safe_integration_secret_refresh_rotation.sql` al proyecto remoto `ffcudwwrzttkumbdvada`, previamente validada localmente. Validar con fixtures exclusivamente dentro de transacciones `ROLLBACK`: versión inicial, claim, claim concurrente `busy`, lease activo y recuperable, complete CAS, stale writer rejection, release y version bump de reconnect. No refrescar credenciales reales, no llamar a Mercado Libre, no retomar listing sync ni modificar el prompt 2.20E.

Antes de aplicar: confirmar `HEAD == origin/develop` en `0741518`, conservar sin tocar el archivo preexistente `docs/prompts/phase-02/2.20e-first-real-listing-sync.md`, verificar el project ref y leer el estado real: Owner 1, Store 1, Connection activa 1, Assignments 0, `integration_secrets` 1, listings 0 y migration aún no aplicada. El dry-run debe listar exclusivamente la migration 2.20F; si no, detenerse.

Después del push: verificar history alineado, columnas y constraints de `integration_secrets`; las RPC claim/complete/release deben tener `SECURITY DEFINER`, `search_path` seguro y ejecución sólo de `service_role`. RLS enabled, cero policies browser-facing y acceso denegado para `PUBLIC`, `anon` y `authenticated`.

No ejecutar tests, typecheck, lint, build, reset local, `git add`, commit ni push. Al finalizar, ejecutar `git diff --check`, registrar resultados sin secretos y detenerse.

## Resultado — remote safe refresh validation

Target confirmado: `ffcudwwrzttkumbdvada`. El dry-run enumeró exclusivamente `20260825100000_safe_integration_secret_refresh_rotation.sql` y el push la aplicó; migration history local/remoto quedó alineado.

La matriz remota PASS, ejecutada con fixtures `remote_validation_220g_*` en una subtransacción revertida: credential version inicial 1, claim, concurrent claim `busy`, lease activo, lease recovery, CAS 1→2, stale CAS rechazado, release y reconnect version bump. Los campos, RLS enabled, cero policies, denegación de `PUBLIC`/`anon`/`authenticated` y grants exclusivos de `service_role` para tabla/RPCs fueron verificados. No quedaron fixtures; los conteos reales se mantuvieron Owner 1, Store 1, Connection activa 1, Assignments 0, `integration_secrets` 1 y listings 0.

No se expusieron ni consultaron tokens/ciphertexts, no hubo refresh real, llamadas a Mercado Libre ni listing sync. Tests, typecheck, lint y reset local se mantienen como validaciones previas; no hubo `git add`, commit ni push.

## Cierre — 2.20G remote safe refresh validation

Estado aprobado: migration `20260825100000_safe_integration_secret_refresh_rotation.sql` **APPLIED** en `ffcudwwrzttkumbdvada`; credential version, claim, concurrent claim `busy`, lease activo, recovery, CAS success, stale CAS rejection, release, reconnect version bump, RLS, policies 0, PUBLIC/anon/authenticated denied, service_role allowed y RPC grants service_role-only: **PASS**. Fixtures 0; Stores 1; Connections activas 1; Assignments 0; `integration_secrets` 1; Listings 0; Persistent Owners 1. Refresh real y listing sync real: **NO**. Prompt 2.20E: **PRESERVADO**.

Auditar `git status --short`, `git diff` y `git diff --check`. Deben cambiar únicamente `docs/meli-database.md` y este prompt; `docs/prompts/phase-02/2.20e-first-real-listing-sync.md` puede permanecer no trackeado y no debe eliminarse ni stagearse. Confirmar que la documentación no contiene access/refresh tokens, authorization code, state completo, ciphertext, Client Secret, master key ni service role key.

No repetir tests, typecheck, lint, build, `supabase db reset`, `supabase db push`, matriz SQL, refresh real ni listing sync. Stagear explícitamente sólo los dos documentos 2.20G, revisar `git diff --cached --check` y crear:

```text
docs(auth): record remote safe refresh validation
```

Hacer `git push origin develop`; verificar `HEAD == origin/develop`. El working tree puede conservar únicamente el prompt 2.20E no trackeado. No ejecutar refresh real, `POST /oauth/token`, nuevas llamadas Mercado Libre, business writes, OAuth nuevo ni Fase 3. Entregar hash, mensaje, archivos, secret hygiene, push, sincronización, estado del working tree, migration APPLIED, validación PASS, refresh/listing sync NO y prompt 2.20E preservado.
