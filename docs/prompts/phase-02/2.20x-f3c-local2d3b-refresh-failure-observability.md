# 2.20X-F3-C-LOCAL-2D3B — Refresh failure observability

## Mandato operativo

- Preservar diagnósticos internos exactos para `CAS_RPC_THROW`,
  `CAS_RPC_ERROR`, `CAS_RESPONSE_INVALID` y `CAS_CONFLICT`.
- Mantener genéricos los errores públicos cuando corresponda y no conservar
  tokens, ciphertexts, secrets, headers ni errores raw.
- Aplicar el cambio TypeScript mínimo; no cambiar SQL CAS, versionado, lease ni
  atomicidad y no crear migration.
- Agregar tests focalizados para throw, error Supabase, respuesta inválida,
  conflicto, éxito y redacción.
- Auditar read-only el reconnect target-bound existente y preparar la siguiente
  reautorización sin ejecutarla.
- Mantener en cero provider, OAuth, `/users/me`, maintenance y scheduler.
- Preservar los prompts locales existentes y `.vscode/tasks.json`.
- Validar tests focalizados, integración auth relevante, typecheck, lint,
  `git diff --check` y churn. No build salvo necesidad.
- No commit ni push.

## Estado inicial

2D3A confirmó que la RPC remota y el caller son compatibles y que dos flujos
sintéticos completos pasan con `3 → 4`, lease limpio y cleanup total. El fallo
real original no conservó el subtipo seguro y el refresh token persistido debe
considerarse potencialmente consumido. La Connection real permanece en versión
3 con credencial expirada.

## Resultado

- El gap quedó localizado en el harness operativo de 2D3, que omitió
  `MercadoLibreCredentialError.details.casFailure`; además, el repositorio no
  retenía el código seguro de un error RPC explícito.
- El caller conserva `CAS_RPC_THROW`, `CAS_RPC_ERROR`,
  `CAS_RESPONSE_INVALID` y `CAS_CONFLICT`. Para `CAS_RPC_ERROR` sólo propaga un
  `databaseCode` alfanumérico validado; descarta mensajes, details, hints y
  material de credenciales.
- Los errores públicos permanecen genéricos. Los summaries históricos con
  `CAS_REJECTED` continúan siendo legibles.
- No cambiaron SQL, migrations, atomicidad, versionado ni lease.
- Tests relevantes: **89/89 PASS**. Typecheck: **PASS**. Lint: **PASS**.
  Build: **SKIPPED** por no ser necesario. `git diff --check`: **PASS**.
- Auditoría read-only del reconnect: **REAUTH_FLOW_READY**. El inicio requiere
  `purpose=reconnect`; callback, pending target-bound y finalización canónica
  validan tenant, Store, provider e identidad server-side y reutilizan la
  Connection existente.
- Estado real read-only: Connection activa, `credential_version = 3`, credencial
  expirada, lease limpio, `maintenance_run_count = 0` y
  `missed_feed_due = true`.
- Provider calls, refresh, OAuth, `/users/me`, listing sync, maintenance y
  scheduler ejecutados durante 2D3B: **0**. Remote writes: **0**.
- La exposición efímera previa de una credencial por CLI queda reconocida; el
  valor no se repitió ni se almacenó en esta fase.
- Git add/commit/push: **NO**.
