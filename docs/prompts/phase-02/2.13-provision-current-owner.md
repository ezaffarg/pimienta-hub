# FASE 2 — CHECKPOINT 2.13
# PROVISION CURRENT OWNER PERSISTENTLY
# UNA ÚNICA ESCRITURA REAL CONTROLADA
# NO CREAR STORES / OTROS USUARIOS
# NO RETIRAR FALLBACK
# NO INICIAR OAUTH / FASE 3
# ARCHIVAR ESTE PROMPT ANTES DE CUALQUIER ACCIÓN

Proyecto:
e-ngenieria Hub

Rama:
develop

Commit base:
64b067b feat(authz): add controlled membership provisioning

Estado aprobado:

2.11:
CLOSED

2.12:
PLANNING COMPLETE / USER APPROVAL OBTAINED FOR CURRENT OWNER

Current Clerk Admin:
CURRENT REAL OWNER

Proposed persistent role:
Owner

Approval:
APPROVED

Remote Supabase project:
ffcudwwrzttkumbdvada

Remote inventory:

hub_memberships = 0
stores = 0
store_assignments = 0
connections = 0

Owner cardinality:
ONE OR MORE

Current real Owner count:
1

Safety invariant:
AT LEAST ONE OWNER

Fallback Clerk:
TRANSITIONAL

Real Stores:
NONE

Managers:
UNDEFINED

Employees:
UNDEFINED

Clients:
UNDEFINED

OAuth:
NOT STARTED

Phase 3:
NOT STARTED

Norma documental:

Repo = Source of Truth.
Obsidian = índice/memoria resumida.
Prompts = historial operativo enlazado.

OBJETIVO:

1. archivar este prompt;
2. auditar identidad/Organization del Current Clerk Admin;
3. verificar nuevamente que NO exista membership persistente;
4. provisionar únicamente al Current Clerk Admin como Owner persistente;
5. verificar que roleSource pase a persistent;
6. comprobar idempotencia;
7. verificar que el fallback ya no sea necesario para este usuario;
8. NO eliminar fallback global;
9. NO crear Stores;
10. NO crear otros usuarios;
11. actualizar docs/Obsidian si disponible;
12. detenerse antes de git add/commit/push.

============================================================
0. ARCHIVAR ESTE PROMPT PRIMERO
============================================================

Crear:

docs/prompts/phase-02/2.13-provision-current-owner.md

Guardar el texto COMPLETO de ESTE mandato.

Metadata:

# 2.13 — Provision Current Owner Persistently

Status: ACTIVE

Purpose:
Crear de forma controlada la primera membership persistente real
del Owner actual y verificar el cambio de roleSource desde fallback
Clerk a autoridad persistente.

Result:
PENDING

Commit:
PENDING

Agregar References según la convención vigente.

============================================================
1. SCOPE ESTRICTO
============================================================

ÚNICA escritura real autorizada:

crear la hub_membership persistente del Current Clerk Admin
como:

role = Owner

dentro de la Organization Clerk activa y previamente auditada.

No están autorizadas otras escrituras reales.

============================================================
2. NO USAR SQL MANUAL COMO PRIMERA OPCIÓN
============================================================

Preferir utilizar la primitive server-only implementada en 2.11:

membership provisioning

para ejecutar el flujo real.

Objetivo:

probar el mismo camino de aplicación que utilizaremos después
para otros miembros.

No insertar manualmente mediante SQL
salvo que exista un bloqueo técnico y se DETENGA antes
para reportarlo.

============================================================
3. IDENTIDAD TRUSTED
============================================================

La operación debe derivar server-side:

actor Clerk user
active Organization

Confirmar:

actor = Current Clerk Admin

El actor debe resolver actualmente:

fallback Owner

porque aún no existe membership persistente.

No aceptar:

organizationId
actor role
actor user ID

como autoridad del browser.

============================================================
4. TARGET
============================================================

Target del provisioning:

el MISMO Current Clerk Admin.

Debe verificarse que:

target pertenece a la Organization Clerk activa.

Proposed persistent role:

Owner

No usar otro target.

No usar emails como autoridad.

============================================================
5. PRECHECK REMOTO
============================================================

Antes de escribir:

consultar read-only:

hub_memberships

para:

active organization
+
Current Clerk Admin

Resultado esperado:

NOT FOUND

Además confirmar:

total hub_memberships = 0

si sigue siendo cierto.

Si ya existe membership:

NO crear otra.

Clasificar:

SKIP EXISTING

y detener escritura.

============================================================
6. VERIFICAR TARGET PROJECT
============================================================

Confirmar:

Supabase linked project ref =
ffcudwwrzttkumbdvada

Si no coincide:

DETENERSE.

============================================================
7. EJECUCIÓN DEL PROVISIONING
============================================================

Ejecutar la primitive server-only real:

Owner actor
→ target Current Clerk Admin
→ role Owner

Resultado esperado:

created

No cambiar ningún otro dato.

============================================================
8. POSTCHECK DB
============================================================

Consultar read-only después.

Esperado:

hub_memberships count = 1

Membership:

organization = active Organization
clerk_user_id = Current Clerk Admin
role = Owner

No documentar datos personales innecesarios.

============================================================
9. ROLE RESOLUTION POST-PROVISIONING
============================================================

Ejecutar/resolver la autorización del mismo usuario nuevamente.

Esperado:

role = Owner

roleSource = persistent

NO:

clerk-fallback

Esto es requisito crítico del checkpoint.

============================================================
10. PERMISOS OWNER
============================================================

Validar mediante tests/service actual:

Owner persistente mantiene permissions Owner esperados.

No modificar RBAC.

============================================================
11. STORE SCOPE OWNER SIN STORES
============================================================

Como actualmente:

stores = 0

Owner persistente debe resolver:

all-stores

dentro del tenant,

pero el conjunto material de Stores actuales es vacío.

No crear Stores para probarlo.

No considerar esto error.

============================================================
12. IDEMPOTENCIA
============================================================

Repetir de forma controlada la operación de provisioning
para el mismo target/role.

Resultado esperado:

already_exists

o contrato equivalente actual.

No crear segunda membership.

Después confirmar:

hub_memberships count = 1.

============================================================
13. FALLBACK PARA ESTE USUARIO
============================================================

Confirmar:

Current Clerk Admin ya NO depende del fallback
para obtener Owner.

Su resolución actual debe ser:

persistent.

Sin embargo:

NO retirar fallback global.

Otros usuarios futuros sin membership
todavía podrían necesitarlo durante transición.

============================================================
14. LAST OWNER SAFETY
============================================================

Estado después de provisioning:

persistent Owners = 1

Confirmar:

AT LEAST ONE OWNER

cumplido.

No probar degradación/eliminación.

No modificar Owner.

============================================================
15. NO CREAR STORES
============================================================

Mantener:

stores = 0

No crear Store real.

El siguiente checkpoint será separado.

============================================================
16. NO CREAR OTROS MIEMBROS
============================================================

No crear:

Manager
Employee
Client
second Owner

Mantenerlos:

UNDEFINED / NOT PROVISIONED

============================================================
17. NO STORE ASSIGNMENTS
============================================================

Mantener:

store_assignments = 0

Owner no requiere assignments explícitos.

============================================================
18. CONNECTIONS
============================================================

No modificar:

connections

Esperado:

0

si sigue vacío.

============================================================
19. FALLBACK CUTOVER READINESS
============================================================

Actualizar checklist:

Remote DB:
✓

Bootstrap:
✓

Persistent resolver:
✓

Provisioning primitive:
✓

Current Owner persistent:
✓ si este checkpoint pasa

Real Stores:
✗

Managers/Employees/Clients:
✗

Assignments:
✗

Fallback usage Owner:
0 esperado después de provisioning

Global fallback usage:
todavía no evaluado

Cutover:
NOT READY

Fallback:
TRANSITIONAL

============================================================
20. DOCUMENTACIÓN
============================================================

Actualizar mínimamente:

docs/provisioning-plan.md
docs/meli-multi-tenancy.md
docs/plan-y-gobierno.md
docs/codex-handoff.md
docs/prompts/phase-02/README.md
docs/prompts/phase-02/2.13-provision-current-owner.md

Actualizar meli-database.md sólo si resulta necesario
para registrar el primer dato persistente real.

No duplicar contenido.

Estado esperado:

Current Owner:
PERSISTENT

roleSource:
persistent

hub_memberships real count:
1

Stores:
0

Assignments:
0

Fallback:
TRANSITIONAL

Cutover:
NOT READY

============================================================
21. OBSIDIAN
============================================================

Si MCP disponible:

actualizar:

current-state
decisions
open-questions

Registrar:

Current Owner:
PERSISTENT

Persistent Owner count:
1

Fallback for Owner:
NOT USED

Global fallback:
TRANSITIONAL

Stores:
0

Next:
REAL STORE CREATION

Si MCP no está disponible:
documentarlo, no inventarlo.

============================================================
22. VALIDACIONES
============================================================

Ejecutar:

bun run test
bun run typecheck
bun run lint
bun run build

Si build falla sólo por bloqueo de red de Google Fonts
en entorno Codex:

NO modificar fuentes.

Registrar el mismo workaround ya conocido
y permitir validación manual local posterior.

Luego:

git diff --check
git status --short
git diff

============================================================
23. RESULT DEL PROMPT
============================================================

Si todo pasa:

Status:
COMPLETED

Result incluir:

- Current Owner persistent created;
- idempotence;
- roleSource persistent;
- membership count 1;
- fallback no longer used for Current Owner;
- global fallback remains;
- stores still 0;
- no other members;
- no OAuth;
- Phase 3 not started.

Commit:
PENDING

============================================================
24. NO HACER
============================================================

NO:

git add
git commit
git push

crear Stores
crear second Owner
crear Manager
crear Employee
crear Client

crear assignments
crear Connections

retirar fallback global

crear migration
db push
remote reset

OAuth
Mercado Libre API
tokens
callbacks
webhooks
sync
StoreIntegrationResolver

activar RLS
rate limiting

iniciar Fase 3.

============================================================
25. STOP CONDITIONS
============================================================

DETENERSE si:

- linked project ref cambia;
- target Clerk user no coincide con Current Clerk Admin;
- target no pertenece a Organization;
- aparece membership inesperada;
- provisioning intenta escribir más de una fila;
- roleSource sigue clerk-fallback después de creación;
- hub_memberships count termina distinto de 1;
- aparece cualquier dato remoto inesperado;
- se requiere cambiar schema.

============================================================
26. CHECKPOINT
============================================================

Entregar:

1. prompt archivado;
2. target project verificado;
3. actor verificado;
4. target verificado;
5. precheck membership;
6. provisioning result;
7. hub_memberships count post-write;
8. persistent role;
9. roleSource post-write;
10. Owner permissions;
11. Owner Store Scope;
12. idempotence result;
13. membership count after retry;
14. fallback usage Current Owner;
15. global fallback state;
16. persistent Owner count;
17. last Owner invariant;
18. Stores count;
19. assignments count;
20. Connections count;
21. other roles state;
22. cutover readiness;
23. docs updated;
24. Obsidian status;
25. tests;
26. typecheck;
27. lint;
28. build;
29. git diff --check;
30. confirmar only one authorized real write;
31. confirmar no Stores created;
32. confirmar no other memberships;
33. confirmar no assignments;
34. confirmar fallback global NOT removed;
35. confirmar OAuth NO;
36. confirmar Phase 3 NO;
37. confirmar git add/commit/push NO.

Después detenerse.

### Resultado de diagnóstico y corrección

CHECK: `requireServerAuthorizationContext()` dentro de `provisionMembership()`.

EXPECTED: `Owner` resuelto.

ACTUAL: `hub_memberships = 0`; el resolver sólo tenía fallback Clerk para el contexto de la ruta, pero la primitive exigía autoridad Owner persistente para crear el primer Owner.

RESULT: DENY / `AUTHORIZATION_DENIED` antes de escribir.

La corrección usa `bootstrap_first_owner` para el primer Owner, bajo `requireServerBootstrapContext()` (sesión, Organization activa y `org:admin`). El RPC es atómico e idempotente. `provisionMembership()` no fue modificado y sigue siendo la primitive para Owners posteriores y otros memberships. No se realizó ningún write remoto durante este diagnóstico.

## Continuación — diagnose real bootstrap 500

Mandato recibido: diagnosticar el HTTP 500 del POST temporal sin repetir el fetch ni realizar nuevas escrituras. Antes de cualquier corrección se debe confirmar read-only el conteo y membership del Current Clerk Admin, auditar la firma real de `bootstrap_first_owner` contra su llamada TypeScript y verificar que el runtime localhost apunte a `ffcudwwrzttkumbdvada`, sin exponer secretos. La respuesta pública debe permanecer normalizada; el detalle de diagnóstico solo puede estar en logs server-side. Se prohíbe SQL manual, schema changes, ampliación de permisos, OAuth, Fase 3 y Git write. Tras una corrección demostrada: tests, typecheck, lint, `git diff --check` y checkpoint antes de autorizar un único retry manual.

### Resultado del diagnóstico de 500

- Precheck remoto: `hub_memberships = 0`; el POST anterior no escribió una membership.
- Runtime target: `ffcudwwrzttkumbdvada` confirmado por hostname, no local.
- RPC remoto verificado: `public.bootstrap_first_owner(p_organization_id text, p_clerk_user_id text) returns TABLE(outcome text, membership_id uuid)`; coincide con el repositorio TypeScript.
- Error concreto: `SUPABASE_SERVICE_ROLE_KEY is not configured` / `supabaseKey is required`.
- Causa: la variable existe en `.env.local` pero su valor está vacío. El cliente Supabase server-only falla antes de ejecutar el RPC.
- Corrección requerida fuera de Git: configurar localmente la service-role key del proyecto remoto esperado y reiniciar `bun run dev`. No se registran ni exponen valores en este documento.
- No se repitió el fetch ni se realizaron writes remotos.

## Continuación — postcheck after first Owner created

Mandato recibido: tras el resultado manual `200 / created`, realizar únicamente postchecks remotos read-only. Confirmar una sola membership Owner del Current Clerk Admin dentro de la Organization activa, `stores = 0`, `store_assignments = 0`, `connections = 0`, y resolución real `role = Owner`, `roleSource = persistent`. No ejecutar el retry de idempotencia hasta completar todos los checks; conservar temporalmente el endpoint. Actualizar documentación provisional y ejecutar `git diff --check`, sin Git write, OAuth ni Fase 3.

### Resultado de postcheck

- `hub_memberships = 1`: única fila `Owner`, con Organization y Clerk user ref coincidentes con Current Clerk Admin.
- `stores = 0`, `store_assignments = 0`, `connections = 0`; persistent Owner count = 1.
- `AT LEAST ONE OWNER`: satisfied.
- El resolver persistente prioriza la membership encontrada, por lo que Current Owner resuelve `Owner / persistent`; no depende del fallback. Global fallback continúa TRANSITIONAL.
- La sesión de navegador disponible para el agente no contiene la sesión Clerk del usuario, por lo que no se hizo una segunda solicitud autenticada desde esta sesión.
- Idempotence: PENDING USER RETRY. El endpoint temporal permanece disponible hasta ese retry y posterior retiro.

## Continuación — idempotence confirmed + close 2.13

# CIERRE 2.13 — VERIFY IDEMPOTENCE + REMOVE TEMPORARY ENDPOINT

Estado confirmado por usuario: primer POST `200 / created`; segundo POST `200 / already_exists`. El cierre exige postcheck read-only (`hub_memberships = 1`, Stores, assignments y connections en 0), registrar `IDEMPOTENCE VALIDATED`, confirmar `Owner / persistent` y retirar por completo la ruta temporal `POST /api/internal/provision-current-owner` junto con sus tests e imports exclusivos. Deben preservarse `bootstrap_first_owner`, `requireServerBootstrapContext`, `provisionMembership` y el resolver persistente. No están autorizados writes remotos, SQL manual, migration, OAuth, Fase 3 ni Git write. El estado final debe declarar endpoint removed, global fallback transitional y próximo paso real Store creation planning.

### Resultado de cierre

Status: COMPLETED

- First call: `created`; second call: `already_exists`; final membership count: `1`.
- Current Owner: `Owner / persistent`; fallback dependency: no. Global fallback: TRANSITIONAL.
- Persistent Owners: `1`; `AT LEAST ONE OWNER` satisfied.
- Stores, assignments y connections: `0`.
- Endpoint temporal, helper exclusivo, tests HTTP exclusivos e imports muertos: REMOVED.
- Preservados: `bootstrap_first_owner`, `requireServerBootstrapContext`, `provisionMembership()` y resolver persistente.
- OAuth y Phase 3: NOT STARTED. Commit: PENDING.

## Cierre — audit + commit + push

# CIERRE FINAL 2.13 — AUDIT + COMMIT + PUSH
# GPT-5.6 LUNA
# NO NUEVAS IMPLEMENTACIONES
# NO NUEVAS WRITES REMOTAS

Proyecto:
e-ngenieria Hub

Rama:
develop

Estado aprobado:

2.13:
READY FOR COMMIT

Current Owner:
PERSISTENT

Persistent Owner count:
1

role:
Owner

roleSource:
persistent

Idempotence:
VALIDATED

First real request:
created

Second request:
already_exists

Final hub_memberships count:
1

Temporary provisioning endpoint:
REMOVED

Operational references remaining:
NONE

bootstrap_first_owner:
PRESERVED

provisionMembership():
PRESERVED

stores:
0

store_assignments:
0

connections:
0

Fallback Current Owner:
NOT REQUIRED

Global Clerk fallback:
TRANSITIONAL

OAuth:
NOT STARTED

Phase 3:
NOT STARTED

Validation:

tests:
80/80 PASS

typecheck:
PASS

lint:
PASS

build:
PASS

Known non-blocking warning:
Google Sans Flex fallback

git diff --check:
PASS

Working tree:
only docs/

.playwright-mcp:
REMOVED AS LOCAL OUT-OF-SCOPE ARTIFACT

OBJETIVO:

Auditar el cierre documental de 2.12 + 2.13,
archivar este mandato,
hacer staging explícito,
commit,
push a origin/develop
y verificar sincronización limpia.

============================================================
0. ARCHIVAR ESTE MANDATO
============================================================

Agregar el texto COMPLETO de este mandato al final de:

docs/prompts/phase-02/2.13-provision-current-owner.md

bajo:

## Cierre — audit + commit + push

No crear un nuevo archivo de prompt.

Actualizar metadata final:

Status:
COMPLETED

Result:
Current Owner provisioned persistently and first-owner flow validated.

Commit:
PENDING

============================================================
1. AUDITORÍA FINAL
============================================================

Ejecutar:

git status --short
git diff --stat
git diff
git diff --check

Confirmar:

- únicamente archivos docs/ pendientes;
- ningún src/ pendiente;
- ninguna migration pendiente;
- ningún secret;
- ningún .env;
- ningún artefacto Playwright;
- ningún archivo temporal;
- ningún cambio ajeno a 2.12/2.13.

Si aparece algo fuera de alcance:

DETENERSE.

============================================================
2. REVISAR DOCUMENTACIÓN FINAL
============================================================

Confirmar coherencia entre:

docs/provisioning-plan.md
docs/meli-multi-tenancy.md
docs/plan-y-gobierno.md
docs/codex-handoff.md
docs/prompts/phase-02/README.md
docs/prompts/phase-02/2.12-real-user-provisioning-plan.md
docs/prompts/phase-02/2.13-provision-current-owner.md

Estado canónico que debe quedar:

FIRST OWNER:
bootstrap_first_owner
+
requireServerBootstrapContext()

ADDITIONAL OWNER / OTHER MEMBERSHIP:
provisionMembership()

Owner cardinality:
ONE OR MORE

Safety invariant:
AT LEAST ONE OWNER

Current persistent Owners:
1

Current Owner roleSource:
persistent

Current Owner fallback dependency:
NO

Global fallback:
TRANSITIONAL

Stores:
0

Assignments:
0

Connections:
0

Managers:
UNDEFINED

Employees:
UNDEFINED

Clients:
UNDEFINED

Next:
REAL STORE CREATION PLANNING

OAuth:
NOT STARTED

Phase 3:
NOT STARTED

============================================================
3. NO REEJECUTAR PROVISIONING
============================================================

NO llamar nuevamente:

/api/internal/provision-current-owner

La ruta ya no debe existir.

NO hacer:

remote writes
SQL
RPC manual
DB mutation.

============================================================
4. STAGING EXPLÍCITO
============================================================

NO usar:

git add .
git add -A

Agregar explícitamente únicamente los archivos
documentales pertenecientes a 2.12/2.13.

Antes del commit:

git diff --cached --stat
git diff --cached
git diff --cached --check

Confirmar que el staged diff coincide exactamente con
el cierre documental esperado.

============================================================
5. COMMIT
============================================================

Crear:

git commit -m "docs(authz): close persistent owner provisioning"

No incluir cambios adicionales.

============================================================
6. PUSH
============================================================

Ejecutar:

git push origin develop

No force push.

============================================================
7. VERIFICACIÓN POST-PUSH
============================================================

Ejecutar:

git rev-parse HEAD
git rev-parse origin/develop
git status --short

Esperado:

HEAD == origin/develop

Working tree:
clean

============================================================
8. ACTUALIZAR COMMIT EN HISTORIAL
============================================================

IMPORTANTE:

No crear un nuevo commit únicamente para insertar
el hash recién generado dentro del propio prompt
si eso produciría metadata circular.

El hash final puede quedar registrado en:

- checkpoint de salida;
- README/índice cuando corresponda en un checkpoint futuro;
- Git history.

No generar commit documental extra sólo para auto-referenciar
su propio hash.

============================================================
9. NO HACER
============================================================

NO:

modificar código
crear migration
db push
remote reset

crear Store
crear second Owner
Manager
Employee
Client
assignment
Connection

retirar fallback

OAuth
Mercado Libre
RLS
Phase 3

nuevas remote writes.

============================================================
10. CHECKPOINT FINAL
============================================================

Entregar:

1. commit hash;
2. commit message;
3. archivos incluidos;
4. confirmar únicamente docs;
5. staged diff check;
6. push result;
7. HEAD;
8. origin/develop;
9. working tree clean sí/no;
10. Current persistent Owner count = 1;
11. roleSource = persistent;
12. idempotence = validated;
13. temporary endpoint = removed;
14. Stores = 0;
15. assignments = 0;
16. connections = 0;
17. global fallback = TRANSITIONAL;
18. OAuth = NOT STARTED;
19. Phase 3 = NOT STARTED;
20. confirmar no nuevas remote writes.

Después detenerse.


## Resultado de precheck

Status: BLOCKED — SERVER-SIDE INVOCATION PATH REQUIRED

- Target linked confirmado: `ffcudwwrzttkumbdvada`.
- Precheck remoto: `hub_memberships = 0`.
- El repositorio no contiene una ruta, Server Action, script operativo autenticado ni otra frontera server-side reutilizable para invocar `provisionMembership()` con `requireServerAuthorizationContext()` real.
- La primitive no acepta actor ni Organization desde CLI por diseño, y no se usó SQL manual como sustituto.
- No se realizó ninguna escritura remota.

La continuación requiere una decisión explícita: autorizar un entrypoint server-only mínimo y autenticado para ejecutar la primitive, o autorizar expresamente un procedimiento operativo alternativo. No se debe pasar actor, role u Organization como autoridad desde el navegador o la CLI.

## Continuación — diagnose first-owner 403

# CONTINUAR 2.13 — DIAGNOSTICAR Y CORREGIR 403 DEL FIRST OWNER PROVISIONING
# NO REPETIR WRITE HASTA CORREGIR Y VALIDAR
# NO AMPLIAR AUTORIZACIÓN GENERAL
# NO USAR SQL MANUAL
# CONTINUAR EL PROMPT 2.13 EXISTENTE

Proyecto:
e-ngenieria Hub

Rama:
develop

Estado observado por el usuario:

POST /api/internal/provision-current-owner

Resultado:

HTTP 403

Body:

{
  "error": {
    "code": "AUTHORIZATION_DENIED",
    "message": "The current user is not authorized for this action"
  }
}

Confirmado:

- sesión Clerk autenticada existe;
- Organization activa seleccionada = My Organization;
- Current Clerk Admin es el Owner real aprobado;
- Clerk role observado previamente = org:admin;
- hub_memberships remoto = 0;
- no hubo escritura remota;
- no se creó Owner persistente;
- no se debe repetir el fetch hasta resolver el 403.

Hipótesis a auditar:

El endpoint puede estar creando una dependencia circular:

persistent Owner
→ requerido para ejecutar provisionMembership()
→ pero persistent Owner todavía no existe
→ por lo tanto nunca puede crearse el primer Owner.

OBJETIVO:

1. archivar esta continuación;
2. identificar exactamente qué guard/check devuelve 403;
3. demostrar la causa;
4. corregir únicamente el flujo especial del Current Owner inicial;
5. mantener la autorización normal fail-closed;
6. agregar tests de regresión;
7. detenerse ANTES de volver a ejecutar la escritura real;
8. entregar instrucciones para que el usuario repita el fetch sólo después del checkpoint.

============================================================
0. ARCHIVAR CONTINUACIÓN
============================================================

Agregar el texto COMPLETO de este mandato a:

docs/prompts/phase-02/2.13-provision-current-owner.md

bajo:

## Continuación — diagnose first-owner 403

No crear otro prompt.

============================================================
1. DIAGNÓSTICO EXACTO
============================================================

Inspeccionar:

- route handler temporal;
- requireServerAuthorizationContext();
- requireServerBootstrapContext();
- persistent role resolver;
- Clerk fallback resolver;
- provisionMembership();
- RBAC/permission checks usados por provisioning.

Identificar exactamente qué condición produce:

AUTHORIZATION_DENIED

No adivinar.

Documentar:

CHECK:
<nombre exacto>

EXPECTED:
<valor esperado>

ACTUAL:
<valor observado>

RESULT:
DENY

============================================================
2. NO MODIFICAR PRIMITIVES A CIEGAS
============================================================

No cambiar provisionMembership()
ni requireServerAuthorizationContext()
hasta entender la causa.

No debilitar authz global para resolver el bootstrap.

============================================================
3. MODELO DE SEGURIDAD ESPERADO
============================================================

El flujo excepcional del primer Owner debe cumplir TODOS:

- Clerk authenticated;
- active Organization;
- current Clerk user;
- Clerk Organization membership real;
- Clerk role = org:admin;
- target = actor;
- requested role fijo = Owner;
- hub_memberships para esa Organization = 0;
- no persistent Owner existente;
- una sola operación idempotente.

Sólo bajo esas condiciones puede permitirse
el first-owner self-provisioning.

============================================================
4. NO CONVERTIR FALLBACK EN AUTORIZACIÓN GENERAL
============================================================

No establecer:

"todo org:admin puede crear Owners siempre".

La excepción debe limitarse al estado:

NO persistent Owner / first Owner bootstrap transition.

Después de existir un Owner persistente,
el flujo normal debe volver a autoridad persistente.

============================================================
5. EVALUAR REUTILIZAR BOOTSTRAP CONTEXT
============================================================

Existe:

requireServerBootstrapContext()

Auditar si esa frontera es conceptualmente correcta
para este caso.

Si ya valida:

- Clerk auth;
- active org;
- org:admin;
- server-side identity;

preferir reutilizarla o extraer una primitive interna común,
en vez de duplicar validaciones.

NO mezclar responsabilidades sin necesidad.

============================================================
6. POSIBLE DISEÑO
============================================================

Una solución aceptable puede ser una primitive específica:

provisionCurrentInitialOwner()

o equivalente,

que:

1. obtiene bootstrap context server-side;
2. verifica actor = target;
3. verifica org sin persistent Owner;
4. invoca la capa/repositorio segura;
5. crea Owner;
6. es idempotente.

Pero NO implementar este nombre por obligación.
Elegir la solución mínima coherente con el repo.

============================================================
7. NO ACEPTAR INPUT DE AUTORIDAD
============================================================

El endpoint debe seguir sin aceptar:

- orgId;
- userId;
- targetUserId;
- role;
- membershipId.

Role sigue fijo:

Owner.

Target sigue siendo:

authenticated user.

============================================================
8. PRECONDICIÓN DB
============================================================

Para permitir first-owner provisioning:

consultar server-side:

persistent Owner count para Organization.

Resultado necesario:

0

y membership del current Clerk user:

NOT FOUND.

Si existe cualquier Owner persistente:

NO usar first-owner exception.

Volver a flujo normal.

============================================================
9. CONCURRENCIA
============================================================

Recordar que ya existe bootstrap/RPC concurrency-safe
para first Owner.

Si la solución necesita garantizar que dos requests simultáneos
no creen dos first Owners inconsistentes:

reutilizar la estrategia existente.

No inventar un check-then-insert vulnerable
si la DB ya tiene una primitive atómica apropiada.

============================================================
10. IMPORTANTE: BOOTSTRAP VS PROVISION MEMBERSHIP
============================================================

Reevaluar esta decisión:

Para el PRIMER Owner persistente,
quizás la primitive correcta sea el bootstrap RPC
y no provisionMembership().

El bootstrap ya fue diseñado específicamente para:

- Organization sin Owner;
- Clerk org:admin;
- crear primer Owner;
- concurrency safety;
- idempotencia.

Si esta es la arquitectura correcta:

usar bootstrap para el primer Owner
y reservar provisionMembership() para Owners posteriores
y otros roles.

Si se concluye esto:

documentarlo como decisión canónica.

NO usar SQL manual.

============================================================
11. TESTS REQUERIDOS
============================================================

Agregar/ajustar tests para cubrir:

A.
authenticated org:admin
+ no persistent Owner
→ first Owner allowed.

B.
una vez creado Owner persistente
→ first-owner path no vuelve a crear.

C.
org:member
→ denied.

D.
unauthenticated
→ denied.

E.
sin active Organization
→ denied.

F.
target siempre actor.

G.
role siempre Owner.

H.
Organization no configurable.

I.
dos requests/resultado idempotente según primitive real.

J.
DB failure
→ fail closed.

K.
persistent Owner existente distinto
→ first-owner exception disabled.

============================================================
12. NO HACER WRITE REAL TODAVÍA
============================================================

Aunque los tests pasen:

NO ejecutar todavía la request real.

Primero entregar checkpoint.

El usuario repetirá el fetch sólo después
de revisión humana.

============================================================
13. ENDPOINT TEMPORAL
============================================================

Mantener temporalmente:

POST /api/internal/provision-current-owner

sólo si sigue siendo necesario.

No hacerlo más genérico.

No agregar UI.

============================================================
14. VALIDACIONES
============================================================

Ejecutar:

bun run test
bun run typecheck
bun run lint

Build:

bun run build

Si sólo falla por Google Fonts/network conocido:

no modificar fonts.

Ejecutar además:

git diff --check
git status --short
git diff

============================================================
15. DOCUMENTACIÓN
============================================================

Actualizar según resultado:

docs/provisioning-plan.md
docs/meli-multi-tenancy.md
docs/plan-y-gobierno.md
docs/codex-handoff.md
docs/prompts/phase-02/2.13-provision-current-owner.md

Registrar claramente cuál es la primitive canónica para:

FIRST OWNER

y cuál para:

ADDITIONAL OWNER / OTHER MEMBERSHIP

si la auditoría determina que son diferentes.

============================================================
16. NO HACER
============================================================

NO:

git add
git commit
git push

remote write
SQL manual
migration
db push
remote reset

crear Store
crear otro Owner
Manager
Employee
Client
assignment
Connection

retirar fallback

OAuth
Phase 3.

============================================================
17. CHECKPOINT
============================================================

Entregar:

1. causa exacta del 403;
2. guard/check que fallaba;
3. si existía dependencia circular;
4. solución elegida;
5. primitive canónica para first Owner;
6. primitive canónica para Owners adicionales;
7. si se modificó requireServerAuthorizationContext;
8. si se modificó provisionMembership;
9. si se reutilizó bootstrap;
10. seguridad de actor;
11. seguridad de Organization;
12. role fijo;
13. target fijo;
14. idempotencia;
15. concurrency safety;
16. tests nuevos;
17. total tests;
18. typecheck;
19. lint;
20. build;
21. git diff --check;
22. remote writes = NO;
23. membership count remoto sigue 0;
24. endpoint temporal sigue disponible sí/no;
25. instrucciones exactas para repetir el fetch;
26. no Stores;
27. no otros memberships;
28. fallback global intacto;
29. OAuth NO;
30. Phase 3 NO;
31. git add/commit/push NO.

Después detenerse.


## Continuación — authenticated provisioning entrypoint

# CONTINUAR 2.13 — CREAR ENTRYPOINT AUTENTICADO MÍNIMO PARA PROVISION CURRENT OWNER
# NO USAR SQL MANUAL
# NO CREAR UI
# NO AMPLIAR SCOPE
# CONTINUAR PROMPT 2.13 EXISTENTE

Proyecto:
e-ngenieria Hub

Rama:
develop

Estado actual:

2.13 — BLOCKED BEFORE WRITE

Confirmado:

- remote project ref = ffcudwwrzttkumbdvada;
- hub_memberships = 0;
- Current Clerk Admin no tiene membership persistente;
- provisionMembership() existe;
- provisionMembership() exige requireServerAuthorizationContext();
- requireServerAuthorizationContext() necesita request autenticada Clerk real;
- no existe entrypoint operativo autenticado para invocarlo;
- no hubo escritura remota;
- no SQL manual;
- no Stores;
- no otros memberships;
- no assignments;
- no OAuth;
- no Phase 3;
- no staging/commit/push.

DECISIÓN HUMANA:

Se autoriza crear un ENTRYPOINT SERVER-ONLY MÍNIMO Y AUTENTICADO
para invocar la primitive existente de provisioning.

NO se autoriza SQL manual como sustituto.

OBJETIVO:

1. archivar esta continuación dentro de 2.13;
2. diseñar el entrypoint mínimo;
3. reutilizar Clerk/server auth existente;
4. no aceptar actor/org/role desde browser como autoridad;
5. permitir únicamente el provisioning del Current Clerk Admin como Owner;
6. ejecutar UNA vez mediante sesión Clerk real;
7. verificar persistent roleSource;
8. mantener idempotencia;
9. no crear UI permanente innecesaria;
10. detenerse antes de git add/commit/push.

============================================================
0. ARCHIVAR CONTINUACIÓN
============================================================

Agregar el texto COMPLETO de este mandato a:

docs/prompts/phase-02/2.13-provision-current-owner.md

bajo:

## Continuación — authenticated provisioning entrypoint

No crear 2.13b.

Preservar historial previo.

============================================================
1. REUTILIZAR ARQUITECTURA EXISTENTE
============================================================

Antes de crear nada revisar:

src/lib/auth/
route handlers existentes
Server Actions existentes
API error conventions
requireServerAuthorizationContext()
membership-provisioning.ts

No crear un segundo sistema de auth.

No duplicar lógica de Clerk.

============================================================
2. ENTRYPOINT MÍNIMO
============================================================

Crear el mecanismo operativo más pequeño posible.

Preferencia:

Server Action server-only

o route handler autenticado mínimo

según arquitectura existente y facilidad real de invocación.

No crear UI administrativa completa.

No crear endpoint genérico de provisioning.

No crear:

POST /api/users/:id/role

ni API amplia equivalente.

============================================================
3. SCOPE DEL ENTRYPOINT
============================================================

Este entrypoint debe servir EXCLUSIVAMENTE para:

provisionar al usuario autenticado actual,
dentro de su Organization activa,
como Owner inicial persistente aprobado.

No debe aceptar:

organizationId
actorUserId
targetUserId
membershipId
role

desde browser como autoridad.

El contrato ideal no necesita parámetros de identidad.

Conceptualmente:

provisionCurrentOwner()

============================================================
4. AUTHENTICATION
============================================================

El entrypoint debe exigir:

- Clerk authenticated session;
- active Organization;
- current Clerk user;
- current Clerk org role = org:admin.

Derivar todo server-side.

Si cualquiera falta:

DENY.

============================================================
5. TARGET = ACTOR
============================================================

Para 2.13:

target user debe ser exactamente:

authenticated current Clerk user.

No permitir provisionar a otra persona.

Esto evita spoofing y mantiene scope mínimo.

============================================================
6. ROLE FIJO
============================================================

Role debe quedar fijo:

Owner

en código server-side.

No recibir role desde form/body/query.

No usar mass assignment.

============================================================
7. INVOCACIÓN DE PRIMITIVE
============================================================

Después de validar Clerk:

invocar la primitive existente:

provisionMembership()

o contrato real equivalente.

Reutilizar:

- Organization derivada;
- actor persistente/fallback autorizado;
- target membership validation;
- create membership;
- controlled result.

No duplicar repository logic.

============================================================
8. PARTICULARIDAD DEL ACTOR ACTUAL
============================================================

Antes del provisioning, Current Clerk Admin todavía resuelve:

role = Owner
roleSource = clerk-fallback

Eso es esperado y autorizado temporalmente
para ejecutar el provisioning inicial.

Después:

role = Owner
roleSource = persistent

debe quedar confirmado.

============================================================
9. SEGURIDAD DEL BOOTSTRAP/PROVISIONING
============================================================

No confundir:

Bootstrap First Owner RPC

con:

provisionCurrentOwner entrypoint.

La DB está vacía y ya existe primitive provisioning Owner.

Usar el flujo que mejor respete la arquitectura existente.

No crear segundo Owner.

No crear duplicado.

============================================================
10. EXPOSICIÓN TEMPORAL/PERMANENTE
============================================================

Evaluar si el entrypoint debe quedar como primitive interna reutilizable.

Preferencia:

mantener una primitive server-only segura,
pero NO dejar una ruta pública innecesaria.

Si se necesita una route temporal únicamente para ejecutar 2.13:

debe ser fuertemente restringida
y después evaluarse si eliminarla antes del commit.

No dejar backdoor operativo.

============================================================
11. TESTS OBLIGATORIOS
============================================================

Agregar tests para el entrypoint/service:

A. authenticated org:admin
→ puede continuar.

B. org:member
→ deny.

C. no auth
→ deny.

D. sin active Organization
→ deny.

E. target = authenticated user.

F. organizationId no configurable.

G. userId no configurable.

H. role no configurable.

I. provisionMembership returns created
→ preservar created.

J. already_exists
→ preservar resultado controlado.

K. DB/provisioning error
→ comportamiento controlado.

No usar usuarios reales en tests.

============================================================
12. EJECUCIÓN REAL
============================================================

Después de tests y auditoría:

ejecutar el entrypoint usando la sesión Clerk REAL
del Current Clerk Admin.

NO simular identidad mediante CLI.

NO pasar IDs manualmente como autoridad.

Si la ejecución requiere que el usuario abra la app en localhost
y dispare una acción autenticada:

puede implementarse un mecanismo mínimo y seguro.

Pero NO crear una UI administrativa permanente.

Si requiere intervención humana:

detenerse y dar instrucciones exactas al usuario.

============================================================
13. PRECHECK JUSTO ANTES DE WRITE
============================================================

Antes de ejecutar la escritura real:

confirmar read-only:

hub_memberships = 0

y Current Clerk Admin:
no persistent membership.

Si cambió:

DETENERSE.

============================================================
14. WRITE AUTORIZADO
============================================================

Única escritura remota autorizada:

Current Clerk Admin
→ hub_membership
→ role Owner

Una sola Organization.

Una sola membership.

No otras tablas.

============================================================
15. POSTCHECK
============================================================

Después de created:

verificar:

hub_memberships = 1

Current Owner:
role = Owner

No otros memberships.

stores = 0
store_assignments = 0
connections = 0

============================================================
16. ROLE SOURCE POST-WRITE
============================================================

Ejecutar resolución autenticada nuevamente.

Resultado obligatorio:

role = Owner
roleSource = persistent

Si sigue:

clerk-fallback

DETENERSE.

No declarar 2.13 exitoso.

============================================================
17. IDEMPOTENCIA
============================================================

Invocar nuevamente el flujo autenticado.

Esperado:

already_exists

y:

hub_memberships = 1

No insertar segunda fila.

============================================================
18. FALLBACK
============================================================

Para Current Owner:

fallback usage:
NO LONGER REQUIRED.

Global fallback:

TRANSITIONAL

NO eliminarlo.

============================================================
19. ENTRYPOINT FINAL
============================================================

Antes de cerrar:

auditar si quedó algún entrypoint temporal peligroso.

Si fue una route temporal exclusivamente para ejecución:

preferir eliminar la route temporal
si la primitive server-only reutilizable ya cumple el futuro flujo.

No eliminar tests/servicio útil.

No dejar una URL secreta como mecanismo de seguridad.

============================================================
20. DOCUMENTACIÓN
============================================================

Actualizar:

docs/provisioning-plan.md
docs/meli-multi-tenancy.md
docs/plan-y-gobierno.md
docs/codex-handoff.md
docs/prompts/phase-02/README.md
docs/prompts/phase-02/2.13-provision-current-owner.md

Estado esperado:

Current Owner:
PERSISTENT

Persistent Owner count:
1

roleSource:
persistent

Stores:
0

Assignments:
0

Connections:
0

Other memberships:
0

Global fallback:
TRANSITIONAL

Next:
REAL STORE CREATION

============================================================
21. OBSIDIAN
============================================================

Si MCP disponible:

actualizar resumido.

Si no:
documentarlo.

No inventar integración.

============================================================
22. VALIDACIONES
============================================================

Ejecutar:

bun run test
bun run typecheck
bun run lint
bun run build

Si Codex vuelve a fallar por Google Fonts/network,
recordar que build manual local ya pasó previamente
y no modificar fuentes.

Luego:

git diff --check
git status --short
git diff

============================================================
23. RESULT
============================================================

Si todo pasa:

2.13 Status:
COMPLETED

Result:

- authenticated entrypoint implemented;
- Current Owner persisted;
- roleSource persistent;
- idempotence validated;
- no unsafe public provisioning endpoint;
- global fallback remains;
- Stores remain zero;
- Phase 3 not started.

Commit:
PENDING

============================================================
24. NO HACER
============================================================

NO:

git add
git commit
git push

SQL manual para Owner

crear Store
second Owner
Manager
Employee
Client
assignments
Connections

retirar global fallback

migration
db push
remote reset

OAuth
Mercado Libre
RLS
rate limiting
Phase 3.

============================================================
25. STOP CONDITIONS
============================================================

DETENERSE si:

- no puede usar sesión Clerk real;
- entrypoint exige IDs confiados al browser;
- target ≠ authenticated user;
- role puede configurarse;
- project ref cambia;
- membership aparece antes del write;
- más de una fila sería escrita;
- roleSource no pasa a persistent;
- hace falta SQL manual;
- hace falta modificar schema.

============================================================
26. CHECKPOINT
============================================================

Entregar:

1. continuación archivada;
2. tipo/path del entrypoint;
3. si es permanente o temporal;
4. authenticated session validation;
5. active Organization derivation;
6. org:admin validation;
7. target = actor confirmation;
8. role fixed Owner;
9. tests nuevos;
10. total tests;
11. precheck DB;
12. si requirió acción manual del usuario;
13. provisioning result;
14. membership count;
15. role post-write;
16. roleSource post-write;
17. idempotence;
18. membership count retry;
19. entrypoint temporal eliminado sí/no/no aplica;
20. Stores count;
21. assignments count;
22. Connections count;
23. global fallback;
24. docs;
25. Obsidian;
26. typecheck;
27. lint;
28. build;
29. git diff --check;
30. confirmar una sola escritura real;
31. confirmar no SQL manual;
32. confirmar no otros memberships;
33. confirmar no Stores;
34. confirmar no assignments;
35. confirmar no OAuth;
36. confirmar no Phase 3;
37. confirmar no git add/commit/push.

Después detenerse.
