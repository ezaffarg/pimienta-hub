# 2.20X-F3-C-LOCAL-2D3C9 — Missed-feed failure diagnostics

## Objetivo

Diagnosticar sin provider calls dónde se pierde el stage de
`missed_feed_failed`, por qué el run real no conserva un conteo exacto de
requests y si un intento fallido se rearma de forma segura.

## Estado de entrada

- Commit desplegado: `a9eba72463ce188cdc562725384f0ef0f59a64ae`.
- Única maintenance real: HTTP 503, run `failed/missed_feed_failed`, counters 0.
- `credential_version=4`, credencial vigente y lease libre.
- El intento avanzó `last_missed_feed_check_at`, mantuvo cursor null y dejó
  `missed_feed_due=false` inmediatamente después.

## Alcance

- Trazar servicio, client, repository, RPCs y cadence SQL.
- Reproducir con mocks identidad, HTTP, respuesta inválida y éxitos con cero o
  más eventos.
- Aplicar sólo un fix local si no requiere cambiar schema.
- Mantener en cero provider, maintenance, OAuth, reconnect y scheduler.

## Resultado

- El detalle se pierde en el `catch` de
  `MercadoLibreIntegrationEventMaintenanceService`: cualquier error emitido por
  `recoverItems()` se reduce al par canónico
  `missed_feed_failed` / `Missed feeds recovery failed safely`.
- El servicio de recovery ya distingue fallas seguras de configuración,
  binding, identidad, provider, paginación e intake, pero ese stage no se
  conserva en el run de maintenance.
- El conteo exacto de provider calls del run real anterior es irrecuperable. El
  flujo permite acotarlo a 0–2 (`/users/me` y `/missed_feeds`), pero no registra
  attempts/successes antes de lanzar el error.
- Conservar stage y counters exactos de manera durable para observabilidad
  administrativa requiere ampliar schema, RPCs y repository. No se creó ni se
  aplicó ninguna migration en este bloque.
- La semántica de estado es correcta: `last_missed_feed_check_at` representa un
  intento reciente, aun si falló. El catch fija el timestamp después de la
  falla y checkpoint/finalize lo persisten; por eso `missed_feed_due` queda en
  `false` durante 15 minutos y se rearma automáticamente al vencer la ventana.
  Un cursor no nulo fuerza continuidad inmediata; cursor null no impide el
  próximo intento por cadence.
- Se agregaron pruebas deterministas para falla de identidad antes de la página,
  falla HTTP después de los attempts, página válida sin eventos y persistencia
  segura del estado terminal fallido. Los casos existentes siguen cubriendo
  respuesta inválida e intake exitoso.
- Validación focalizada: 28/28 PASS.
- Typecheck: PASS.
- Lint: PASS, 0 warnings y 0 errors.
- Read-only remoto: proyecto `ffcudwwrzttkumbdvada`, una credencial,
  `credential_version=4`, vigente, lease CLEAR; último run `failed`, cursor
  null y `missed_feed_due=false` dentro de la ventana vigente de 15 minutos.
- Provider calls, maintenance executions, OAuth y reconnect durante este bloque:
  0.
- Clasificación: `MISSED_FEED_STAGE_OBSERVABILITY_GAP` y
  `PROVIDER_CALL_COUNTER_GAP`; semántica de estado
  `MISSED_FEED_FAILURE_STATE_CORRECT`.
- Recomendación: `MISSED_FEED_MIGRATION_REQUIRED`.
