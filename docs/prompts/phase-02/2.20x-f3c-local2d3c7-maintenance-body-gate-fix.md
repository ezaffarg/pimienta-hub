# 2.20X-F3-C-LOCAL-2D3C7 — Fix empty POST body gate for internal maintenance

## Objetivo

Corregir únicamente la detección de body vacío de
`POST /api/internal/maintenance/incremental-events` para que un POST autenticado
sin bytes llegue al orquestador y cualquier payload siga rechazado.

## Estado de entrada

- `credential_version=4`, credencial vigente y refresh lease libre.
- `missed_feed_due=true`.
- La única invocación 2D3C6 autenticó el bearer, pero respondió HTTP 400
  `invalid_request` antes de ejecutar `runIncrementalEventMaintenance()`.
- Provider calls, mantenimiento persistido y cambios de DB en 2D3C6: cero.

## Alcance autorizado

- Aceptar ausencia de body, `Content-Length: 0` y streams de cero bytes.
- Rechazar whitespace, `{}` y cualquier contenido no vacío o ilegible.
- Mantener bearer auth, secreto, aislamiento, budgets y orquestador sin cambios.
- Agregar tests focalizados con orquestación mockeada.
- Ejecutar tests focalizados, typecheck, lint y diff-check.

## Fuera de alcance

- Provider calls, OAuth, reconnect, refresh o `/users/me`.
- Mantenimiento real, scheduler, migrations o cambios remotos.
- Commit o push.

## Resultado

- La ruta ahora lee el contenido después del bearer gate: acepta cero bytes y
  rechaza cualquier contenido o fallo de lectura antes del orquestador.
- Tests de ruta: **12/12 PASS**. Regresión directa: **20/20 PASS**.
- Typecheck y lint: **PASS**. Build: **SKIPPED**.
- Provider, OAuth, refresh, `/users/me`, mantenimiento real y scheduler: **0**.
