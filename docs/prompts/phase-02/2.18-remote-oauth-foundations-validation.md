# Fase 2 — 2.18 Remote Migration + Privilege Validation

> Aplicación remota controlada. Sin OAuth real ni Stores/Connections reales.

## Estado de entrada

- Base Git: `f171fe3 feat(integrations): add secure oauth foundations` en `develop`.
- Pendientes locales: `20260824184934_oauth_security_foundations.sql` y `20260824191800_database_privilege_hardening.sql`.
- Reset local, 89/89 tests y typecheck: PASS. RLS local habilitado en las tablas Hub; browser roles denegados; service role server-only validado.
- Proyecto remoto exclusivo: `ffcudwwrzttkumbdvada` (`e-hub-db`). Estado conocido: una membership Owner persistente; cero Stores, assignments y Connections; OAuth no iniciado.

## Mandato operativo

1. Confirmar Git limpio y `HEAD == origin/develop == f171fe3`; detenerse si no coincide.
2. Confirmar que Supabase CLI está linkeada exclusivamente a `ffcudwwrzttkumbdvada`; no cambiar de proyecto automáticamente.
3. Antes del push, mediante consultas read-only confirmar: `hub_memberships = 1`, Owners persistentes = 1, `stores = store_assignments = connections = 0`; las migrations 2.16/2.17 aún no aplicadas. Detenerse si hay Connections u otro estado inesperado.
4. Ejecutar dry-run con Supabase CLI 2.114.0. Deben aparecer exactamente las migrations 2.16 y 2.17; detenerse si aparece otra.
5. Sólo si coincide, ejecutar `supabase db push` contra el proyecto linkeado. Nunca remote reset ni SQL manual alternativo.
6. Verificar historial remoto alineado y las dos migrations aplicadas.
7. Confirmar post-push: una membership y Owner persistente; cero Stores, assignments, Connections, OAuth attempts, integration secrets y audit events. No crear fixtures persistentes.
8. Verificar RLS real en las siete tablas Hub: `hub_memberships`, `stores`, `store_assignments`, `connections`, `oauth_attempts`, `integration_secrets`, `audit_events`; sin policies browser-facing inesperadas.
9. Auditar grants efectivos: `PUBLIC`, `anon` y `authenticated` sin DML/SELECT; `service_role` con DML server-only sobre las siete tablas.
10. Auditar `bootstrap_first_owner`, onboarding admin y onboarding Client: sólo `service_role` tiene EXECUTE; son `SECURITY DEFINER`, con `search_path = pg_catalog` y tablas schema-qualified.
11. Probar de forma real y sin datos persistentes que anon/authenticated no leen foundations ni ejecutan RPCs; validar `service_role` en una transacción rollback sin alterar el Owner ni dejar Stores/Connections. No probar onboarding con datos permanentes.
12. No generar state OAuth, authorize URL, callback, tokens ni `GET /users/me`; no configurar runtime Mercado Libre.

## Límites

Actualizar sólo `docs/meli-database.md`, `docs/codex-handoff.md` y este prompt para registrar migrations remotas, RLS, grants, RPC privileges y row counts. No repetir tests/typecheck/lint/build; ejecutar `git diff --check` si cambia documentación. No hacer `git add`, commit ni push.

Detenerse si el project ref no coincide, el dry-run incluye migrations inesperadas, hay Connections antes del push, Owner count difiere de uno, falla una migration, RLS/grants/RPCs no coinciden, service role pierde acceso o queda cualquier fixture. El checkpoint debe reportar target, conteos, dry-run/push/history, RLS/policies/grants/RPCs, tests de privilegio, Owner intacto, docs, diff check y confirmar explícitamente: OAuth real NO, Stores/Connections reales NO, Git add/commit/push NO.

## Resultado

- Target confirmado: `ffcudwwrzttkumbdvada` (`e-hub-db`).
- Dry-run y push: exclusivamente `20260824184934` y `20260824191800`; ambas aplicadas.
- Conteos finales: una membership Owner; cero Stores, assignments, Connections, OAuth attempts, integration secrets y audit events.
- RLS activo y cero policies en las siete tablas Hub. Roles `PUBLIC`, `anon` y `authenticated` sin acceso directo; `service_role` conserva DML y las tres RPCs.
- Pruebas remotas reales de denegación para anon/authenticated y de primitive server-only con rollback: PASS. Sin fixtures residuales.
- OAuth real, Stores/Connections reales, `git add`, commit y push: NO.

## Cierre — remote validation commit

Proyecto: e-ngenieria Hub. Rama: `develop`. Sólo documentación; no repetir tests, typecheck, lint, build, `supabase db reset` ni `supabase db push`.

Auditar `git status --short`, `git diff` y `git diff --check`. Deben aparecer únicamente `docs/meli-database.md`, `docs/codex-handoff.md` y este prompt. Si aparece cualquier otro archivo, detenerse.

Hacer staging explícito sólo de esos tres documentos; no usar `git add .` ni `git add -A`. Revisar `git diff --cached --check`. Crear el commit `docs(database): record remote oauth foundation validation` y ejecutar `git push origin develop`, sin force push. Confirmar `HEAD == origin/develop` y working tree limpio.

No ejecutar remote DB writes, migrations, OAuth, authorize, callback, token exchange, `GET /users/me`, creación de Store/Connection/assignment/audit event real, retiro de Clerk fallback ni Fase 3.

Checkpoint: informar únicamente hash, archivos, push, sincronización, working tree, migrations remotas APPLIED, Owner persistente = 1, RLS remoto VALIDATED, RPC privileges VALIDATED, OAuth real NO y Stores/Connections reales = 0.
