# Subfase 2.3 — Repositories, roles persistentes y memoria

Status: **COMPLETED**

## Purpose

Crear la capa server-only de persistencia para memberships y Stores, preparar la resolución definitiva de roles e-Hub desde `hub_memberships` y verificar/inicializar Obsidian Memory Layer.

## Context

- Schema definido en 2.2, aún no ejecutado.
- Clerk continúa proporcionando identidad y Organization.
- `hub_memberships` será la fuente definitiva del rol e-Hub.
- El usuario configuró Obsidian MCP y se debe verificar en esta sesión.

## Restrictions

No Store Scope completo, Connections, OAuth, RLS, `db push`, link remoto ni 2.4.

## Prompt

### Fase 2 — Subfase 2.3: repositories server-only, roles persistentes y memoria Obsidian

Con base `95d4828`, Fase 1 y 2.0/2.1/2.1B/2.2 cerradas y 2.3 autorizada, crear repositorios server-only para `hub_memberships`, Stores y assignments, preparar la resolución persistente de roles y verificar la capa Obsidian. La migración existe pero no se ejecutó; no hay Docker ni proyecto remoto linkeado. Antes de toda acción, archivar este mandato con Status ACTIVE, Purpose, Context, Restrictions, Result PENDING y Commit PENDING.

Verificar realmente las herramientas o recursos de Obsidian MCP. Si están disponibles, confirmar lectura/escritura dentro del vault e inicializar/actualizar notas condensadas para índice, estado, arquitectura, decisiones, terminología, preguntas abiertas e investigación Mercado Libre/MercadoCuentas. Git sigue como fuente de verdad; no copiar docs, prompts, código, SQL completo, secretos, tokens, env, cookies, claves o passwords. Membrane/skills.sh solo es referencia conceptual para capacidades de Mercado Libre: no instalar ni usar como runtime, backend, proxy, OAuth o gestor de tokens; documentación oficial y decisiones del repo prevalecen.

Auditar AGENTS, REGLAS, handoff, plan, database, multi-tenancy, decisiones 2.0/2.1/2.2, infraestructura, auth, integrations y tests. Clerk conserva AuthN, identidad y Organization activa; `hub_memberships` es la autoridad de rol. El navegador nunca decide Organization, role, membership, Store o permisos.

Crear repositorios server-only mínimos: membership lookup por Organization+Clerk user, verificación de Owner y creación controlada; Stores list/get/create siempre tenant-scoped; assignments por membership+Organization y primitives tenant-safe. No crear lookup inseguro por Store global, browser client, framework paralelo, ORM, Store Scope final, Connections, OAuth, RLS, endpoints ni sync. Reutilizar roles aprobados Owner/Manager/Employee/Client y policy actual.

Preparar resolución `Clerk user + active Organization → hub_membership → Hub Role`; el mapping Clerk `org:admin → Owner` y `org:member → Employee` debe quedar solo como fallback transitorio para demos, sin ampliar permisos ni persistir memberships automáticamente. Implementar bootstrap del primer Owner solo si puede garantizar usuario/Organization/org:admin/ausencia de Owner server-side y sin cambio silencioso de schema; si la concurrencia exige nueva constraint, detener solo esa parte y registrar decisión.

Agregar tests deterministas sin DB real, mockeando frontera Supabase: queries scoped, errores DB, Stores scoped, assignments Organization y resolución Owner/Manager/Employee/Client/missing membership sin concesión. Si hay bootstrap, probar admin, primer Owner, segundo deny y autoridad server-side. Mantener permisos actuales y diferir Store Scope a 2.4.

Actualizar documentación y memoria si MCP está disponible. Ejecutar tests, typecheck, lint, build, diff check/status/diff. No instalar Docker, ejecutar Supabase local/remoto, crear Connections/OAuth/RLS/2.4 ni hacer Git add/commit/push. Actualizar Result con estado Obsidian, notas y resultado real; marcar COMPLETED solo si corresponde y detener tras checkpoint.

## Result

- Obsidian Memory Layer estuvo disponible: se verificó lectura/escritura y se inicializaron notas condensadas de índice, estado, arquitectura, decisiones, terminología, preguntas abiertas e investigación de Mercado Libre/MercadoCuentas. Git continúa siendo la fuente de verdad.
- Se crearon repositorios server-only tenant-scoped para memberships, Stores y assignments, además de un resolver de rol persistente que no concede acceso cuando falta el membership.
- El mapping de Clerk sigue como fallback transitorio en los guards existentes: la migración no fue ejecutada y no se añadieron auto-provisioning ni acceso a DB en runtime.
- El bootstrap del primer Owner quedó diferido: garantizarlo bajo concurrencia requiere una decisión y una constraint de base de datos adicional.
- Validación: 40 tests aprobados; typecheck y lint correctos; build correcto; `git diff --check` correcto. Persisten únicamente los warnings conocidos de Vitest/Vite ESM, Google Sans Flex y `metadataBase`. La comprobación adicional `bun run format:check` reporta 302 incidencias preexistentes fuera del alcance; no se realizó un formateo masivo.

## Commit

PENDING
