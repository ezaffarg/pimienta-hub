# FASE 2 — 2.19C REMOTE PENDING OAUTH FOUNDATION VALIDATION

# GPT-5.6 TERRA + MEDIUM

# APLICAR UNA SOLA MIGRATION REMOTA

# NO OAUTH REAL

Proyecto: e-ngenieria Hub

Rama: develop

Base: `a3ebdea feat(integrations): add pending oauth authorization storage`

Migration pendiente: `20260824195457_oauth_pending_authorizations.sql`

Estado remoto conocido:

- Project ref: `ffcudwwrzttkumbdvada`
- `hub_memberships = 1`; persistent Owners = 1.
- `stores = 0`; `store_assignments = 0`; `connections = 0`.
- `oauth_attempts = 0`; `integration_secrets = 0`; `audit_events = 0`.
- 2.16 + 2.17: APPLIED + VALIDATED REMOTELY.
- RLS y RPC privileges: VALIDATED.
- OAuth real: NOT STARTED.

## Objetivo

Aplicar únicamente la migration `20260824195457_oauth_pending_authorizations.sql` al Supabase remoto y validar tabla, constraints, RLS, grants, service_role, estado vacío y Owner real intacto. No ejecutar OAuth real.

## Prechecks

Confirmar working tree limpio y `HEAD == origin/develop` en `a3ebdea...`. Confirmar que el proyecto Supabase vinculado es exclusivamente `ffcudwwrzttkumbdvada`. Antes del push, comprobar por lectura que persistent Owners = 1, stores = 0, connections = 0 y que `20260824195457` todavía no está aplicada. Si alguno no coincide, detenerse.

## Dry-run y push

Ejecutar `supabase db push --dry-run` (o la sintaxis equivalente de CLI 2.114.0). Debe listar exactamente `20260824195457_oauth_pending_authorizations.sql`; si aparece otra migration, detenerse. Sólo entonces ejecutar `supabase db push`. No usar `db reset`, SQL manual ni otras migrations.

## Validación remota

Confirmar history local/remota alineada y `20260824195457 = APPLIED`. Validar que existe `oauth_pending_authorizations` con FK compuesta a `oauth_attempts`, Organization, actor, provider, purpose, `external_account_id`, campos de token cifrado, `expires_at`, `consumed_at` y relación única con attempt. Nunca mostrar ciphertext.

Confirmar RLS ENABLED, policies = 0, grants PUBLIC/anon/authenticated DENIED y DML server-only para `service_role`. Probar realmente que anon y authenticated no pueden SELECT. Si es necesario INSERT/UPDATE de service_role o constraints, usar transacción revertida y no dejar fixtures. No repetir toda la matriz local: sólo demostrar coincidencia remota con el schema validado localmente.

## Estado final requerido

Sin OAuth real, attempts, Stores, Connections, assignments, integration secrets ni pending authorizations reales. Los conteos finales deben ser: `oauth_pending_authorizations = 0`, `oauth_attempts = 0`, `integration_secrets = 0`, `audit_events = 0`, `stores = 0`, `connections = 0`, `store_assignments = 0`, `hub_memberships = 1` y persistent Owners = 1. No modificar el Owner existente.

## Documentación y límites

Actualizar únicamente `docs/meli-database.md` y este prompt; `docs/codex-handoff.md` sólo si es estrictamente necesario. No repetir tests, typecheck, lint, build ni local reset. Ejecutar `git diff --check`.

No ejecutar `git add`, commit o push. No ejecutar OAuth authorize, callback, token exchange, `GET /users/me`, Store, Connection, assignment, integration secret, OAuth attempt ni pending authorization reales. Detenerse si el target es incorrecto, el dry-run contiene otra migration, Owner count != 1, la migration falla, RLS/grants no coinciden o quedan fixtures.

## Checkpoint requerido

Informar project ref, dry-run, db push, migration aplicada, tabla, RLS, policies, accesos PUBLIC/anon/authenticated/service_role, constraints, todos los counts finales, Owner, fixtures, docs, `git diff --check`, OAuth real = NO, remote business writes = NO y git add/commit/push = NO.

## Resultado

- Precheck Git: `HEAD == origin/develop == a3ebdea` y branch `develop`; el árbol estaba limpio antes de archivar este mandato.
- Target confirmado: `ffcudwwrzttkumbdvada`; CLI `2.114.0`.
- Precheck remoto: una membership Owner persistente; cero Stores, assignments, Connections, OAuth attempts, secretos y audit events; la migration 20260824195457 estaba pendiente.
- Dry-run: listó exclusivamente `20260824195457_oauth_pending_authorizations.sql`.
- Push: PASS; migration aplicada y history local/remota alineada.
- Schema: tabla creada con FK compuesta a `oauth_attempts`, binding Organization/actor/provider/purpose, unicidad por `oauth_attempt_id`, identidad externa, campos de tokens cifrados, expiración y consumo.
- RLS: enabled; policies: 0. PUBLIC, anon y authenticated no tienen SELECT/INSERT/UPDATE/DELETE; service_role tiene DML server-only.
- Pruebas reales: SELECT de anon y authenticated denegados. Una transacción con service_role creó una attempt y pending authorization ficticias, verificó consumo único y terminó en rollback; no se mostró ciphertext ni quedaron fixtures.
- Conteos finales: pending authorizations, OAuth attempts, integration secrets, audit events, Stores, Connections y assignments = 0; memberships = 1; persistent Owners = 1.
- OAuth real, rutas OAuth runtime, Stores/Connections/assignments reales, `git add`, commit y push: NO.

## Cierre — remote validation commit

# CIERRE 2.19C — COMMIT DOCUMENTAL REMOTE VALIDATION

# GPT-5.6 LUNA

# SOLO DOCUMENTACIÓN

# NO REPETIR TESTS / BUILD / DB PUSH

Proyecto: e-ngenieria Hub

Rama: develop

Estado aprobado:

- Project ref: `ffcudwwrzttkumbdvada`
- Migration: `20260824195457_oauth_pending_authorizations.sql`
- Remote status: APPLIED + VALIDATED
- `oauth_pending_authorizations = 0`
- `oauth_attempts = 0`
- `integration_secrets = 0`
- `audit_events = 0`
- `stores = 0`
- `connections = 0`
- `store_assignments = 0`
- `hub_memberships = 1`
- Persistent Owners: 1
- RLS: ENABLED
- Policies: 0
- PUBLIC: DENIED
- anon: DENIED
- authenticated: DENIED
- service_role: VALIDATED
- Fixtures residuales: 0
- OAuth real: NOT STARTED

### Auditoría mínima

Ejecutar `git status --short`, `git diff` y `git diff --check`. Deben aparecer únicamente `docs/meli-database.md` y `docs/prompts/phase-02/2.19c-remote-pending-oauth-validation.md`; si aparece otro archivo, detenerse.

### No repetir validaciones

No ejecutar tests, typecheck, lint, build, `supabase db reset` ni `supabase db push`: ya fueron validados.

### Staging, commit y push

Agregar explícitamente sólo esos dos documentos; no usar `git add .` ni `git add -A`. Ejecutar `git diff --cached --check`. Crear `git commit -m "docs(database): record remote pending oauth validation"` y ejecutar `git push origin develop`.

### Verificación y límites

Confirmar `HEAD == origin/develop` y working tree limpio. No hacer remote DB writes, OAuth real, authorize, callback, token exchange, `GET /users/me`, Store, Connection, assignment, integration secret real ni Phase 3.

### Checkpoint

Entregar únicamente commit hash, archivos incluidos, push OK, `HEAD == origin/develop`, working tree clean, migration remota APPLIED, pending OAuth foundation VALIDATED, Owner persistente = 1 y OAuth real = NO. Después detenerse.
