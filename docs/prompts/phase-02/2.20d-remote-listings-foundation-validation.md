# FASE 2 — 2.20D REMOTE LISTINGS FOUNDATION VALIDATION

Base `af5eba8`. Proyecto remoto exclusivo: `ffcudwwrzttkumbdvada`. Aplicar únicamente `20260824223000_phase_2_listings_foundation.sql`; validar schema, FK compuesta, unique/upsert, RLS y grants con fixtures ficticias dentro de transacciones `ROLLBACK`.

Precondiciones: `develop` limpio y sincronizado; Owner persistente 1, Stores 1, Connection activa 1, Assignments 0 e `integration_secrets` 1. Confirmar read-only que la migration y tabla `listings` aún no existen. Ejecutar `supabase db push --dry-run`; debe listar exclusivamente la migration indicada. Sólo entonces ejecutar `supabase db push`, nunca SQL manual ni otra migration.

Validar remotamente: columnas y `price numeric(20,4)`; FK `(connection_id, store_id, organization_id)` hacia `connections`; unique `(connection_id, external_listing_id)`; RLS enabled, cero policies browser-facing; PUBLIC/anon/authenticated denegados y `service_role` con DML. Probar dentro de rollback primer insert, upsert idempotente, actualización de precio/stock/status/sold quantity, SKU nulo, dos listings, mismo external ID en otra Connection, ownership mismatch rechazado y `last_synced_at` actualizado. No borrar por ausencia ni ejecutar stress test pesado; evaluar la concurrencia por la unique key + upsert.

Al finalizar, confirmar fixtures residuales 0, listings 0 y preservación de Stores 1, Connection real activa 1, Assignments 0, secretos 1 y Owner persistente 1. No persistir la listing real de E.A.ZOCOOL, no sincronizar Mercado Libre, no ejecutar PUT/PATCH/DELETE ni cambios de precio/stock/status, no modificar UI, workers, cron, webhooks o Fase 3.

Sólo actualizar `docs/meli-database.md` y este prompt si corresponde. No repetir tests, typecheck, lint, build, reset local ni cierre Git. No ejecutar `git add`, commit o push. El checkpoint debe registrar target, dry-run, apply, migration APPLIED, schema, constraints, RLS/grants, cada assertion, conteos finales, documentación, `git diff --check` y confirmar que no hubo persistencia real ni mutaciones.

## Resultado — 2.20D remote listings foundation validation

Target `ffcudwwrzttkumbdvada` confirmado. Dry-run enumeró exclusivamente `20260824223000_phase_2_listings_foundation.sql`; `supabase db push` la aplicó y migration history quedó APPLIED/aligned. `public.listings` existe con el schema aprobado, `numeric(20,4)`, binding compuesto, unique idempotente, RLS enabled y policies 0.

La matriz remota con `service_role` pasó first insert, idempotent upsert, update, null SKU, two listings, same external ID in other Connection, ownership mismatch y `last_synced_at`. Se revirtieron 3 fixtures. PUBLIC, anon y authenticated quedaron DENIED (SELECT/INSERT directos confirmados); service_role DML quedó ALLOWED.

Conteos finales: listings 0, Stores 1, Connections 1 activa, Assignments 0, integration_secrets 1 y Persistent Owners 1. No se modificó la Connection real, no se persistió E.A.ZOCOOL, no hubo sync real ni mutaciones Mercado Libre. Tests/typecheck/lint/build/reset no se repitieron; sólo queda `git diff --check`. No se ejecutó git add/commit/push.

## Cierre — 2.20D remote listings validation

Estado aprobado: migration `20260824223000_phase_2_listings_foundation.sql` APPLIED; `public.listings` validada remotamente con `numeric(20,4)`, FK/binding, unique, RLS enabled, policies 0, PUBLIC/anon/authenticated DENIED y service_role ALLOWED. First insert, upsert idempotente, update, SKU nulo, dos listings, otra Connection, ownership mismatch y `last_synced_at`: PASS. Delete by absence: NO. Fixtures 0; listings 0; Stores 1; Connections 1 activa; assignments 0; secrets 1; Owners 1. Persistencia real y mutaciones Mercado Libre: NO.

Para cerrar: auditar sólo `docs/meli-database.md` y este prompt; confirmar higiene de secretos y ejecutar `git diff --check`. Stagear explícitamente ambos documentos, verificar `git diff --cached --check`, crear `docs(listings): record remote foundation validation`, hacer push a `origin/develop` y verificar `HEAD == origin/develop` con árbol limpio. No repetir tests, typecheck, lint, build, reset ni `supabase db push`; no ejecutar sync real, nuevas llamadas Mercado Libre, escrituras de negocio, workers, cron, webhooks, UI ni Fase 3.
