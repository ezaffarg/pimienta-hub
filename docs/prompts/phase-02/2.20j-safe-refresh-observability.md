# FASE 2 — 2.20J SAFE REFRESH OBSERVABILITY

Base: `0ac6b4a`. El refresh real 2.20H terminó en `real_refresh_failed`; 2.20I clasificó el problema como observabilidad insuficiente. No ejecutar refresh real, listing sync, OAuth, `/connect`, `/users/me`, DB push, migration ni cierre Git.

## Mandato

Agregar observabilidad server-side segura y determinista al refresh sin exponer access/refresh tokens, ciphertext, client secret, master key, service-role key, body crudo del proveedor, request body ni headers sensibles. Preservar sin modificar los prompts 2.20E, 2.20H y 2.20I.

Auditar `MercadoLibreCredentialService.getValidAccessToken()`, cliente OAuth, repository claim/complete/release, cifrado/descifrado, configuración y normalización. La taxonomía interna debe distinguir como mínimo:

`CONFIGURATION_ERROR`, `CREDENTIAL_READ_FAILED`, `CREDENTIAL_DECRYPT_FAILED`, `REFRESH_CLAIM_FAILED`, `REFRESH_BUSY`, `REFRESH_DOUBLE_CHECK_FAILED`, `PROVIDER_NETWORK_ERROR`, `PROVIDER_TIMEOUT`, `PROVIDER_HTTP_ERROR`, `PROVIDER_INVALID_REFRESH_TOKEN`, `PROVIDER_RESPONSE_INVALID`, `CREDENTIAL_ENCRYPTION_FAILED`, `REFRESH_CAS_FAILED`, `REFRESH_RELEASE_FAILED` y `REFRESH_UNKNOWN_ERROR`.

Separar errores internos de los errores públicos: el navegador puede continuar recibiendo `real_refresh_failed`, pero el servidor debe conservar `stage`, código seguro, HTTP status y únicamente códigos allowlisted del proveedor. Para `/oauth/token`, distinguir timeout de red, status HTTP y `invalid_grant` sin almacenar respuestas completas. Mantener obligatorio el refresh token rotado, sin fallback silencioso, y no cambiar lease, `credential_version`, CAS, TTL, cifrado, schema, RLS, grants ni modelo Connection.

Usar logging estructurado mínimo existente; sólo permitir `connectionId` interno, stage, safeCode, status, providerCode allowlisted y credential version. Si release falla, conservar el error primario y registrar el fallo secundario de forma segura. No dejar runners temporales innecesarios ni ejecutar el runner contra la Connection real.

Agregar tests deterministas sin credenciales reales para configuración, descifrado, claim, busy, red, timeout, HTTP 4xx/5xx, `invalid_grant`, respuesta malformada, refresh rotado ausente, cifrado, CAS, release, redacción y camino exitoso. No crear migration.

Actualizar mínimamente `docs/meli-mercadolibre-OAuth/API.md`, `docs/meli-database.md` cuando corresponda y este prompt. La política es ningún retry automático del refresh token. Repo = Source of Truth; Obsidian sólo índice/resumen.

## Resultado de ejecución

- La pérdida de información estaba en `MercadoLibreOAuthClient.exchangeToken()` y en el catch del servicio, que reducían red, timeout, HTTP y respuesta inválida a un error genérico.
- Implementada taxonomía server-side con stages `READ`, `DECRYPT`, `CLAIM`, `DOUBLE_CHECK`, `PROVIDER_REQUEST`, `PROVIDER_RESPONSE`, `ENCRYPT`, `CAS_COMPLETE` y `RELEASE`.
- HTTP status y `error` allowlisted se conservan sin body crudo; `invalid_grant` se clasifica como refresh inválido; timeout y red quedan separados.
- El error público de OAuth continúa normalizado y no recibe secretos.
- Release secundario no reemplaza el error primario.
- Migration nueva: `NO`.
- Refresh real, OAuth, listing sync y remote writes: `NO`.

## Validación y checkpoint

Tests focalizados de auth/refresh: `17/17 PASS`.

`bun run typecheck`: `PASS`.

`bun run lint`: `PASS` (0 warnings, 0 errors).

Build: `NO` (no requerido por el mandato).

`git diff --check`: se ejecuta antes del checkpoint. No hacer `git add`, `git commit` ni `git push`. Recomendar `SAFE_TO_RETRY_ONCE` sólo después de revisar el diff y aprobar explícitamente el único retry real; hasta entonces, `NOT_SAFE_TO_RETRY`.

## Cierre — diagnóstico y safe refresh observability

Base previa: `0ac6b4a`. Este cierre consolida 2.20H, 2.20I y 2.20J. El primer refresh real falló con observabilidad insuficiente; no hubo retry, listing sync, token leakage ni escritura confirmada. 2.20I validó configuración, master key, descifrado y contrato estático; 2.20J implementó clasificación segura, separación internal/public y redacción.

Auditar `git status --short`, `git diff --stat`, `git diff` y `git diff --check`. Incluir únicamente los cinco archivos de auth modificados, `docs/meli-mercadolibre-OAuth/API.md`, `docs/meli-database.md` y los prompts 2.20H, 2.20I y 2.20J. Preservar fuera del cierre `docs/prompts/phase-02/2.20e-first-real-listing-sync.md`.

Confirmar que no hay access/refresh tokens reales, authorization code, state, ciphertext, secrets, service-role key, Authorization headers, request bodies ni raw provider responses. No repetir tests/typecheck/lint/build/db reset/db push ni refresh real. Staging explícito; no usar `git add .` ni `git add -A`.

Crear el commit:

```text
feat(auth): add safe refresh diagnostics
```

Hacer `git push origin develop` y confirmar `HEAD == origin/develop`. Sólo usar `--no-verify` si un hook ejecuta un build no requerido y las validaciones aprobadas siguen vigentes; no ejecutar build manual. El estado final puede conservar únicamente el prompt 2.20E sin trackear.

Registrar: hash, mensaje, incluidos/excluidos, secret hygiene, cached diff check, push, uso de `--no-verify`, HEAD/origin, working tree, tests focalizados `18/18 PASS`, typecheck/lint PASS, migration nueva NO, remote writes NO, segundo refresh real NO, listing sync NO y prompt 2.20E preservado. El segundo refresh queda autorizado sólo para la subfase siguiente, no se ejecuta en este cierre.
