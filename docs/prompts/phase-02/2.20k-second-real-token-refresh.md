# FASE 2 — 2.20K SECOND REAL MERCADO LIBRE TOKEN REFRESH

Base: `6115c15`. Ejecutar exactamente un segundo intento real, una sola vez, mediante `MercadoLibreCredentialService.getValidAccessToken()` para la Connection activa de E.A.ZOCOOL. La observabilidad segura de 2.20J está activa. No ejecutar listing sync.

## Reglas y precondiciones

Confirmar `HEAD == origin/develop`; preservar sin modificar `docs/prompts/phase-02/2.20e-first-real-listing-sync.md`. Verificar read-only que existan Owner persistente 1, Store 1, Connection activa 1, Assignments 0, `integration_secrets` 1 y Listings 0. Confirmar `credential_version`, lease libre, access expirado y refresh cifrado presente.

Validar sólo indicadores de `MERCADO_LIBRE_CLIENT_ID`, `MERCADO_LIBRE_CLIENT_SECRET` e `INTEGRATION_SECRETS_MASTER_KEY`; descifrar access y refresh server-side sin mostrar plaintext. Si falla configuración o descifrado, detenerse.

## Único intento

Invocar una sola vez la primitive real. Registrar sólo `CLAIMED`, `BUSY` o `FAILED`; si es `BUSY` o falla, detenerse. Respetar double-check: si otro proceso ya dejó un access token vigente, informar `ALREADY_REFRESHED` sin llamar al proveedor. Si sigue vencido, permitir únicamente el POST `/oauth/token` realizado por el cliente OAuth existente; no imprimir request body, headers ni secretos.

En cualquier fallo registrar sólo `stage`, `safeCode`, HTTP status y providerCode allowlisted. `invalid_grant`/`PROVIDER_INVALID_REFRESH_TOKEN` implica `RECONNECT_REQUIRED` y detención inmediata, sin tercer intento. En todos los fallos confirmar lease clear/recuperable, versión correcta, credenciales previas preservadas y Connection activa.

Si el refresh funciona, confirmar response válida, refresh rotado presente, cifrado antes de persistencia, CAS, `credential_version N→N+1`, lease clear y expiración futura. Sólo entonces ejecutar una llamada read-only `GET /users/me`, confirmar identidad y displayName E.A.ZOCOOL, sin mostrar tokens ni IDs completos. No ejecutar búsquedas de listings, multiget, `ListingSyncService` ni persistencia.

Las únicas escrituras remotas autorizadas son claim/release y rotación de credenciales mediante CAS. No modificar Stores, Assignments, Listings, OAuth pending/attempts ni datos de negocio. No cambiar código, migrations ni configuración. No repetir tests/typecheck/lint/build/db reset/db push. No hacer git add/commit/push; ejecutar únicamente `git diff --check` y detenerse.

## Resultado seguro

El checkpoint debe informar: Connection resuelta; indicadores de configuración y descifrado; versión/lease/expiración inicial; claim y double-check; si se ejecutó POST y cantidad de retries; resultado; stage/safeCode/status/providerCode; reconnect required; validez y presencia de credenciales sin valores; encryption/CAS; versión/lease/expiración final; Connection; `GET /users/me`, identity y displayName; conteos finales de Stores, Connections, Assignments, integration_secrets, Listings y Owners; remote writes; mutaciones ML limitadas al refresh; token leakage; listing sync; código modificado; documentación; diff check; Git; y prompt 2.20E preservado.

## Estado

Este mandato queda archivado antes del único intento real. Bajo ninguna circunstancia ejecutar un tercer refresh en esta subfase.

## Resultado — único intento real

- Configuración: Client ID `SET`, Client Secret `SET`, master key `VALID`.
- Descifrado server-side: access `PASS`, refresh `PASS`.
- Connection E.A.ZOCOOL: resuelta, activa.
- Credential version inicial/final: `1` / `1`.
- Lease inicial/final: `CLEAR` / `CLEAR`.
- Access inicial/final: expirado / expirado.
- Claim: ejecutado y sin `BUSY`; double-check: continuó por expiración.
- POST `/oauth/token`: ejecutado una sola vez desde la primitive; no se repitió.
- Resultado: `REFRESH_FAILED_CLASSIFIED`.
- Stage: `CAS_COMPLETE`.
- Safe code: `REFRESH_CAS_FAILED`.
- HTTP/provider code: no expuestos ni disponibles en este resultado.
- Persistencia de credenciales rotadas: no confirmada; credenciales anteriores preservadas.
- `GET /users/me`: `NO EJECUTADO`.
- Conteos remotos finales: Stores `1`, Connections `1`, Assignments `0`, `integration_secrets` `1`, Listings `0`.
- Remote writes: sólo las operaciones técnicas de claim/release; CAS no confirmó rotación.
- Token leakage: `NO`.
- Listing sync: `NO`.
- Código/migrations modificados: `NO`.
- Reconnect: recomendado antes de una nueva operación, no ejecutado.
- `git diff --check`: `PASS`.
- `git add/commit/push`: `NO`.

## Cierre — second real refresh CAS failure

El segundo y único retry real quedó cerrado como `REFRESH_CAS_FAILED` en `CAS_COMPLETE`. Mercado Libre aceptó el refresh y devolvió access/refresh rotados válidos; ambos existieron sólo en memoria. La persistencia CAS falló antes de actualizar la fila, por lo que el registro remoto sigue en `credential_version=1`, con access vencido y lease libre.

El refresh token persistido se considera potencialmente consumido/no reutilizable y no debe volver a usarse. No se autoriza un tercer refresh. Antes de reconnect debe diagnosticarse y corregirse la causa de `REFRESH_CAS_FAILED`; reconnect, OAuth, `GET /users/me` y listing sync no se ejecutan en este cierre. No se documentan tokens, ciphertext, secretos ni cuerpos crudos.

Para el cierre Git: auditar sólo `docs/meli-database.md`, `docs/meli-mercadolibre-OAuth/API.md` y este prompt; excluir `docs/prompts/phase-02/2.20e-first-real-listing-sync.md`; hacer staging explícito, comprobar `git diff --cached --check`, crear `docs(auth): record real refresh cas failure` y pushear `develop`. Si el hook pre-push queda bloqueado exclusivamente por el build concurrente, se permite `--no-verify` sin ejecutar un build manual.
