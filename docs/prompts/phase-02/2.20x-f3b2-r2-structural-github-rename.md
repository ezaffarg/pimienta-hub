# Fase 2.20X-F3-B2-R2 — Structural skill + GitHub rename

Fecha: 2026-08-31. Estado: **STRUCTURAL LOCAL PASS / GITHUB BLOQUEADO**.

## Base y preservación

R1 cerró en `531ba753510b691752ad7753fea1720a7591b306`. Antes de R2,
`HEAD == origin/develop`; el working tree acumulado conservaba 19 archivos
tracked y 50 archivos untracked de bloques X anteriores. R2 no restaura,
formatea, stagea ni modifica ese trabajo.

## Alcance R2

El alcance local renombra únicamente la skill propia
`e-ngenieria-architecture` a `pimienta-hub-architecture`, su directorio, YAML y
referencias activas. La carpeta raíz local, `supabase.project_id`, containers,
volumes, migrations, SQL, runtime y sistemas externos permanecen intactos.

El repositorio GitHub debe renombrarse de `e-ngenieria-hub` a `pimienta-hub`
después del commit estructural. El precheck encontró que GitHub CLI no está
instalado, por lo que el rename remoto requiere acción del usuario y no se
intenta mediante otro mecanismo en este bloque.

## Checkpoint

El directorio anterior ya no existe y
`.agents/skills/pimienta-hub-architecture` conserva sus 14 archivos con
`name: pimienta-hub-architecture`. La búsqueda deja cero referencias activas al
nombre anterior y dos menciones permitidas en prompts. El validador oficial no
pudo iniciar porque el entorno carece de `PyYAML`; la revisión manual de
frontmatter, nombre y referencias pasó sin instalar dependencias.

El commit y push estructurales se realizan de forma selectiva. El rename GitHub
queda bloqueado por ausencia de GitHub CLI autenticado. El rename manual de la
carpeta local se difiere hasta cerrar VS Code, Codex y los procesos que usen el
path actual.
