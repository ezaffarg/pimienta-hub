# FASE 2 — 2.20A MERCADO LIBRE READ-ONLY ACCOUNT PROBE
# GPT-5.6 TERRA + MEDIUM
# PRIMERA LECTURA REAL DESDE LA CONNECTION PERSISTIDA
# NO MODIFICAR DATOS EN MERCADO LIBRE

Proyecto: e-ngenieria Hub. Rama: develop. Base: `09c3593`.

Estado aprobado: Owner persistente 1; Store `E.A.ZOCOOL` 1; Connection Mercado Libre activa 1; `integration_secrets` 1; Assignments 0; OAuth y onboarding validados realmente.

Objetivo: comprobar desde backend server-only que e-Hub reutiliza la Connection persistida y sus credenciales cifradas mediante una única lectura real `GET /users/me`. No modificar publicaciones, ventas, stock, precios ni configuración.

1. Auditar sólo Connection/integration secrets primitives, cifrado/descifrado, cliente HTTP Mercado Libre, refresh actual, auth/context server-side y documentación API vigente.
2. Resolver desde contexto server-side Owner → Organization → Store `E.A.ZOCOOL` → Connection Mercado Libre activa. Browser no autoriza Organization, rol, provider account ID, tokens ni external account ID.
3. Leer `integration_secrets` y descifrar exclusivamente en memoria con `INTEGRATION_SECRETS_MASTER_KEY`; no enviar/loggear/persistir plaintext.
4. Revisar expiración: usar access token válido; si está expirado o próximo a expirar, usar refresh token oficial server-side. Validar respuesta con Zod, cifrar y actualizar secrets sólo si rota, sin perder credencial válida ante fallo.
5. Antes de refresh real, revisar protección de concurrencia por Connection. Si no es segura, detenerse antes de refresh; no improvisar una solución.
6. Con token válido, llamar únicamente `GET /users/me`, validar respuesta y usar sólo `id` para verificación interna, nickname y campos mínimos opcionales. Comparar `String(usersMe.id)` con `Connection.external_account_id`; ante mismatch, detenerse y no modificar Connection.
7. No crear UI; preferir primitive/service server-only. No ejecutar PUT/PATCH/DELETE ni mutaciones de Mercado Libre. Las únicas escrituras remotas permitidas son una rotación cifrada de secrets tras refresh necesario; no crear Store, Connection, Assignment, OAuth attempt ni pending.
8. Lectura ordinaria no requiere audit event; nunca registrar tokens en metadata.
9. Si cambia TypeScript, añadir tests para decrypt server-only, token válido/sin refresh, refresh/rotación cifrada, respuesta inválida, users/me, identity mismatch y redacción; ejecutar tests, typecheck y lint. Build sólo si cambia route/UI. Ejecutar `git diff --check`.
10. Tras una única lectura real, confirmar Store/Connection/secrets 1, Connection active, Assignments 0, Owner 1, identity match y cero mutación de negocio. Actualizar sólo `docs/meli-mercadolibre-OAuth/API.md` y este documento; `meli-database.md` sólo si hubo refresh relevante. No `git add`, commit ni push. Entregar checkpoint y detenerse.

## Resultado 2.20A

Se resolvió server-side la Connection Mercado Libre activa de la Store `E.A.ZOCOOL` y sus credenciales cifradas. El descifrado se realizó únicamente en memoria. El access token todavía estaba vigente, por lo que no hubo refresh, rotación ni escritura remota.

La única llamada real `GET /users/me` pasó. La identidad obtenida coincidió internamente con la Connection persistida y el displayName fue `E.A.ZOCOOL`. El postcheck mantuvo Store 1, Connection activa 1, `integration_secrets` 1, Assignments 0 y Owner persistente 1; no hubo mutaciones de negocio ni audit event adicional. No se registraron tokens, códigos, ciphertext ni identificadores técnicos.

## Cierre — 2.20A real account probe

# CIERRE 2.20A — REAL READ-ONLY ACCOUNT PROBE
# GPT-5.6 LUNA
# SOLO DOCUMENTACIÓN + GIT

Estado aprobado: Store `E.A.ZOCOOL`; Connection active; `GET /users/me` real e identity match PASS; descifrado server-only PASS; access token vigente; refresh no requerido; remote DB writes, mutaciones de negocio y token leakage: NO.

1. Auditar `git status --short`, `git diff` y `git diff --check`; sólo pueden aparecer `docs/meli-mercadolibre-OAuth/API.md` y este documento.
2. Confirmar que no se documentan access/refresh tokens, ciphertext, authorization code, state, client secret ni master key.
3. No repetir tests, typecheck, lint, build, OAuth, `GET /users/me` ni DB writes.
4. Stagear explícitamente sólo ambos documentos sin `git add .` ni `git add -A`; ejecutar `git diff --cached --check`.
5. Crear `docs(meli): record real readonly account probe`, hacer push a `origin/develop` y confirmar árbol limpio y `HEAD == origin/develop`.
6. Entregar hash, mensaje, archivos, higiene, push, sincronización, probe e identidad PASS, refresh NO y mutaciones NO; detenerse.
