# Fase 1 — Subfase 1.2: Server tenant context

Estado: COMPLETED. Commit: `4da2dcb`.

## Prompt archivado

Implementa únicamente el contexto server-side de identidad y Organization. Resuelve sesión y `orgId` en servidor, define un contrato reutilizable para handlers y protege las rutas demo autorizadas. No implementes roles, Store scope, persistencia, OAuth ni lógica de negocio. Los datos enviados por el navegador no pueden convertirse en autoridad de tenant.

## Resultado

Se estableció el contexto de sesión/Organization y el contrato HTTP inicial. El alcance quedó limitado al tenant de identidad; la autorización fina y el aislamiento de recursos se reservaron para las subfases siguientes.
