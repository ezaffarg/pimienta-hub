# Fase 1 — Checkpoint arquitectónico previo a 1.1

Estado: COMPLETED / reconstruido desde `fase1.txt`.

> **Procedencia:** reconstrucción semántica del mandato histórico; no es una transcripción literal del prompt original.

## Prompt archivado

Antes de implementar testing o seguridad, audita la arquitectura actual contra estas reglas: `features/` representa capacidades del producto; `integrations/` representa proveedores externos; una Store no contiene una copia de Mercado Libre; una implementación de Mercado Libre sirve a múltiples Stores; cada Store tiene su propia conexión; Clerk resuelve identidad y Organization; Supabase será PostgreSQL, no Auth; `application/` y `domain/` no se crean anticipadamente.

Inspecciona el árbol relevante, imports entre capas, modelos que asuman integración por Organization y cualquier mezcla de UI, dominio, persistencia e integración. No modifiques nada. Indica inconsistencias, GAP, cambios mínimos por fase y si algo bloquea 1.1.

## Resultado

Quedó fijado históricamente el modelo conceptual `Organization → Store → ExternalConnection`. La entidad de conexión fue posteriormente consolidada y nombrada simplemente `Connection`, por lo que el modelo vigente se expresa como `Organization → Store → Connection → Provider`.

Las capas `application/` y `domain/` permanecieron diferidas hasta que existiera una responsabilidad real que justificara su creación. Mercado Libre debía implementarse como adapter reutilizable, no como código duplicado por tienda.

Commit: PENDING / decisión arquitectónica posteriormente persistida en el repositorio.
